# Security Diff — 2026-09-22

Commits scanned: 40 in the last 24h (`ba2f1af5`…`07a5382e`)
Security-sensitive files changed: 120 classifier-flagged files; the remaining 57 changed files were also triaged against the behavioural question and path index.

Scan window: `be1259ec..07a5382e`, captured 2026-09-22T09:03:36Z.

## Classifier output

Changed files: 177 — content-flagged: 120.

| changed-file group | classifier flags observed | review result |
|---|---|---|
| `src/wallet-core/**`, keystore, recovery shares, EVM send | crypto, credential-floor, deniability, shared-store | Read in full; audit remediation and panic-wipe repairs, no new regression found. |
| `src/lib/WalletProvider.jsx`, `tracking-integration.jsx`, `referral.js`, `src/api/**` | egress, shared-store, deniability | Read in full; primary-session and consent/deniability guards remain in place. |
| `src/pages/{SendCrypto,SecurityCenter,NewsSentimentPage,WalletSeedQR}.jsx`, security settings | gate, shared-store, deniability, crypto, security-claim | Read in full; source changes tighten controls or correct copy. |
| `src/lib/WalletConnectProvider.jsx`, WalletConnect modal/tests, `src/risk/**` | gate, egress (file), security-claim | Read in full; approvals are decoded, scam/invalid sessions rejected, and literal recipients required. |
| `functions/**`, `supabase/functions/**`, `sql/**`, deploy workflows | egress, secret, security-claim | Read in full; source hardening is present. Live deployment state remains outside this static scan. |
| Android/iOS native configuration and tests | os-state, crypto | Read in full; release cleartext and privacy-cover changes tighten native posture. |
| tests and documentation | all classifier categories, including two `test-weakened` syntactic signals | Reviewed: the classifier-test signal is a fixture containing `it.skip`; the webhook test replaces weaker acceptance expectations with strict 401/503 checks. Neither removes a live security pin. |

The classifier's unflagged 57 files included CI/build metadata, UI/accessibility changes, test harnesses, dependency metadata, and reports. They were still triaged. No real finding arose from an unflagged file, so no classifier-rule or scan-list addition is proposed.

## Flagged changes

### `src/wallet-core/panic.js:1310` / `src/lib/tracking-integration.jsx:439` — SAFE
**Commit:** `ffda3dfd` fix(panic): cancel pending OS notifications on panic wipe (#2695)

**Change summary:** `panicWipeLocal()` now awaits cancellation of every notification id the reminder module can schedule, including dormancy id 9005. The companion regression pin forces the native path and verifies ids 9001–9005 reach `LocalNotifications.cancel`.

**Assessment:** This resolves the 2026-09-21 panic-residue regression in source. The cancellation is best-effort where the plugin is unavailable, which is appropriate for web/test environments; native device execution was not performed in this scan.

### `src/pages/SecurityCenter.jsx:52` / `src/components/security/TheftProtectionSettings.jsx:93` — SAFE
**Commit:** `80acddf9` fix(theft-protection): require step-up to loosen a spend limit or turn protection off (#2696)

**Change summary:** Disable, delete, and edit/raise operations now run the supported Theft Protection biometric gate before the configured second factor; creating or tightening a limit remains ungated. Turning Theft Protection off receives the same treatment.

**Assessment:** This resolves the unlocked-phone bypass identified in the prior report. Decoy/hidden sessions preserve the no-op setter/gate behaviour, and unsupported devices retain the existing escape hatch rather than being permanently locked out. INTERNAL source and unit-test evidence only.

### `src/pages/WalletSeedQR.jsx:176` — SAFE
**Commit:** `e8a8abbf` fix(seed-backup): remove the printed recovery phrase from the DOM after print, Clear, and unmount (#2699)

**Change summary:** The recovery phrase is cleared after print, explicit Clear, and component unmount; the added test covers all three paths.

**Assessment:** This reduces in-page seed residue (I1). No export, storage, or signing path was widened.

### `src/pages/SecurityDashboard.jsx:56` / `src/lib/fastpathUnlock.js:49` — SAFE
**Commit:** `56126fb7` fix(biometric): stop re-arming biometric unlock on every launch; dashboard no longer claims ON on web (SEC-05) (#2704)

**Change summary:** The dashboard reflects platform capability rather than showing a native-biometric state on web, and launch no longer re-arms the biometric fast path.

**Assessment:** This is an I4 honesty correction and removes unnecessary OS-state mutation. It does not claim device verification.

### `src/components/SessionRevocationGuard.jsx:48` — SAFE
**Commit:** `63fe0dac` fix(sessions): revoking the current device now locks the session that minted the token (SEC-03) (#2708)

**Change summary:** Session revocation compares the current minted token/session and locks immediately when it is the revoked one.

**Assessment:** Static inspection shows the current-session branch now reaches the existing lock path rather than leaving the revoked token usable in the UI.

### `src/pages/NewsSentimentPage.jsx:47` — SAFE
**Commit:** `c7759e60` fix(deniability): gate NewsSentimentPage's shared-store read in decoy sessions (#2711)

**Change summary:** The page disables and blanks its shared-store query in decoy/hidden sessions, with a deniability regression test.

**Assessment:** This resolves the shared-store K-2/I3 leak reported in the prior scan. No backend request is introduced.

### `src/wallet-core/panic.js:124` / `src/lib/WalletProvider.jsx:2355` — SAFE
**Commit:** `3ba2406c` fix(panic): stop the wipe re-creating the biometric pref, and sweep the nav-route session keys (#2712)

**Change summary:** Wipe no longer recreates the biometric preference, and the metadata residue sweep covers navigation-route session keys.

**Assessment:** The new keys are included in the residue tests, preventing a false `clean` outcome in the covered source path.

### `src/wallet-core/wipeEpoch.js:1` / `src/wallet-core/provisionChaff.js:74` — SAFE
**Commit:** `725c6c77` fix(panic): stop in-flight chaff provisioning re-creating storage after a wipe (#2725)

**Change summary:** Chaff provisioning records and checks a wipe epoch before committing generated state.

**Assessment:** The commit closes a wipe race in source: post-wipe async work no longer recreates chaff/stealth storage. Regression coverage exercises the race; no device test was run here.

### `src/api/referralApi.js:83` / `src/lib/WalletProvider.jsx:2124` — SAFE
**Commit:** `6ac2d59d` fix(referral): classify redemption failures by status and cap retries (#2640) (#2727)

**Change summary:** Redemption preserves HTTP status, clears only permanent non-429 4xx failures, bounds transient retries, and sweeps the retry counter on panic wipe. Retry/failure telemetry carries only a reason enum through the existing consent- and deniability-gated chokepoint.

**Assessment:** The source keeps redemption off decoy/hidden paths and does not transmit a referral code or wallet material. The SQL allowlist was corrected by `52a4556f`; its production application is an external-state assertion, not newly verified by this scan.

### `src/wallet-core/keystore/kekProfiles.js:1`, `src/risk/calldata.js:1`, `functions/**`, `supabase/functions/**`, and native/build files — NEEDS-REVIEW
**Commit:** `2e17c655` fix(security): independent audit 2026-09-21 — remediate all findings (#2731)

**Change summary:** This bundle adds bounded historical KEK-C profile fallback and stamps successful profiles; decodes approval variants and requires literal transaction recipients; tightens webhook defaults, mobile release/network policy, RPC/edge proxy forwarding, server entitlement/rate controls, fee caps, custom-RPC policy, recovery-share profiles, and deploy provenance. It also includes a production-revoke SQL record and a new Production-environment Edge Function deployment workflow.

**Assessment:** The source changes are fail-closed in the reviewed paths: unknown KDF profiles reject before allocation; malformed/ambiguous approval or recipient input rejects; missing strict webhook secrets return 503; non-production RevenueCat events are ignored; and production release builds now invoke `build:release`. Tests added or revised alongside these paths strengthen rather than weaken coverage. However, this static review cannot establish that all new workflows have run, secrets/environment approvals are correctly set, native release artifacts contain these changes, or Supabase/Pages functions currently match this ref. The SQL file records production application and intentionally excludes staging; that statement was not independently checked. Treat runtime/deployment confirmation as outstanding operational review, not as code verification.

### `supabase/functions/tip-screen/index.ts:196` / `.github/workflows/deploy-preview.yml:78` — SAFE (source only)
**Commit:** `f43de6ad` fix(tip-screen,ci): key the tip-screen limiter on the forwarded client IP; bind production Pages deploys to the Production environment (#2732)

**Change summary:** `tip-screen` adds a bounded per-isolate rate bucket keyed from the proxy-forwarded client address, while the Pages production deploy is environment-bound.

**Assessment:** The Pages proxy overwrites `X-Forwarded-For` from `CF-Connecting-IP`, so the source route does not trust a caller-supplied forwarded value. Per-isolate limiting is explicitly only a smoothing bound, not represented as authentication. Deployment execution remains covered by the operational item above.

### `docs/transak-relay.md` / `docs/Feature-Status.md` — NEEDS-REVIEW (status records)
**Commits:** `04353df6` docs(transak): record the static-egress relay as active on prod (#2729); `e0ccb23b` docs(transak): record native Buy as confirmed; web still unverified (#2730)

**Change summary:** The documents record a production relay session result and owner native Buy checks, while explicitly retaining web-widget, bounded-401, allowlist, monitoring, and device-evidence limits.

**Assessment:** The wording is materially more candid than an unsupported "verified" label, and no source behavior changes here. These are external operational claims; this Git-only run cannot independently confirm the relay, the deployment, or the described owner checks. They must not be read as independent audit evidence or on-chain verification.

## Prior-report follow-ups

The 2026-09-21 report's source findings are addressed in this window: pending-notification wipe residue (`#2695`), spend-limit/Theft Protection step-up (`#2696`), WinPaywall paid-feature copy (`#2698`, `#2700`, `#2710`, `#2724`), the NewsSentiment decoy read (`#2711`), and Security Center wording (`#2716`). These are BUILT/source-tested changes; this report does not promote them to device-verified or independently audited.

## Non-security changes (summary)

Version/build-number changes, documentation archival, accessibility and typography work, UI copy, package metadata, test-harness updates, and the 29-package minor/patch dependency group were reviewed for changed security behaviour. No additional regression was found.

## Scan-list maintenance

No new maintenance item. The new changed-file classifier surfaced the OS-state, shared-store, egress, gate, credential, secret, crypto, and claim surfaces reviewed above. Its two syntactic test-weakened alerts were false positives after manual diff review, not missed security findings.

## Post-scan re-check

Verified against `origin/main` at `07a5382ec0251edb1bcb1f8ef78c1f4c3ebf63a9` (2026-09-22T09:06:46Z). Nothing landed between the window-close tip and this checkpoint.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, no device verification, no deployment/database confirmation, and no on-chain confirmation. Not the outstanding independent third-party audit.*
