# Security Diff — 2026-07-16

Commits scanned: 13 in last 24h (2026-07-15, 08:27 → 18:30 +0100)
Security-sensitive files changed: 17
Overall verdict: **SAFE** — no regressions. Every change is additive detection,
fail-closed hardening, a honest-scope doc correction, or a stale-verdict-window
closure. No security control was removed or weakened; the one deleted file
(`resolveProbeSource.js`) was itself a fail-OPEN path already superseded by a
fail-CLOSED replacement.

Diff range: `1dcc68b3~1..HEAD` (b154f823). Static analysis only — no dynamic testing.

## Flagged changes

### android/.../HardwareKekPlugin.kt:284 — SAFE (strong positive)
**Commit:** ee47ae39 feat(rasp): native signing gate — isBlockTier closes JS presignGate bypass (#1008)
**Change summary:** `getHardwareFactor()` now calls `RaspIntegrityPlugin.isBlockTier(context)` and rejects with `RASP_BLOCK` before the hardware H factor is ever returned.
**Assessment:** Closes a real bypass — a JS-layer `presignGate` circumvention could previously still reach the native KEK factor. Gate is enforced at the native layer; `isBlockTier` is fail-closed (`getOrElse { true }`). H is never returned on hook/tamper/screen-capture → vault unlock impossible (I4). No key material added to any log path.

### android/.../RaspIntegrityPlugin.kt:835 — SAFE
**Commit:** ee47ae39 (#1008) + b5477446 (#1007) + 8ff8fd18 (#1014)
**Change summary:** Adds `isBlockTier()` static (fail-closed), `earlyPtraceTraceme()` (JNI PTRACE_TRACEME anti-debug), `earlyCheckJdwp()` + `earlyCheckScreenCapture()` early gates, and eight new post-bridge WARN-tier signals (checkOverlay, checkDeveloperMode, checkVirtualApp, checkSuspiciousPackages, checkThirdPartyKeyboard, checkMockLocation, checkNetworkProxy, checkAccessibilityService) + `checkJdwpDebugger`/`checkScreenCapture`. Removes the structurally-inert `checkProcNetUnix`/`checkSuFromRuntime` (SELinux/Runtime.exec-blocked on Android 10+).
**Assessment:** All additive detection; every block is independently `runCatching`-guarded and fails to `false` (not-detected → JS maps absence to WARN, never fabricated CLEAN). BLOCK-tier early set stays minimal (hook + tamper + screen-capture); the eight new signals are WARN-tier only. Removed checks were dead on modern devices and were superseded, not silently dropped (documented, item 9). Note (non-blocking): several new WARN signals (thirdPartyKeyboard, networkProxy, mockLocation, developerMode, accessibilityService) can raise false-positive CAUTION friction for legitimate power users — WARN not BLOCK, honestly scoped in code comments; appropriate for a wallet send flow. Honesty / evidence-integrity item (not cosmetic): the 2026-07-12 device-verified header verdict in `RaspIntegrityPlugin.kt` was rewritten to list 10 newer detection fields (debuggerAttached, screenCapture, overlayActive, developerMode, virtualApp, suspiciousPackage, thirdPartyKeyboard, mockLocation, networkProxy, accessibilityService) as `false`, but the code producing those axes did not exist until these very PRs (#1007/#1008, 2026-07-14/15). The CLAUDE.md 2026-07-12 record captured only the 4-field verdict `{"rooted":false,"hookedProcess":false,"emulator":false,"tampered":false}`. A dated device-verification record asserting observed-`false` values that were never captured on that device on that date overstates the scope of the verification — the same overstated-evidence class the project has treated as substantive elsewhere (F2.1 contradiction; the correction banner on `device-verification-2026-07-05.md`), and a `verify-don't-assert` / I4 concern, not a code correctness issue. **Resolved 2026-07-16:** `RaspIntegrityPlugin.kt` header restored to the actual 4-field 2026-07-12 verdict, with an EVIDENCE SCOPE note recording that the 10 newer axes (added by #1007/#1008) have not yet been captured in a dated all-fields device verdict and must not be backfilled into an earlier record.

### android/app/src/main/cpp/rasp_early.c — SAFE (new file)
**Commit:** b5477446 (#1007)
**Change summary:** New JNI wrapper `nativeEarlyTraceme()` calling `ptrace(PTRACE_TRACEME)` — preventive anti-debug + reactive detection (EPERM ⇒ debugger already attached ⇒ BLOCK signal).
**Assessment:** Pure hardening, no key material, no egress (I2). Fail-open on library-load / UnsatisfiedLinkError is acceptable — it is a hardening layer complementing PR_SET_DUMPABLE=0, not the sole detection gate, and it never fabricates a clean verdict.

### ios/App/App/RaspIntegrityPlugin.m:118 — SAFE
**Commit:** b5477446 (#1007)
**Change summary:** Adds `checkDebugger` (sysctl P_TRACED) surfaced as `debuggerAttached`, `earlyAntiDump` (clears Mach exception ports), `earlyCheckDebugger`, `earlyCheckScreenCapture`; header advanced BUILT-UNVALIDATED → DEVICE-VERIFIED (INTERNAL, 2026-07-14, palera1n + Frida-stub) with honest gaps preserved.
**Assessment:** Additive detection + hardening; all detection paths `@try`-guarded, fail to `NO` (not-detected). `earlyCheck` BLOCK set extended with debugger + screen-capture. Status advance is legitimate per CLAUDE.md 2026-07-13/14 records, labeled INTERNAL with explicit honest gaps (per-vector attribution UI-only; `tampered:false` cert-pin parity gap; independent audit outstanding). A RASP detection feature is META evidence — no on-chain txid required and none claimed (verify-don't-assert respected).

### ios/App/App/AppAttestPlugin.m — SAFE (honesty)
**Commit:** 855f26e8 (#1013)
**Change summary:** Comment-only P2-5 rescoping — a successful local `generateAssertion` proves only "this install still holds its SE-enrolled key", NOT "device not jailbroken." No logic change.
**Assessment:** Honesty correction (I4). Correctly explains the compose lattice makes CLEAN-from-this-leg safe because OS-probe TAMPERED/HOOKED outranks it. Reduces risk of a future maintainer over-trusting the assertion leg. I5 (backend untrusted) preserved — no server-side leg added.

### ios/App/App/HardwareKekPlugin.m:334 — SAFE
**Commit:** b5477446 (#1007)
**Change summary:** Wraps the decrypted H mutable buffer in `mlock`/`munlock` around `resetBytesInRange` zeroing.
**Assessment:** Positive hardening — pins key pages in RAM to prevent swap-to-disk during the SE-decrypt→zero window. Fail-open on `mlock` error is acceptable; `resetBytesInRange` zeroing unchanged, `munlock` after zeroing. Known iOS-F5 base64 residue is unchanged (pre-existing, documented).

### ios/App/App/App.entitlements — SAFE (new file, config)
**Commit:** b5477446 (#1007)
**Change summary:** New entitlements plist declaring `com.apple.developer.devicecheck.appattest-environment = development`.
**Assessment:** Enables the App Attest key path (previously an honest gap). `development` environment is correct for non-App-Store builds. No secret material. Production release must switch to `production`.

### src/rasp/getFreshRaspArtifact.js — SAFE (new file, strong positive)
**Commit:** 422ddddc (#1012)
**Change summary:** New module-level `getFreshRaspArtifact()` awaiting OS + attestation probes fresh under a 1500 ms fail-closed timeout, then compose+degrade; any timeout/throw/shape-drift → `TIER.BLOCK`.
**Assessment:** Closes the stale-verdict window on the sign hot-path (mount-time state could be ~60 s stale). Mirrors the audited WalletConnect `presignGateOrReject` pattern. I3 preserved (attestation leg checks `isDeniabilityOrDemoActive()` internally); I4 fail-closed throughout — never fabricates CLEAN.

### src/pages/SendCrypto.jsx:706 — SAFE (strong positive)
**Commit:** 422ddddc (#1012)
**Change summary:** Removes ~140 lines of inline probe-sampling effects; display gate now via `useRaspArtifact({ deferAttestation: step !== 'verify' })`; sign hot-path now `await getFreshRaspArtifact()` and uses `freshRaspTier` for `presignGate`, RASP notification, and biometric enforcement.
**Assessment:** Strengthens the signer chokepoint — fresh fail-closed re-probe at sign time instead of a possibly-stale closure value. All `?? TIER.BLOCK` fail-closed defaults preserved. `deferAttestation` ties attestation egress to explicit sign intent (I2/I3 positive — no network call on Send-page mount). No weakening of any gate.

### src/rasp/useRaspArtifact.js — SAFE
**Commit:** 422ddddc (#1012) + 8ff8fd18 (#1014) + 855f26e8 (#1013)
**Change summary:** Adds `deferAttestation` option (attestation network call only on sign intent); invalidates attestation on foreground + heartbeat; P2-9 moves the `BYPASS_RASP` early-return BELOW all hook calls (rules-of-hooks); exposes composed `condition` in the returned artifact.
**Assessment:** Fail-closed semantics unchanged (`detect` throw → BLOCK; `degrade(undefined)` on catch). BYPASS is a compile-time env constant, dev-only. Attestation freshness gap closed. Superset return shape — existing consumers unaffected.

### src/rasp/nativeProbe.js — SAFE
**Commit:** b5477446 (#1007)
**Change summary:** Folds new native verdict fields into signals — overlay/developerMode/virtualApp/suspiciousPackage/thirdPartyKeyboard/mockLocation/networkProxy/accessibilityService → `rooted` (WARN); debuggerAttached/screenCapture → `hooked` (BLOCK). Retains the P2-6b fail-closed shape validation.
**Assessment:** Additive; all new fields gated by `=== true` so absence → false (cross-platform safe, iOS verdicts without Android-only fields still pass). P2-6b partial-shape rejection preserved (a `{}` or one-field partial still fails closed → UNAVAILABLE, not fabricated CLEAN). Same WARN false-positive note as the .kt entry (WARN, not BLOCK).

### src/rasp/attestation.js — SAFE (honesty)
**Commit:** 855f26e8 (#1013)
**Change summary:** Comment-only — documents that on iOS a CLEAN attestation result means "SE key intact", not "device integrity confirmed", and that the compose lattice makes this safe.
**Assessment:** Honesty (I4). No logic change.

### src/rasp/index.js — SAFE
**Commit:** 8ff8fd18 (#1014) + 422ddddc (#1012)
**Change summary:** Removes the `resolveProbeSource` export (legacy fail-open chooser), adds `getFreshRaspArtifact`/`FRESH_PROBE_TIMEOUT_MS`; updates honesty comment (Play Integrity JWS RS256/ES256 now on-device verified; residual is G2-ROOTCERT-PIN).
**Assessment:** Removing the fail-open export is a net security improvement — no live consumers remained (superseded by fail-closed `selectPresignProbeSource`, C-01/PR #825). Honesty comment now accurate.

### src/rasp/resolveProbeSource.js — SAFE (deleted)
**Commit:** 8ff8fd18 (#1014)
**Change summary:** Deletes the legacy fail-OPEN native/browser probe chooser.
**Assessment:** Deletion of a fail-open path already replaced by the fail-closed `selectPresignProbeSource`. No live consumers; removing it eliminates the risk of a future callsite reintroducing the browser-CLEAN fallback on native. Security-positive.

### src/pages/RaspSecurity.jsx — SAFE
**Commit:** 855f26e8 (#1013)
**Change summary:** Dashboard swaps inline once-per-mount native sampling for the shared `useRaspArtifact()` hook (foreground/heartbeat re-probe + attestation axis composed).
**Assessment:** Closes a display-only inconsistency where the dashboard could render "clean/allow" while the Send flow correctly composed BLOCK. Fail-closed default (`artifact?.tier ?? TIER.BLOCK`). Deniability preserved (environment-derived, no wallet-set handle — renders byte-identical across primary/decoy).

### src/components/FeeSelector.jsx:87 — SAFE (I3 positive)
**Commit:** a3839427 (#1011)
**Change summary:** Wraps each chain's fee-estimate `queryFn` in a live `isDeniabilityOrDemoActive()` gate that throws `FEE_ESTIMATE_SUPPRESSED_DENIABILITY` before touching any fee provider.
**Assessment:** Closes issue #977 — react-query's 30 s `refetchInterval` re-invokes `queryFn` independent of re-render, so a mid-session `veyrnox-demo` flip could keep leaking the (real, under Trezor) address to a third-party RPC. Live check inside the queryFn fails closed before egress (I3). Degrades gracefully on throw (I4).

### src/pages/DuressPin.jsx:332 — SAFE (copy only)
**Commit:** 1dcc68b3 copy(duress): D-02 disclosure reflects H-1 timing equalizer (#1001)
**Change summary:** Disclosure copy updated to reflect that unlock now spends equal effort on real vs Emergency PIN (H-1 timing equalizer), replacing the old "real PIN unlocks faster" wording.
**Assessment:** User-facing copy change only; brings the disclosure in line with the shipped timing-equalizer behavior. No logic, no key/auth path touched.

## Non-security changes (summary)
`.claude/settings.json` (#1020 sandbox dep-install hook); `.github/workflows/ci.yml` + `scripts/check-cert-pin-manager-safety.mjs` (#1014 CI guard — security-adjacent, blocks CertPinManager use with placeholder pins, safe); `android/app/build.gradle`, `android/app/src/main/cpp/CMakeLists.txt`, `ios/App/App.xcodeproj/project.pbxproj` (#1007 build wiring for rasp_early / entitlements); `package.json` (#1014/#1018 script wiring); `scripts/phase2-checklist-runner.mjs`, `scripts/phase2-evidence-ledger.mjs`, `docs/*` (#1015–#1019 audit-cycle docs, Feature-Status, TEST_COVERAGE, verified-evidence.json); test files under `src/**/__tests__/**` (#1007/#1011/#1012/#1013/#1014).

---
*Automated daily scan. Static analysis only — no dynamic testing. INTERNAL analysis — not an independent audit; no on-chain verification performed or claimed.*
