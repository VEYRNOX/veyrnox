# Security Diff — 2026-07-12

Commits scanned: 61 in last 24h (window `e92227bc`..`be51e314`, 2026-07-11 09:59 → 2026-07-12 07:56)
Security-sensitive files changed: 12 flagged-path production files (+ 3 keystore test files, + iOS RaspIntegrity native trio)

> Large batch: yesterday's scan (`diff-2026-07-11.md`) only caught 4 commits (#778–#781);
> today's window opens one commit later (#782) and spans a full day of high-velocity work.
> No overlap with the prior report. Net diffs assessed across the whole range per file.
> **Overall verdict: every security-sensitive change is a strengthening or a fix — no regression detected.**

## Flagged changes

### src/rasp/selectPresignProbeSource.js (new) — SAFE
**Commit:** 7633c308 fix(rasp): fail closed on native in the pre-sign gate (C-01) (#825)
**Change summary:** New pure, no-egress function that picks detect()'s ProbeSource by platform. On native it trusts the OS leg ONLY when `available === true`, otherwise fails closed to `UNAVAILABLE_PROBE_SOURCE` → `INTEGRITY_UNAVAILABLE` → `degrade()` → WARN — it never falls back to the browser leg's hard-coded CLEAN.
**Assessment:** Directly closes a fail-OPEN hole (C-01 CRITICAL): the old `resolveProbeSource` fell back to the always-`available:true`/CLEAN browser leg on native, so a rooted device with an absent/thrown/not-yet-sampled OS probe could reach `TIER.ALLOW` and sign with zero friction. New seam is fail-closed (I4), pure (no key, no walletSet handle → I3-safe), no egress (I2). Strong positive.

### src/rasp/resolveProbeSource.js (new) + src/rasp/index.js — SAFE
**Commit:** dcf8db11 feat(rasp): F-09 native RASP probe wired (PARTIAL) (#814); 7633c308 (#825)
**Change summary:** `resolveProbeSource` (older seam, prefers native-when-ran else browser) added and exported alongside `nativeProbeSource` and `selectPresignProbeSource` via `index.js`.
**Assessment:** `resolveProbeSource` is superseded on the Send chokepoint by `selectPresignProbeSource` (#825); it remains the F-09 composition helper. Both are pure and never fabricate a clean source. Export-surface only in `index.js`. SAFE.

### src/rasp/nativeProbe.js + src/rasp/raspIntegrityPlugin.js — SAFE
**Commit:** dcf8db11 (#814); cb065cee (#826); f6fa1992 (#830)
**Change summary:** Header/status comments updated from "native plugin NOT written / iOS NOT written" to "BUILT + device-verified (PARTIAL) 2026-07-11, INTERNAL" — Android Kotlin + iOS ObjC probes now exist and are registered. `checkIntegrity()` contract extended with `tampered?`.
**Assessment:** Comment/status changes only in these two JS files; the fail-closed bridge behaviour (plugin absent/throws → `INTEGRITY_UNAVAILABLE`, never fabricated clean) is unchanged. Honesty bar respected: status is explicitly PARTIAL/INTERNAL, records the exact gaps (rooted-signal + Send-WARN not captured on device, iOS device test not done, Frida test not done, not independently audited) — no unevidenced "verified" claim. SAFE.

### src/wallet-core/keystore/hardware.js — SAFE
**Commit:** 055bf8dd fix(security+ui): KEK biometric-invalidation lockout (#813); 028c8b37 Codex review (#783)
**Change summary:** `getHardwareFactor` now wraps the Kotlin bridge call in try/catch and classifies raw rejections into STABLE codes: `KEK_KEY_PERMANENTLY_INVALIDATED`, `KEK_USER_CANCELLED`, else `NO_HARDWARE_FACTOR`. Adds a null/non-object bridge-result guard (Codex P1 #3) and attaches `.code` to every validation throw.
**Assessment:** Prevents an irreversible-data-loss bug: WalletEntry's wrong-PIN wipe counter exempts on `e.code`, so a message-only or uncoded throw from a hardware invalidation / cancelled biometric sheet would previously be miscounted as a wrong PIN and march a correct-PIN user toward the panic wipe. All new paths fail closed (I4), carry stable codes, and never touch key material on error. Strong positive.

### src/wallet-core/keystore/kek.js — SAFE
**Commit:** 055bf8dd (#813)
**Change summary:** Adds two frozen error codes to `KEK_ERR`: `KEY_PERMANENTLY_INVALIDATED` and `USER_CANCELLED`, with doc comments mandating callers NOT count them toward the wrong-PIN wipe limit.
**Assessment:** Pure constant additions supporting the hardware.js classification above. No logic change. SAFE.

### src/wallet-core/keystore/native.js — SAFE
**Commit:** 90edde15 (#821, M-3); 028c8b37 Codex review (#783, L-4)
**Change summary:** (1) M-3: extracts `logM2cMigrationFailure(e)` — the dormant M2c up-migration in `unlock()` is still best-effort/non-fatal but now logs the failure's `code`/`message` ONLY (never the error object, vault blob, ciphertext, or key material — LOG-1 safe) instead of swallowing it silently. (2) L-4: a biometric-gated read now uses `parseVaultBlob(blobJson)` instead of raw `JSON.parse` (MALFORMED_VAULT fail-closed guard).
**Assessment:** Both improve observability/robustness without weakening any control. The log helper is deliberately string-coerced to prevent logger field-serialisation of attached key material. Migration remains non-fatal (secret already recovered). SAFE.

### src/wallet-core/vault.js — SAFE
**Commit:** 028c8b37 Codex review (#783, L-5)
**Change summary:** `deriveKekC` hoists the encoded password bytes to a local `pw` and `zero(pw)`s them in a `finally`, mirroring `deriveKey()`.
**Assessment:** Memory-hygiene strengthening — encoded password bytes are wiped even if Argon2id throws. No behavioural change to derivation output. SAFE.

### src/lib/WalletConnectProvider.jsx — SAFE
**Commit:** 028c8b37 Codex review (#783, L-1 + L-2)
**Change summary:** (1) L-1/F-07-WC: `eth_sendTransaction` now resolves the chain EXCLUSIVELY from the live session store (`getActiveSessions()` + `resolveSessionCaip2`) and rejects `SESSION_CHAINID_INVALID` before the key is touched if the session did not approve the chain — mirrors the typed-data path. (2) L-2: new pure `resolveMaxPriorityFeePerGas` clamps the dApp-supplied priority fee to `min(parsed, cappedMaxFee)`, defaulting to `0n` on absent/negative/unparseable input.
**Assessment:** Two fail-closed (I4) hardenings on the WC signing path: an unapproved-chain broadcast is blocked before key access (I1-relevant), and an uncapped/invalid EIP-1559 tip is clamped. No control removed. Strong positive.

### src/lib/WalletProvider.jsx — SAFE
**Commit:** e92227bc (#782); ff3711d5 (#806); eb41102a (#822); 785b0675 (#838, L-2)
**Change summary:** (1) L-2: `setActionPassword` in a decoy/hidden session now re-authenticates the supplied credential via `tryDuressUnlock`/`tryRevealHidden` and fails closed on mismatch (previously read `containerRef.current` with NO re-auth, letting an open decoy/hidden session install a new Action Password without proving knowledge of the duress/reveal secret). (2) `PRIMARY_UNLOCK_EQUALIZER_MS` 1500 → 2000 to re-match the 192 MiB KDF timing after the SAST-M3 memory revert (fixes deniability-timing test VU-06). (3) Portfolio address-map BTC derivation `testnet` → `mainnet` (public address only) + comment sync EVM/BTC/SOL to mainnet. (4) Re-exposes `validateMnemonic` and `withLockSuppressed` as context facades (ring-boundary burn-down).
**Assessment:** (1) is a genuine deniability/auth fix — closes an unauthenticated second-factor install in decoy/hidden sessions (I4). (2) restores the two-sided timing-equalizer bound so the padded success path stays within the 4-KDF miss window — a timing-oracle guard correction, pinned by `primaryUnlockEqualizer.test.js`. (3) is a mainnet public-address derivation change consistent with mainnet being live; no key egress (address only). (4) exposes read-only helpers. No control weakened. SAFE.

### android/app/src/main/java/com/veyrnox/app/HardwareKekPlugin.kt — SAFE
**Commit:** 055bf8dd (#813)
**Change summary:** Two `call.reject` sites now emit STABLE machine-code prefixes: `KEK_KEY_PERMANENTLY_INVALIDATED: …` on `KeyPermanentlyInvalidatedException`, and `KEK_BIOMETRIC_ERROR:<code>: …` on non-cancel biometric errors (previously bare prose strings).
**Assessment:** The native contract half of the hardware.js classification fix — bare prose fell through to the JS wrong-PIN counter → irreversible wipe. Now the JS layer maps the prefix to a coded error exempt from the counter and routes to seed recovery. Data-loss prevention, fail-closed (I4). SAFE.

## Security-adjacent changes (not on the flagged path list, noted for intent)

### src/wallet-core/duress.js — SAFE
**Commit:** 73be9aad fix(security): stealth PIN entropy disclosure + duress constant-time guard (#849)
**Change summary:** `tryDuressUnlock` now runs a dummy Argon2id (`encryptVault('__duress_timing_chaff__', …)`) on the no-decoy-vault path so absence is timing-indistinguishable from a wrong-password miss; mirrors `stealth.js:tryRevealHidden`.
**Assessment:** Closes a duress-presence timing side-channel (I3/deniability). Fail-closed, no key exposure. SAFE.

### src/risk/score.js — SAFE
**Commit:** 433fd1e9 fix(presign): CAUTION verdict now requires confirmation + RASP WARN acknowledge (#832)
**Change summary:** `requiresConfirmation` now true for `LEVEL.CAUTION` as well as `LEVEL.RISK`.
**Assessment:** Adds pre-sign friction (more, not fewer, confirmations). SAFE.

### src/wallet-core/sol/send.js — SAFE
**Commit:** ff3711d5 (#806, M-5)
**Change summary:** `planSolTransfer` rejects non-bigint `amountLamports` instead of silently `BigInt(...)`-coercing (which would bypass the caller's decimal-amount validation).
**Assessment:** Fail-closed input guard on a signing-path amount. SAFE.

### src/plugins/veyrnoxEnclave.js — SAFE
**Commit:** 90edde15 (#821, M-5)
**Change summary:** Adds `M2C_ENABLED = false`; `createWrappingKey`/`hwWrap`/`hwUnwrap` throw `M2C_DISABLED` while disabled; `isHardwareKeyAvailable` (read-only probe) and `deleteWrappingKey` (cleanup) stay ungated.
**Assessment:** Fail-closed gate on an auto-registered, page-reachable native plugin — blocks an injected script from minting an orphaned Enclave key or invoking wrap/unwrap while the M2c path is off. Cleanup intentionally ungated to avoid stranding keys. Comment mandates lockstep with `native.js` `M2C_HARDWARE_WRAP_ENABLED` and the Swift `m2cEnabled`. SAFE.

## Non-security changes (summary)
Large UI/dashboard/docs churn: crypto detail page + navigation (#784–#786, #798, #801, #802, #807), Send-page UI/logo/asset-selector polish (#787, #793–#797, #803, #811, #815, #834, #831), portfolio/analytics/gas-tracker (#788–#792, #808, #809, #816–#819), Receive page (#829, #842), converter/calculator logos (be51e314), push-notification deep-links + web disable (#804, #813, #846), security-nav copy humanization (#833, #836), StealthWallets PinPad + banner cleanup (#845, #847), SafetyPlus/tier routing (#845), 2FA reactivity + retry UX (#839, #840), HardwareKekSettings PIN-strength disclosure (#841), Codex two-developer protocol docs (#800, #805, #799), ruflo V3 runtime config (#810), CLAUDE.md/Feature-Status.md/SECURITY.md/QA doc syncs (#812, #821, #822, #828, #830, #835, #838), plus keystore/rasp test additions (`hardware.plugin-rejection.test.js`, `kek.key-invalidated-code.test.js`, `native.m2c-migration-log.test.js`, `kek.honesty-wider.test.js`, `selectPresignProbeSource.test.js`, `SendCrypto.raspNativeProbe.test.jsx`) and the iOS RaspIntegrity native trio (`RaspIntegrityPlugin.m/.h`, `RaspIntegrityPluginBridge.m`, Xcode target #826 — UNVALIDATED on device, Mac required).

---
*Automated daily scan. Static analysis only — no dynamic testing. INTERNAL analysis, not an independent audit. No on-chain verification performed; no status flipped to "verified".*
