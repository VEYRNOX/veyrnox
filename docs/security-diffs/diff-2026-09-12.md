# Security Diff — 2026-09-12

Commits scanned: 16 in last 24h
Window: `29e6c0a9` → `230637fd` (`origin/main` tip captured 2026-09-12T08:23:56Z)
Security-sensitive files changed: 35 of 36

## Summary

Two NEEDS-REVIEW items were found. The partner-referral migration retains its old
two-argument function on an already-upgraded database, so the documented default
partner tier does not reliably take effect. The new Theft Protection spending-limit
acknowledgement also extends the previously recorded device-credential fallback to
money movement. The panic-residue correction, wrong-PIN counter correction, buy-route
rendering change, and the remaining bounded referral implementation are SAFE on this
INTERNAL static review.

## Flagged changes

### N-1 — `sql/partner-referral-codes.sql:94-169` — NEEDS-REVIEW
**Commit:** `230637fd` `feat(referrals): get_referral_tier RPC + partner tier lock (platinum default) (#2528)`

**Change summary:** The migration adds `tier_override` and a three-argument,
service-role-only `mint_partner_referral_code(..., p_tier_override default
'platinum')`. `get_referral_tier` then exposes only the resulting tier key through
the allowlisted RPC; `referralApi.fetchReferralTier` allowlists that key before it
can select a RevenueCat offering.

**Assessment:** The comment says the old two-argument signature is dropped, but
line 98 drops `(text, text, text)`, not the pre-existing `(text, text)` function
introduced by `803bfb07`. On a database where #2526 was already applied, the old
service-role-only overload survives. A two-argument call resolves to that exact old
signature and inserts a NULL `tier_override`; a one-argument call can be ambiguous
because both overloads have defaults. This defeats the documented default-platinum
partner lock and can leave a partner referral offer fail-closed as `none`.

This is a migration/commerce-integrity defect, not an authorization widening: the
surviving old function remains service-role-only and is still absent from the Pages
RPC allowlist. Before production apply, replace the mistaken drop with an explicit
`DROP FUNCTION ... (text, text)`, re-issue its ACL only for the intended
three-argument signature, and verify `pg_proc` contains exactly that signature.
The scan did not query staging or production.

### N-2 — `src/pages/SendCrypto.jsx:994-1000, 1375-1528, 1745-1778` and `src/lib/WalletConnectProvider.jsx:672-702` — NEEDS-REVIEW
**Commit:** `4ba80bbf` `feat(theft-protection): enforce spend limits with a fresh biometric gate`

**Change summary:** An over-limit primary-session send now obtains a one-shot
Theft Protection token before the shared signing chokepoint; WalletConnect receives
the same in-band path. Failed or unavailable valuation/cap reads still reject.
Tests pin the one-shot token, stale-checkbox rejection, primary-session condition,
and WalletConnect rejection paths.

**Assessment:** The new flow is fail-closed in the JavaScript gate and is an
improvement over the previous silent checkbox. However, its claimed fresh
Face-ID/biometric acknowledgement inherits the open `verifyBiometric2fa()` behavior:
after `biometryLockout`, or when no biometric is enrolled on a secured device, it
retries with `allowDeviceCredential: true` (`src/lib/biometric.js:163-177`). The
post-check only identifies the device's configured iOS hardware, not the factor used
in that prompt. Thus an OS device credential can authorize an over-limit send,
including a WalletConnect request, despite the UI and commit describing a biometric
gate. The native prompt's reason is generic and does not bind the dapp or amount.

This does not remove the old limit control—without a successful OS prompt the send
still rejects—but it materially broadens the previously recorded N-1 residual from
unlock to money movement. Decide whether device credential is an accepted step-up
factor. If not, remove the fallback for Theft Protection specifically and add a
real-device negative test for biometric lockout/no-enrolment before describing this
path as biometric-gated. INTERNAL static analysis only; no device test was run.

### `src/wallet-core/panic.js:255` and `src/components/WalletEntry.jsx:877-882, 974-977, 1129-1135` — SAFE
**Commits:** `c8c72fc8` (#2518), `61fa71e8` (#2519)

The Theft Protection error class is now excluded before the wrong-PIN wipe counter,
reported honestly on cached-biometric and typed-credential paths, and its opt-in
marker is in `DENIABILITY_RESIDUE_KEYS` for both wipe and clean-state inspection.
The focused tests cover the intended regression cases. This is BUILT/source-test
coverage only, not device verification.

### `functions/api/rpc/[fn].js:14-22`, `sql/get-referral-tier.sql:37-81`, `src/api/referralApi.js:121-138`, `src/pages/Subscription.jsx:227-275` — SAFE, subject to N-1
**Commit:** `230637fd` (#2528)

The new anonymous RPC returns a tier bucket rather than a paid-count value. It has a
pinned empty `search_path`, explicit grants, a closed Pages allowlist, existing
per-IP proxy rate limiting, an I5 client allowlist, and a deniability/demo no-call
guard. A public referral code now reveals one of five paid-volume buckets; the
commit records this as an owner-accepted posture, not a hidden privacy property.
No code or test result here verifies the database deployment.

### `src/lib/sendGate.js:129-155`, `src/pages/BuyCrypto.jsx:158-177`, `src/pages/BuyInProgress.jsx:54-75` — SAFE
**Commits:** `4ba80bbf`, `bf77471f`, `52ad759e`

The send gate still re-evaluates limits at signing time and consumes the Theft
Protection token before that evaluation. The buy pages retain deniability/demo
null-rendering and render a neutral unavailable state only when the ship/region gate
is off; no return URL transaction identifier is rendered. The Advisor snapshot hook
remains its I3 chokepoint.

### `CLAUDE.md`, `sql/env-parity-2026-09-09.sql`, `sql/live-project-hardening-2026-08-07.sql`, `ios/App/AppUITests/AppUITests.swift` — SAFE

These changes add project-scope safeguards and mobile-test/release coverage. They do
not change runtime credentials, database grants, or signing behavior.

## Test coverage deltas

- No changed test added or removed a `skip`, `fixme`, or `todo` marker.
- The new Theft Protection, WalletConnect, referral-tier, panic-residue, buy-gate,
  and RPC tests are useful regression pins. They are not real-device biometric,
  database-deployment, or on-chain verification.
- The Security Advisor sentinel became a nonnumeric string, avoiding an accidental
  match in a consented device identifier; the no-egress assertion remains intact.

## Non-security changes (summary)

`.claude/scheduled-tasks/veyrnox-stream-json-upstream-watch/SKILL.md` and the
send-amount typography/default-mode adjustment were reviewed as non-security.

## Open items carried forward

- **2026-09-11 N-1:** Theft Protection still permits device-credential fallback,
  its biometric-only fast path bypasses Theft Protection, and `WalletProvider`
  assigns the decrypted container before the gate can refuse. N-2 above expands
  the device-credential portion to over-limit signing.
- **2026-09-11 N-3:** the CSP-blocked inline boot watchdog remains unresolved.
- **2026-09-11 N-4:** the removed `process.version` shim on the Digital Shield
  path remains unresolved.

## Scan-list maintenance

No new unmatched-file finding. The leading behavioural triage question caught the
SQL overload defect because the changed file decides an entitlement and offer path.

## Post-scan re-check

Verified against `origin/main` at `230637fd` (`2026-09-12T09:03:07Z`). Nothing
landed between the scan-window tip and this checkpoint; no amendment applies.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, device
verification, database verification, or on-chain confirmation. Not the outstanding
independent third-party audit.*

---

# Addendum — second run, 2026-09-12 (post-merge)

The scan above was published as PR #2530, which **merged at
`2026-09-12T09:20:29Z`**. This addendum is a second invocation of the same day's
task. It does two things the first run did not: it performs the runbook's
**Step 6 post-merge checkpoint** for that report, and it scans the one
security-relevant commit that landed afterwards.

## Step 6 — post-merge re-check of the 2026-09-12 report

The report above carries five open items (N-1, N-2, and three carried forward
from 2026-09-11), so Step 6 is not optional for it. Its recorded verification
stopped at checkpoint 2 — `origin/main` at `230637fd`, `2026-09-12T09:03:07Z`,
i.e. **before** publication. Neither of the first two checkpoints can observe
the commit-to-merge gap; only this one can.

Re-checked the full window — scan-window tip through the merge:

```
git log 230637fd..origin/main --oneline
8ef00de9 docs(security-diff): daily scan 2026-09-12 (#2530)
```

One commit, touching one file (`docs/security-diffs/diff-2026-09-12.md`) — the
report itself. **Nothing landed in the gap and no open item was resolved.** All
five items stand as written. No correction to the published text is required.

## New scan window

Commits scanned: 1 (`8ef00de9`..`69fbd29c`; `origin/main` tip re-confirmed at
`69fbd29c`, `2026-09-12T12:23:13Z`)
Security-sensitive files changed: 5 of 5

`69fbd29c` `fix(referrals): serve veyrnox.com/r/<code> share links via a Worker
on the zone (#2531)` landed after the report above merged and is therefore
outside its diff range.

### `workers/referral-redirect/src/index.js` — SAFE
**Commit:** `69fbd29c` (#2531)

**Change summary:** A new Cloudflare Worker bound to `veyrnox.com/r/*` and
`www.veyrnox.com/r/*` imports the existing Pages function
`functions/r/[code].js` rather than copying it, and rewrites that function's
relative `Location` to an absolute one on `https://veyrnox-prod.pages.dev`. It
exists because the app hands out `veyrnox.com/r/CODE` share links while the
redirect only ever lived on the Pages host, so every share link 404'd on the
marketing site.

**Assessment:** No open redirect and no header injection. The wrapper's only
mutation is `new URL(res.headers.get('Location') || '/', APP_ORIGIN)`, and the
inner `Location` is never attacker-derived: `functions/r/[code].js` emits either
the literal `/` or `` `/?ref=${encodeURIComponent(code)}` `` with `code` having
passed `CODE_RE` (`/^VYX-[ABCDEFGHJKLMNPQRSTUVWXYZ23456789]{6}$/`). The
destination origin is a module constant, not request-derived, so a hostile path
cannot steer it.

Verified behaviourally rather than by reading — the handler was run directly
over the edge cases:

| path | result |
|---|---|
| `/r/VYX-STRKLB` | `302 https://veyrnox-prod.pages.dev/?ref=VYX-STRKLB` |
| `/r/VYX-STRKLB/extra` | `302 https://veyrnox-prod.pages.dev/` |
| `/r/` | `302 https://veyrnox-prod.pages.dev/` |
| `/r/VYX-STRIKE` (I not in alphabet) | `302 https://veyrnox-prod.pages.dev/` |
| `/r/%2F%2Fevil.com` | `302 https://veyrnox-prod.pages.dev/` |
| `/r/VYX-STRKLB%26x` | `302 https://veyrnox-prod.pages.dev/` |

The protocol-relative probe is the one that matters, and it fails closed to the
app root. The function's `Cache-Control: no-store` and `Referrer-Policy:
no-referrer` survive the header copy — both are load-bearing here rather than
mere hygiene: `no-store` stops a shared cache serving one visitor's referral
code to another, and `no-referrer` stops the code leaking to the marketing
origin. The new test pins both.

No secrets, no bindings, no vars; `workers_dev = false`, so there is no public
`*.workers.dev` hostname alongside the zone route. Routes are scoped to `/r/*`
only and do not overlap the root `wrangler.toml` (`veyrnox-prod`, which declares
no routes). — **SUPERSEDED at `133ded16`; see the checkpoint-2 amendment below.
The route list is no longer `/r/*` only.** Not part of the locked TIP chain. No CI workflow deploys it —
`git grep` over `.github/`, `scripts/` and `package.json` finds no reference, so
deployment is the manual `npx wrangler deploy` the file documents. Nothing here
touches key material, signing, RASP, or the client.

### `workers/referral-redirect/src/index.js:24-27` — NEEDS-REVIEW (low)
**Commit:** `69fbd29c` (#2531)

**Change summary:** The Worker reconstructs the Pages runtime's `params.code`
shape from the path — a string for one segment, an array for anything deeper —
so the imported function's nested-path branch behaves identically on both hosts.
The file states this as a requirement: *"Reproduce exactly that shape from the
path so its nested-path branch keeps behaving the same on both hosts."*

**Assessment:** The reconstruction is `pathname.split('/').filter(Boolean)`, and
`filter(Boolean)` collapses empty segments, so the shapes are **not** identical:

| path | Worker | Pages `params.code` shape |
|---|---|---|
| `/r//VYX-STRKLB` | `302 …/?ref=VYX-STRKLB` | array → fails closed to `/` |
| `/r/VYX-STRKLB/` | `302 …/?ref=VYX-STRKLB` | array → fails closed to `/` |

Both confirmed by running the Worker, not by reading it.

This is **not a security defect.** The divergence is permissive rather than
restrictive, the code still has to clear `CODE_RE`, and the destination is still
the fixed app origin — so the worst case is that a doubled or trailing slash
credits a valid referrer where the Pages host would have dropped the ref. What
it is, is a **claim in code that is not true**, in the one file whose entire
design rationale is "one source of truth, identical behaviour on both hosts".
Either drop the `filter(Boolean)` (splitting on `/` and slicing yields the real
segment list, empties included) or amend the comment to say the shapes match for
the cases that matter and diverge on empty segments. The Worker's test file pins
`/r/` and `/r/<code>/extra` but neither divergent case, so nothing currently
holds this claim honest.

## Test coverage deltas

- `workers/referral-redirect/__tests__/redirect.test.js` is new: 8 cases, **0**
  `skip`/`fixme`/`todo`. It pins the absolute-Location property, case
  normalisation, four fail-closed inputs, HEAD handling, and the two hygiene
  headers.
- `vitest.config.js` adds `workers/**/*.test.{js,jsx}` to `include`. This is a
  control ADDED — without it the new Worker tests would be collected by nothing
  and the file would ship unexercised, the same blind spot the surrounding
  comment records for `/api/data/klines`. The `env` blanking block
  (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_FORCE_TIER`,
  `VITE_BYPASS_RASP`, `VITE_TIP_BASE_URL`, `VITE_EDGE_BASE`) is untouched, so
  bringing `workers/**` under this runner does not reopen the production-write
  path PR #1328 closed.
- These are source-level regression pins plus a local behavioural exercise of
  the handler. They are **not** live verification. The Worker's own
  `wrangler.toml` says to accept on live behaviour, and no request against
  `veyrnox.com/r/…` was made here. Until one is, the deployed redirect is
  unverified — which is exactly the failure mode `functions/r/[code].js`
  documents shipping to production twice.

## Non-security changes (summary)

None. All five changed files in this window were assessed as security-relevant.

## Scan-list maintenance

**Add `workers/**` to the scan list**, alongside `functions/**` in the
"Cloudflare Pages Functions — the server BETWEEN the client and the database"
block, and widen that block's heading: it is now the *Cloudflare server layer*
generally, not Pages Functions specifically.

- **Files:** `workers/referral-redirect/src/index.js`, `wrangler.toml`,
  `__tests__/redirect.test.js` — a new top-level directory.
- **Why the current shape missed it:** the list names `functions/**` because
  that is where Cloudflare code lived when the block was added on 2026-09-03.
  `workers/**` did not exist until today. This is an internet-facing endpoint on
  the **primary brand zone** (`veyrnox.com`, not a `pages.dev` host), sitting in
  front of the marketing origin and bound by zone route rather than by path
  routing — strictly more exposed than the Pages functions the block covers.
- **Honest note on the escalation clause:** this did not produce a finding the
  leading behavioural triage question failed to reach — the one NEEDS-REVIEW
  above is a claim-accuracy item, not a control defect. So it is **not** a
  seventh consecutive unmatched-file finding and does not trigger the "a static
  document cannot do this job" conclusion. But the path index did not see the
  file, and a future Worker holding a binding or a secret would be a different
  story. Suggested entry:

  ```
  - `workers/**` — standalone Cloudflare Workers bound to zone routes. Check:
    a route on the veyrnox.com zone rather than a pages.dev host; `workers_dev`
    left true (a second, unrouted public hostname); any `[vars]`,
    `[[kv_namespaces]]`, `[[d1_databases]]` or secret binding; a
    Location/redirect built from request-derived input; whether CI deploys it or
    a human must.
  ```

## Post-scan re-check

Verified against `origin/main` at `69fbd29c` (`2026-09-12T12:23:13Z`). This
addendum's own window opens at `8ef00de9`, the merge of the report above.
Nothing has landed since. — **FALSE by the time this was published; corrected
at checkpoint 2 below. `133ded16` landed at `2026-09-12T12:25:46Z`, roughly
three minutes after the line was written.**

---
*Automated daily scan, second run. INTERNAL static analysis plus a local
behavioural exercise of one Worker handler — no dynamic testing against the
deployed edge, no device verification, no database verification, no on-chain
confirmation. Not the outstanding independent third-party audit.*

## Amendment (2026-09-12, checkpoint 2 — before auto-merge was armed)

`133ded16` `feat(referrals): capture /r/<code> share links natively (universal /
app links) (#2532)` landed at `2026-09-12T12:25:46Z`, **after** the addendum
above was written and committed but **before** its PR was armed for auto-merge.
Caught by the runbook's second checkpoint. Two statements above were already
false at publication and are marked in place rather than rewritten.

It does **not** resolve the low NEEDS-REVIEW. `filter(Boolean)` is untouched in
`workers/referral-redirect/src/index.js`; the divergence stands.

### What changed, and what it does to the ratings above

**The Worker's route list is no longer `/r/*` only — SAFE, and a control ADDED.**
It now also owns `veyrnox.com/.well-known/apple-app-site-association` and serves
**this repo's** copy, bundled as text (`[[rules]] type = "Text"`; a vitest
`aasa-text` plugin does the same for tests). Previously the live apex AASA was
the marketing site's copy, whose source is not in this repository — so the set of
paths the iOS app is allowed to claim was defined by a file nobody here could
review or version. Moving it under the handler that serves it is a supply-chain
improvement, not merely plumbing. The response is a static bundled string with no
request-derived content, `200 application/json`, `Cache-Control: public,
max-age=3600`. The `/r/*` redirect behaviour is unchanged; every result in the
edge-case table above still holds.

**Native capture of `/r/<code>` — SAFE.** `DeepLinkHandler.jsx` gains a branch
before the `/buy/return` one: exact `u.hostname === 'veyrnox.com'` plus
`u.pathname.startsWith('/r/')` → `captureReferralFromUrl(u, 'universal_link')`,
then an early `return`. No navigation, and the early return means a `/r/` URL
cannot fall through to the WalletConnect pairing path. The I3 gate lives inside
`captureReferralFromUrl` (`isDeniabilityOrDemoActive()` first), so a decoy or
demo session writes nothing and fires no event. `AndroidManifest.xml` adds a
third `autoVerify` intent-filter (`pathPrefix="/r/"`, same host, same
`assetlinks.json`) and the AASA gains a `/r/*` component.

Claiming `/r/*` means any `https://veyrnox.com/r/VYX-XXXXXX` link a user taps
now opens the app and sets a pending referral code. The worst case is attribution
grief — crediting a referrer the user did not choose — which is the same property
`?ref=` has always had, not a new class of exposure. No key material, signing
path, or gate is touched.

**Telemetry — SAFE.** `captureReferralFromUrl` gains a `source` parameter, so
`REFERRAL_CODE_APPLIED` now carries `'universal_link'` as well as `'deep_link'`.
No new event type and no new egress path: this is a low-cardinality string on an
existing event, still behind the `api/trackEvent.js` consent chokepoint and still
suppressed under I3. The `code` field was already being sent.

### The divergence is now two-sided, which sharpens the finding above

`referralAttribution.js` introduces `PATH_RE = /^\/r\/([^/]+)\/?$/` to read the
same share link on the native side. That regex **rejects** `/r//VYX-STRKLB`
(`[^/]+` cannot span the extra separator) and **accepts** a single trailing
slash. So the three readers of one URL shape no longer agree:

| path | Pages function | Worker (`filter(Boolean)`) | native `PATH_RE` |
|---|---|---|---|
| `/r//VYX-STRKLB` | fails closed | accepts | fails closed |
| `/r/VYX-STRKLB/` | fails closed | accepts | accepts |

Still not exploitable — every path ends at `CODE_RE` and a fixed destination —
but the file's claim to reproduce the Pages shape *exactly* is now contradicted
by a second implementation in the same commit series, and `PATH_RE` is the
shape the Worker was trying to match. Whichever way it is fixed, fix it once:
the comment promises one source of truth and there are now three readers.

### Not scanned here

`133ded16` also touches `src/lib/__tests__/referralAttribution.test.js`,
`src/components/__tests__/DeepLinkHandler.referral.test.jsx`,
`src/lib/__tests__/deepLinkAssociations.referral.test.js`,
`workers/referral-redirect/__tests__/redirect.test.js` and
`public/.well-known/README.md`. The commit message states the new tests were
mutation-checked (dropping the handler branch turns 4 red, dropping the AASA
component turns 1 red) and that the Worker was deployed and verified live at
version `ec47f120` — **both are the author's claims, read from the commit
message and not reproduced here.** The full window `69fbd29c..133ded16` belongs
to the next run, which branches fresh from `origin/main` and will cover it.

## Post-scan re-check (checkpoint 2)

Verified against `origin/main` at `133ded16` (`2026-09-12T12:25:46Z`). One
commit landed between the addendum's window close and this checkpoint; it is
folded in above. Amended on the report's own branch before auto-merge was
armed, so nothing was racing the push.
