# Security Diff — 2026-08-28

Commits scanned: 12 in last 24h
Security-sensitive files changed: 21

## ⚠️ REGRESSION DETECTED

- `android/app/src/main/java/com/veyrnox/app/EnclaveKeySpecConfig.kt` — #2131 changes the live Android Enclave wrapping-key alias from `.v1` to `.v2`, but provides neither legacy-key lookup nor a rewrap migration. Existing M2d/M2c outer vault records remain encrypted under the device's `.v1` key; the current unlock path requests only `.v2` before the PIN/KEK path can run. Those users receive `M2D_KEY_NOT_FOUND` and cannot reach the claimed re-enrollment flow. This is an I4 availability regression. Internal static analysis only.

## Flagged changes

### android/app/src/main/java/com/veyrnox/app/EnclaveKeySpecConfig.kt:51 — REGRESSION
**Commit:** `061927da` feat(kek): time-based Keystore validity — Face-unlocked Pixel opens KEK silently (#2131)

**Change summary:** Replaces `com.veyrnox.app.enclaveWrappingKey.v1` with `.v2` while changing the Android Enclave key's authentication validity from per-use to 30 seconds.

**Assessment:** The alias is also the only key lookup used by `EnclaveKeyService.unwrap()` (`EnclaveKeyService.kt:505`). A M2d/M2c outer record is unwrapped before the normal PIN path (`src/wallet-core/keystore/native.js:1428-1464`), and the affected source contains no `.v1` lookup, legacy-alias migration, or old-key rewrap. Therefore an installed vault encrypted with `.v1` cannot be opened merely by asking the user to re-enroll under a newly minted `.v2` key. Preserve the legacy alias long enough to authenticate and rewrap, or provide an explicit, tested recovery/migration path; add an installed-base `.v1` outer-vault regression test.

### android/app/src/main/java/com/veyrnox/app/EnclaveKeyService.kt:679 — NEEDS-REVIEW
**Commit:** `061927da` feat(kek): time-based Keystore validity — Face-unlocked Pixel opens KEK silently (#2131)

**Change summary:** Changes the Enclave AES-GCM key from per-use `AUTH_BIOMETRIC_STRONG` authentication to a device-wide 30-second validity window.

**Assessment:** This deliberately permits a recent strong device authentication to satisfy the key operation without a fresh Veyrnox prompt. The change is documented as owner-approved, retains `AUTH_BIOMETRIC_STRONG` and enrollment invalidation, and makes no unsupported verification claim in code. It is nevertheless a material hardware-key policy change with no device evidence in this scan; the unit test only pins constants. Treat it as BUILT/internal static analysis, not device-verified or independently audited.

### src/wallet-core/keystore/native.js:621 — NEEDS-REVIEW
**Commit:** `b5d8a7fd` feat(fastpath): silent unlock — skip aparajita prompt, rely on Keystore 30s STRONG window (#2135)

**Change summary:** Removes the H-derived wrapper from the Android fast-path cache and stores the raw 32-byte DEK (base64 encoded for the bridge) under a separate 30-second biometric-gated Keystore alias.

**Assessment:** Static review confirms the deniability/demo, duress, opt-in, disclosure, passkey, RASP, malformed-payload, and biometric-enrollment-invalidation gates remain in the JS/native paths; the new tests pin those gates. The new design's sole release boundary is the Android Keystore's device-wide recent-auth window, however, so its behavior and the stated fallback must be verified on representative hardware before any stronger status is claimed. This scan found no static I1–I5 bypass beyond the separate alias-migration regression above.

### src/components/WalletEntry.jsx:829 — SAFE
**Commits:** `60a1ea9f` / `bfe4d4a4` / `dbbe0a64` / `b5d8a7fd` unlock and biometric-consent changes (#2120, #2129, #2133, #2135)

**Change summary:** Adds native biometric auto-prompting, a first-run biometric-consent step, and fast-path forwarding before the existing cached-PIN slow path.

**Assessment:** The fast-path call remains best-effort: every miss falls through to the normal PIN/Argon2id/KEK path. Its release function independently blocks deniability/demo, a configured duress PIN, missing disclosure/opt-in, passkeys, and non-ALLOW RASP. The new preference writers are guarded against deniable/demo state. Static review only; the new consent and native prompt behavior are not device verification.

### src/pages/SendCrypto.jsx:2057 — SAFE
**Commit:** `708725d0` feat(send): compact Send verify screen and celebrate the broadcast (#2132)

**Change summary:** Compacts the simulation, transaction-intelligence, and decoded-calldata presentation; routine transfer/approval details move into expandable sections.

**Assessment:** The send/risk decision functions are unchanged. Unknown calldata and unlimited-approval warnings remain immediately visible, and the unlimited-approval extra confirmation remains outside the disclosure fold. Static review found no removed send gate, acknowledgement check, or simulation fail-closed branch.

## Non-security changes (summary)

Daily-report merge, onboarding/UI copy and layout changes, iOS/Firebase test maintenance, type declarations, and documentation updates.

## Post-scan re-check

Verified against `origin/main` at `b5d8a7fdf0259b7a3e4cdf95a6e8c89e4fda8232` (2026-08-28T09:06:28Z). Nothing landed between window close and this check.

---
*Automated daily scan. Internal static analysis only — no dynamic testing, device verification, on-chain confirmation, or independent audit.*
