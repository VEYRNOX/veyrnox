# Security Diff — 2026-09-09

Commits scanned: 17 in last 24h (`43557d53`~1 → `2d97a064`)
Security-sensitive files changed: 6 of 10

Window head: `2d97a0640cfe6c5e0ac5b55a7e24478fffbd39f7`
Window tail: `43557d53` (oldest commit inside 24h; diffed from `43557d53~1`)

No wallet-core, RASP, sign-gate, native, SQL, or Cloudflare Functions source
changed in this window. The window is documentation, two scheduled-task
runbooks, a devDependency addition, and one test-harness repair. Every finding
below therefore comes from a document — which is the point of the triage
question this runbook now leads with, and is discussed under
*Scan-list maintenance*.

---

## Carried finding — RESOLVED in this window

`diff-2026-09-08.md`'s incremental NEEDS-REVIEW on
`docs/RELEASE-v1.0.1-PLAY-SUBMISSION.md:24,101` (the document told the owner
that `main` was at versionCode `45` while `3ccdc328` had already moved
`android/app/build.gradle` to `48`) is **RESOLVED** by `1ae3a43d` (#2453). Line
25 now reads `versionCode submitted | 48`, and every downstream reference in
that file reads `48`.

One residue: the same line still cites `android/app/build.gradle:25` as the
source, and `versionCode` now sits at **line 32**. See O-1 — the identical
pointer drift exists in `CLAUDE.md`, and there the number is stale too.

---

## Flagged changes

### `supabase/functions/rc-webhook/index.ts:44` + `docs/play-launch/rc-webhook-deploy.md:40` — NEEDS-REVIEW — **RESOLVED, see Step 6 correction below**
**Commits:** not changed in this window. Surfaced by `9cce8489` (#2437),
`8f5b6245` (#2439) and `1ae3a43d` (#2453), which changed the *other* half.

**Change summary:** `CLAUDE.md` and both `RELEASE-v1.0.1-*-SUBMISSION.md` files
now record that `rc-webhook` was redeployed on 2026-09-08 with
**`verify_jwt: false`** (prod v14, staging v5), and state that this is required:
RC's `Authorization` header carries a shared secret, not a Supabase JWT, so the
platform gate rejects legitimate deliveries. The function's own source file was
not touched and still says the opposite.

**Assessment.** Three files on `origin/main` now say the platform JWT gate must
be OFF. Two files — including the source file that is the deployment
source of truth — still say it must be ON:

- `supabase/functions/rc-webhook/index.ts:44`, inside the `DEPLOY` block:
  *"NOTE the missing --no-verify-jwt. Same rationale as first-referral-bonus:
  keep the platform's bearer check in front of this handler"*, above the literal
  command `supabase functions deploy rc-webhook`.
- `docs/play-launch/rc-webhook-deploy.md:40`: *"Note the deliberately absent
  `--no-verify-jwt`"*.

Anyone redeploying `rc-webhook` by following either instruction restores
`verify_jwt: true` and silently re-breaks the referral attribution chain that
was armed the day before. The failure mode is invisible in the way this repo has
already been bitten by once: RC deliveries take a platform 401 and the symptom
presents as *"the webhook isn't firing"*, not as an error anyone reads. This is
the same shape as the `tip-chat` v34 incident that opens `CLAUDE.md`'s hard-rules
section — deployed configuration diverging from repo source, with the repo source
being the thing a future session will act on.

**The `verify_jwt: false` decision itself is sound and is not the finding.** Read
from the ref rather than inferred from the changelog: `handle()` fails closed on
an unset `REVENUECAT_WEBHOOK_AUTHORIZATION` (500 `server_config_missing`, before
any body read), compares the header with a constant-time compare over SHA-256 of
each side so length is not a side channel, returns 401 on mismatch, caps the body
by declared `Content-Length` *and* by actual encoded byte length, filters to
`INITIAL_PURCHASE` / `NON_RENEWING_PURCHASE`, validates the code against
`CODE_RE`, and logs presence only, never payload. The compensating control is
real. Only the instructions disagree with the deployment.

**Fix is one edit in each of the two stale files**, naming the 2026-09-08 change
and why. Do not "resolve" it by flipping the deployment back to match the
comments.

---

### `docs/RELEASE-v1.0.1-PLAY-SUBMISSION.md:53-56` — NEEDS-REVIEW — **RESOLVED as a disclosure error; the Robo evidence gap remains open, see Step 6 correction below**
**Commit:** `1ae3a43d` (#2453) *docs(release): 1.0.1 MDs — both stores submitted
for review*

**Change summary:** The document records that Play 1.0.1 was **submitted for
review on 2026-09-08** from versionCode 48, and presents the mandatory
pre-submission gate as a four-row table.

**Assessment.** Row 3 — the Firebase Test Lab Robo run that `CLAUDE.md` and
issue #1960 designate as the accepted substitute for the never-generated Play
Pre-launch report — is marked **✓** on evidence from a different build:

> `| 3 | Firebase Test Lab Robo output on 48 (substitute for #2) | ✓ Sep 4 matrix
> matrix-1delt54g28ira (versionCode ~44) — 267 UI actions on Pixel 8, no app
> crash. Fresh dispatch on 2026-09-08 failed on CI-artifact dep (workflow needs
> a CI-built AAB for main SHA), not an app failure. |`

The row is honest about its own provenance — it says `~44`, and it says the
fresh dispatch against 48 failed — and then marks the gate ✓ anyway. Rows 2 and
4 of the same table correctly carry **⚠** for gates that are not met. The
inconsistency inside one table is the tell.

Measured, not assumed: the four bumps between 44 and 48 (`74755b7a`,
`418aae8b`, `d0c4423c`, `3ccdc328`) are each two files, `build.gradle` +
`staging-mobile-release.test.js`, so the Android build config is unchanged. The
AAB also packages the web bundle, and **48 non-test `src/` commits landed between
the Sep-4 matrix and the 48 upload**, among them:

- `0dc4f673` — *fix(send): shim `process.version` so Send chunk doesn't blank on
  cold parse* — a cold-parse blank-screen fix, which is exactly the class of
  failure a Robo crawl exists to catch;
- `3424de6f` — RASP hard-signal latch + WalletConnect tier gate;
- `aba21e0f`, `5e852aad`, `d6300dc4` — audit remediations on step-up auth and
  the I4 refusal paths.

So the crawl that stands in for the gate exercised app code that predates a fix
for a blanking bug. That matters more here than it would elsewhere: **the Play
build-5 rejection this HOLD exists to prevent was a stock-device Create-Wallet
screen that appeared frozen.**

This does not assert that 48 is broken. Row 1 (AAB on Internal + Closed testing)
and the owner's stock-Android golden-path walkthrough (line 45) are separate
evidence, and the submission may well be fine. The finding is that the row
labels weaker evidence as a met gate. **Correct the row to ⚠ with the `~44`
caveat inline**, matching rows 2 and 4, so a later reader does not read this
table as four green gates. This is the 2026-09-03 S-6 pattern — a report
labelling a weaker class of evidence "PASS".

---

### `CLAUDE.md:556` — NEEDS-REVIEW — **RESOLVED, with one new stale cross-reference noted in Step 6 below**
**Commit:** not changed in this window; falsified by `1ae3a43d` (#2453).

**Change summary:** Both stores were submitted for review on 2026-09-08. The
governing control text was not updated.

**Assessment.** `CLAUDE.md:556` still reads, unchanged:

> **1.0.1 SUBMISSION HOLD — BOTH stores (owner-locked 2026-08-12).** No
> submission to Play OR App Store review until every check in this section
> passes. […] Do NOT submit for review without a clean report — this is the same
> tool Google's reviewer would have used […]

Both submissions have now happened, and by the release documents' own account one
mandatory check (Play Pre-launch report) is permanently unmet and was accepted as
a residual when #1960 was closed on 2026-09-04 by owner decision.

Nothing here says the submission was improper — the residual was owner-accepted
and `docs/RELEASE-v1.0.1-PLAY-SUBMISSION.md:54` says so plainly. The finding is
that **the repo's own hard-rule section now supports two readings, and both are
wrong**: that submission is still blocked (it is not), or that a standing
owner-locked hold was walked past (it was not — it was released). A control
document that cannot distinguish "released" from "violated" is not recording the
decision it exists to record.

**Fix:** amend the HOLD block with the release — date, who decided, which checks
were waived and on what evidence — the way the `#2276` and `#1960` closures were
recorded elsewhere in the same file. Append, do not delete: the hold's original
text is the record of why the gate existed.

---

### `package.json` / `package-lock.json` — SAFE (with a lockfile-hygiene note)
**Commits:** `655d2935` (#2443), `630b52d1` (#2449)

**Change summary:** Adds `@testing-library/user-event ^14.6.7` as a
devDependency; re-orders the existing exact `@ngraveio/bc-ur 1.1.13` pin into
alphabetical position (no version change); rewrites the `elliptic` entry of
`//overrides-audit-notes` to record the two-requirer correction from #2445.

**Assessment.** Verified structurally against both lockfiles rather than by
reading the diff:

- **0 version changes**, **0 removals**, 33 additions.
- Every crypto and wallet-path pin is byte-identical across the window:
  `elliptic 6.6.1`, `@keystonehq/bc-ur-registry-eth 0.22.1`,
  `@ngraveio/bc-ur 1.1.13`, `appium-uiautomator2-driver 8.6.1` and its
  shrinkwrap-pinned nested `shell-quote 1.10.0`, `qs 6.16.0`,
  `@xmldom/xmldom 0.9.12`, `brace-expansion 5.0.9`.
- 1 of the 33 additions is `@testing-library/user-event` itself. The other **32
  are nested `appium-uiautomator2-driver/node_modules/*` entries** (256 → 288),
  all at versions greater than or equal to their top-level counterparts. No
  vulnerable copy was introduced.

The devDependency is test-only tooling, does not enter the production bundle, and
does not touch the signing path. The audit-note rewrite is a correction that
makes the elliptic remediation path *more* accurate (see the next entry).

**Hygiene note, not a finding.** The 32 nested re-additions are the signature of a
full `npm install` rather than `npm install --package-lock-only` — precisely the
trap `CLAUDE.md`'s environment section documents ("a full `npm install` re-adds
~37 nested `appium-uiautomator2-driver/node_modules/*` entries"). Harmless here
because nothing moved version, but it means the lockfile diff of a two-line
`package.json` change was 441 lines, and a real regression inside that noise
would be easy to miss on review. Regenerate with `--package-lock-only` next time.

---

### `.claude/scheduled-tasks/veyrnox-elliptic-upstream-watch/SKILL.md`, `.claude/scheduled-tasks/veyrnox-daily-dep-audit/SKILL.md` — SAFE (control strengthened)
**Commits:** `4feae365` (#2447), `4bb70be8` (#2451), `630b52d1` (#2449),
`3b04c933` (#2452)

**Change summary:** Records that `@keystonehq/bc-ur-registry-eth@0.22.1` has
**two** requirers — an exact direct root pin in `package.json` and
`keystone-sdk`'s `^0.22.0` — and that npm resolves the direct pin, so a
remediation bumping only `keystone-sdk` leaves `elliptic` resolved. Also
discharges the standing "the watcher ran but nobody read what it said" caveat
with the actual 2026-09-01 and 2026-09-08 verdicts.

**Assessment.** The two-requirer claim is true — confirmed against
`origin/main:package.json`, which carries `"@keystonehq/bc-ur-registry-eth":
"0.22.1"` as a direct dependency. This corrects a remediation instruction that
would have failed silently: the bump would have been made, the audit would still
have shown `elliptic`, and the cause would have been invisible.

The dep-audit entry converts *"available but not yet collected"* into a
transcribed verdict with the probe results and the session id, and adds a second
independent confirmation seven days later. That is the honest direction — an
unverified caveat discharged with evidence rather than dropped. It also names
where the evidence lives (session transcripts, because the watcher writes no
report file), which is the asymmetry that let "it ran" pass for "it reported
nothing" for five days.

Nothing was weakened. `#2452` deduplicates the same correction rather than
restating it in two files, consistent with the mirror-drift lesson from #2295.

---

### `.claude/scheduled-tasks/veyrnox-daily-security-diff/SKILL.md` — SAFE (control strengthened)
**Commit:** `51705885` (#2441)

**Change summary:** Promotes the behavioural triage question to the top of Step
2, relabels the path list as a fast index beneath it, and adds the
commerce-integrity / tier-gate block (`purchases.js`, `purchases/**`, `tier.js`,
`tierCache.js`).

**Assessment.** Diffed the removals specifically, because a runbook that loses a
rule is a lost control: every removed line is either the behavioural question in
its old bottom-of-step position or the escalation clause that this change
discharged. No rule was dropped. The escalation clause was honoured as written —
the question was moved rather than a seventh path being added *instead of* the
reorganisation.

Effect measured on this run, in *Scan-list maintenance* below.

---

### `src/components/__tests__/WalletEntry.pin-cohort-biometric.test.jsx` — SAFE (coverage intact; three harness observations)
**Commits:** `655d2935` (#2443), `7fd5b61d` (merge of #2450)

**Change summary:** Repairs a flaky test by replacing `fireEvent` with awaited
`userEvent` clicks, waiting for the pad to report a full-length value before
pressing Submit, and having `waitForPinPad(ctx)` wait for the mount-time
biometric auto-prompt to settle before driving the typed-PIN fallback.

**Assessment — coverage delta: none.** 7 tests before, 7 after.
`skip`/`fixme`/`todo` count 0 before, 0 after. Every assertion survives
unchanged and each still names the control it pins: unlock-before-cache
ordering (`callOrder`), the `alreadyCached` decoy-cache guard, the H-3
duress-configured guard, the P1-A installed-base disarm
(`setBiometricUnlockEnabled(false)` + `clearUnlockSecret`), the decoy-bound
leave-armed case, and no-cache-on-wrong-PIN. This is a harness repair, not a
weakening — the class of change #1403 got wrong, done right.

Three observations, none of which change the rating:

1. **The new wait is satisfied by mock state, not by production behaviour.**
   `waitForPinPad` waits for `ctx.unlockWithBiometric` to have been called
   whenever `hasStoredUnlockSecret` resolves true. In the two mount-guard tests
   that condition holds only because `isBiometricUnlockEnabled` is a static
   `vi.fn(() => true)`: production reaches
   `biometricEnabled = vaultExists && isBiometricUnlockEnabled() && bioReady`
   (`WalletEntry.jsx:609`) *after* `enforceDuressBiometricInvariant()` has
   disarmed the preference, so the auto-prompt would not fire at all on the very
   device those tests describe. Making the biometric mock stateful — the honest
   improvement — would hang this helper and turn both tests red for a reason
   unrelated to the control. Worth knowing before anyone tightens that mock.
2. **A comment describes configuration that is not present.** The block above
   `const user = userEvent.setup();` reads *"userEvent w/ advanceTimers noop"*;
   `setup()` is called with no arguments and the suite uses no fake timers. Stale
   at birth, in a file whose header is otherwise unusually careful about stating
   why each pin exists.
3. **`userEvent.setup()` is module-scope**, shared across all 7 tests with
   `cleanup()` in `afterEach`. Testing Library's guidance is per-test setup;
   shared pointer state across cases is a flake source, which is an awkward shape
   for a flake fix. No observed failure — noted so it is looked at first if this
   file flakes again.

---

## Observations (not regressions)

### O-1 — `CLAUDE.md` versionCode line is 8 bumps stale, and its line pointer is wrong
`CLAUDE.md` states *"`build.gradle` is at 40 on `main`"* and cites
`android/app/build.gradle:25`. On `origin/main` the value is **48**, at **line
32**. The bumps since 40 — 45 (#2397), 46 (#2407), 47 (#2410), 48 (#2433) — are
not in the enumerated list either.

The bullet predicts its own decay in as many words (*"A number that moves almost
daily does not survive being written down; re-read `android/app/build.gradle:25`
rather than trusting this bullet"*), and the prediction has come true for the
second time. The prediction is now also wrong in its remedy, since the line
number moved. Two cheap options: cite the file without a line number, or drop the
number entirely and keep only the instruction to read it. The same stale
`build.gradle:25` pointer sits in `docs/RELEASE-v1.0.1-PLAY-SUBMISSION.md:25`.

Static analysis only — no release artifact is affected; the submitted AAB carries
48 and the release docs say 48.

### O-2 — one prod/staging SQL divergence was found; nothing establishes there is only one
`CLAUDE.md` now records that the `increment_referral` `ref_code → p_code` rename
had been **staging-only for six weeks**, that every prod client call was failing
at PostgREST, and that 3560 prod referral rows sat at `count=0`. It draws the
right lesson (*"'SQL migration DONE' needs to mean 'applied to prod AND
staging'"*) and then applies it to exactly one function.

`sql/api-security-hardening.sql` is a single file containing many more
SECURITY DEFINER functions, their rate limits, their `search_path` pins, and the
`REVOKE ... FROM anon` block at lines 574-577. If one statement from that lineage
never reached production, nothing in the repo establishes that the rest did. The
same question applies to `sql/definer-search-path-pin.sql` and
`sql/check-first-referral-bonus-hardening.sql`.

Related but **not** a finding, stated so it is not re-filed as one: the 2026-09-08
migration re-granted `anon`/`authenticated` EXECUTE on `increment_referral`, which
is wider than `sql/api-security-hardening.sql:574-576`. That is the *expected*
pre-REVOKE state — `CLAUDE.md`'s H-3 ordering note is unchanged and still requires
`SUPABASE_SERVICE_ROLE_KEY` to be set on the `veyrnox-prod` Pages project *before*
those REVOKEs run, or every referral and telemetry write breaks. Preserving the
grants across a `DROP`+`CREATE` was correct.

This run is static analysis and deliberately did not query either live project.
The check is a signature-and-ACL comparison of `public` functions across
`jwstkrtslotnjyerzzsi` (prod) and `nszlbcmcysftwyudthjz` (staging) against the
`sql/` files — owner action, cheap, and it would have caught the six-week gap.

### O-3 — a secret digest is committed; not a leak, recorded so it is not re-filed
`CLAUDE.md` now carries the SHA-256 of `REVENUECAT_WEBHOOK_AUTHORIZATION`. It is
a digest of a 64-character random secret, so it is not recoverable and not a
credential; it is a rotation-verification fingerprint. `diff-2026-09-08.md`
reached the same conclusion. No action. Noted only because "sha256 in CLAUDE.md"
will keep looking like a hit to the next reader.

---

## Non-security changes (summary)

None. All ten changed files fell to the triage question — six got deep analysis,
and the four documentation files (`CLAUDE.md`, both `RELEASE-v1.0.1-*.md`,
`docs/security-diffs/diff-2026-09-08.md`) are the record of security changes and
were read in full rather than filed as non-security. That categorisation error is
the 2026-09-02 `docs/Feature-Status.md` mistake, and three of this run's four
findings came out of exactly those files.

---

## Scan-list maintenance

**This is the seventh consecutive run whose findings came from files matching no
path pattern — and it is the first where that is not a failure.** All four
findings sit in `CLAUDE.md` and `docs/RELEASE-v1.0.1-*-SUBMISSION.md`, none of
which the path index covers. Every one was caught by the behavioural question
`#2441` moved to the top of Step 2, on the clause *"or record what a security
change does"*, before the index was consulted at all. F-1 in particular could
not have been reached any other way: the offending file
(`supabase/functions/rc-webhook/index.ts`) was **not in the window**, and was only
opened because a document in the window claimed a deployment state that a source
file might contradict.

The runbook says a seventh unmatched-file run means the reorganisation was not the
fix and the next step is a mechanical classifier. **Recommend not escalating to
that yet.** The clause the escalation was written against was *"a list organised
by PATH cannot keep up with a codebase organised by BEHAVIOUR"*, and the
behavioural question did the work this run — the honest reading is that the fix
worked and the path index simply remained silent, which is what a fast index does
when the risk is somewhere it has not been told about. Escalate if a run produces
a finding that *neither* the paths *nor* the question catch.

One modest index addition, for speed rather than coverage:

```
**Governing and release-state documents** — these record what a security change
does, decide whether a store submission gate is met, and are the text a later
session acts on. All four of the 2026-09-09 findings came from them.
- `CLAUDE.md`
- `docs/RELEASE-*.md`
- `docs/Feature-Status.md`, `docs/audit-*.md` (already named in prose in this
  step; give them path entries so they are not skipped)
- What to look for: a deployment flag, secret, grant, or gate whose new value is
  recorded HERE but not in the source file or runbook that a redeploy would
  follow; a gate row marked ✓/PASS on evidence from a different build, branch, or
  environment; a standing hold or lock that events have overtaken without being
  amended.
```

A second, narrower one, from F-1's mechanism:

```
- When a document in the window records a change to DEPLOYED configuration
  (`verify_jwt`, a secret, an env var, a grant, an allowlist), open the source
  file and the deploy runbook for that component even when neither is in the
  window. Deployment state and repo state diverge silently, and the repo copy is
  what the next session will act on — this is the `tip-chat` v34 failure mode.
```

Per the maintenance rule, this run may write only its own report; applying the
above is the handoff. The escalation has now worked four times (2026-09-02,
2026-09-03, 2026-09-08, and the reorganisation itself) — it works when someone
reads this section and edits `SKILL.md`.

---

## Post-scan re-check

**Checkpoint 1 (pre-commit).** Verified against `origin/main` at
`2d97a0640cfe6c5e0ac5b55a7e24478fffbd39f7` at **2026-09-09T06:44:03Z**. Nothing
landed between window close (`2d97a064`, the tip captured at 06:35:42Z) and this
check — `git log 2d97a064..origin/main` was empty.

Report carries three NEEDS-REVIEW items and three observations, so checkpoints 2
(pre-auto-merge) and 3 (post-merge) both apply and are recorded below as they
run.

---

*Automated daily scan. INTERNAL static analysis only — no dynamic testing, no
device verification, no live Supabase or RevenueCat query, no on-chain
confirmation. Not the outstanding independent third-party audit.*

---

## Step 6 — post-merge correction (2026-09-09)

The report merged as `694a5cbd` (PR #2456). This is the required post-merge
re-check from the scan-window head `2d97a064` through `origin/main`
`1f47aaad`, checked at **2026-09-09T09:03:41Z**. The range contains three
commits that directly address the three open review items:

- **F-1 — RESOLVED by `482ea617` (#2457).** Both deployment instructions now
  use `supabase functions deploy rc-webhook --no-verify-jwt` and describe why
  RevenueCat's shared `Authorization` value is not a Supabase JWT. The handler
  code and its fail-closed shared-secret check were not changed. This resolves
  the repo/deployed-configuration divergence; it does not constitute a new
  live deployment verification by this scan.
- **F-2 — RESOLVED as a documentation-honesty finding by `abd4d4db` (#2460).**
  The Play table's Robo row is now `⚠`, explicitly says no Robo run exists for
  versionCode 48, and the heading no longer claims the gates are all green.
  The underlying evidence gap remains: a versionCode-48 Robo run has not been
  supplied or observed, so this correction must not be read as verification.
- **F-3 — RESOLVED by `1c38f9fa` (#2458).** `CLAUDE.md` preserves the original
  1.0.1 submission hold as historical text, records the 2026-09-08 release
  decision and its waivers, and retains the checklist as the standard for a
  later submission.

### `CLAUDE.md:597-599` — NEEDS-REVIEW (new cross-reference drift)

The same `1c38f9fa` release-record amendment says the Play document's table
"still marks" Robo row 3 as `✓`. That statement was already false: `abd4d4db`,
which landed six minutes earlier, changed the row to `⚠`; its current text
correctly states that no versionCode-48 Robo run exists. The release record
therefore preserves an accurate weak-evidence caveat while carrying an obsolete
description of the table. Correct this one clause to say the table now marks
the row `⚠`; do not weaken or remove the underlying caveat.

`1f47aaad` also corrects the Security Advisor consent disclosure and adds
source-level regression pins, but it neither resolves nor changes the release
and webhook items above. It is outside the original scan window; this amendment
does not rate it as if it had been scanned there.

*INTERNAL static analysis only — no dynamic testing, device verification, live
Supabase or RevenueCat query, or on-chain confirmation.*
