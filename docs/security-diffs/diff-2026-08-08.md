# Security Diff — 2026-08-08

Commits scanned: 20 in last 24h (`841c6cfa`~1 → `b86ab3e4`)
Security-sensitive files changed: 19 of 31

No REGRESSION. ~~Six~~ **SEVEN** NEEDS-REVIEW — this line said six and was
wrong; count the `### … — NEEDS-REVIEW` headings below. The miscount was
repeated in every later summary of this report until 2026-08-08, when marking
the closed ones forced an actual count. The omitted one is
`src/wallet-core/evm/simulate.js` (unsanitised `revertReason`), which is also
the only finding never mentioned again after publication — an undercount and a
blind spot in the same place. All seven are tabulated under "Remediation
status" at the end.

They are concentrated in the two surfaces this
window opened up: the Security Advisor's new direct-to-Worker path, and the
newly-reachable EVM revert-prediction branch. The window is strongly
net-positive otherwise — it closes a live CORS reflection hole, adds the first
rate limit to the Transak proxy, and applies real REVOKEs to the production
Supabase project.

---

## Flagged changes

### `supabase/functions/tip-chat/index.ts:159` — NEEDS-REVIEW — **RESOLVED, see remediation status below.**
**Commit:** `0e06180d` fix(advisor): wire wallet SecurityAdvisor to TIP /api/v1/chat directly (#1614)
**Change summary:** New Supabase Edge Function proxying Advisor chat to TIP.
Its `!upstream.ok` branch relays the upstream response body (500 chars), status
and content-type to the caller, under a comment that labels itself temporary:
`// TEMP DEBUG: relay upstream body so we can see what the Worker rejected. Revert to generic 502 once diagnosed.`

**Assessment:** This is the exact pattern `3e7d2efb` (#1605) removed from
`functions/api/buy/session.js` **in this same window** — that commit replaced
`err(502, \`Transak session ${res.status}: ${text.slice(0, 300)}\`)` with a
generic message plus a server-side correlation id, citing the response-hygiene
rule. One commit removed upstream error echo; another added it back on a
different endpoint, five commits apart.

Two mitigating facts, stated so the severity is not overread: the function is
**not deployed** (header says `STATUS: BUILT, NEEDS DEPLOY`), and it is
**unreachable** — see the next finding. So nothing leaks today. What is
recorded here is that a self-declared debug leak landed on `main` with no
tracking issue and no deploy-time gate, in a file whose own header describes it
as pending deployment. The `TEMP` comment is the only thing standing between it
and production, and comments do not fail builds.

### `supabase/functions/tip-chat/index.ts:1-46` — NEEDS-REVIEW — **fix in flight, see remediation status below.**
**Commit:** `0e06180d` (#1614)
**Change summary:** The file's header states *"Companion PR to
SecurityAdvisor.jsx points the wallet at this function instead of tip-screen."*
The same commit points `SecurityAdvisor.jsx` at
`${VITE_TIP_BASE_URL}/api/v1/chat` — the TIP Worker directly, not at this
function.

**Assessment:** Dead code on arrival, carrying a header that asserts a wiring
that does not exist. `git grep tip-chat` finds no caller. The reason is given
honestly in `SecurityAdvisor.jsx:38-52` (Cloudflare Bot Fight Mode 403s
Supabase's Deno IPs), so the abandonment was deliberate — but the abandoned
file was committed rather than dropped, with its header unrevised and its debug
relay intact. Either delete it or correct the header to say it is superseded;
as merged, a future reader deploying it on the strength of its own docs would
ship the leak above.

Same class as the CLAUDE.md-recorded lesson about a disabled flag making a test
block pass vacuously: code that reads as wired and is not.

### `src/components/SecurityAdvisor.jsx:54,448-455` — NEEDS-REVIEW
**Commit:** `0e06180d` (#1614)
**Change summary:** The Advisor's chat request moved from
`${SUPABASE_URL}/functions/v1/tip-screen` to
`${VITE_TIP_BASE_URL}/api/v1/chat`, and the request headers went from

```
{ "Content-Type", "apikey": ANON_KEY, "Authorization": Bearer ANON_KEY }
```

to `{ "Content-Type": "application/json" }` — no credential of any kind.

**Assessment:** The endpoint is now reachable by anyone who knows the URL, and
`tip.veyrnox.com` ships in the client bundle and in the `index.html` CSP, so
it is public knowledge. The commit is candid that the anon key was never
authentication, and it is right — but it *was* a filter against unkeyed
drive-by traffic, and removing it leaves the per-`device_id` cap (30 turns /
24h) as the only control on a Llama-3.1-8B inference endpoint that Veyrnox
pays for.

`device_id` is minted client-side by `getOrCreateDeviceId()` and sent in the
request body. Nothing binds it to anything. Rotating it per request is a
one-line change for a caller, so the cap bounds an honest client and no one
else. This is an uncapped third-party inference-cost vector, not a wallet-key
exposure — no I1/I2 consequence, and consent gating is correctly enforced at
the egress point (`sendMessage` checks `hasAdvisorConsent()` before any fetch,
verified at `SecurityAdvisor.jsx`). Rate limiting belongs on the Worker, keyed
on something the client does not choose.

### `src/components/SecurityAdvisor.jsx:483-488` — NEEDS-REVIEW
**Commit:** `0e06180d` (#1614)
**Change summary:** Two comments, in the wallet and in the proxy, describe the
planned paid-tier mechanism:

> `// Vault subscribers eventually prefix this with "vault:" to bypass the cap (see companion PR).`

> `// Do NOT touch device_id — the client controls whether it prefixes with 'vault:' to bypass the free-tier cap, and the proxy has no business overriding.`

**Assessment:** As designed, the Safety Plus entitlement for the Advisor is a
client-supplied string prefix. Any caller sends `device_id: "vault:anything"`
and is a subscriber. Broken access control (A01) — the entitlement check lives
entirely on the untrusted side.

Flagged now, before the companion PR, because this is far cheaper to redirect
at design time than after a paid tier depends on it. The prefix is not yet
emitted by any code path, so nothing is bypassable today; the finding is
against the documented plan, and both comments are explicit enough that the
plan is unambiguous. The RevenueCat entitlement is already verified
server-side elsewhere in this codebase — that verdict, not a client string, is
what the Worker should key on.

### `src/wallet-core/evm/simulate.js:433,491` — NEEDS-REVIEW
**Commit:** `25fade75` fix(simulate): restore EVM revert prediction, keep the I4 honesty fix (#1597)
**Change summary:** Restores the `willRevert` assignment, which had been
missing entirely — the commit's own comment records that the branch was
"permanently false, its branch below unreachable". On the restored path,
`revertReason = extractRevertReason(callRes.reason)` is interpolated into a
`level: 'high'` risk detail and into `TransactionPreview.jsx:113`.

**Assessment:** `extractRevertReason` is
`e?.reason || e?.shortMessage || e?.info?.error?.message || e?.message || null`.
The third term is the raw JSON-RPC error message from the provider. It reaches
the signing UI with no length cap, no allowlist, no truncation — I checked both
render sites and neither bounds it.

The threat model is not hypothetical or mine; it is stated 40 lines above the
change, in the file's own words: a malicious RPC can *"inject an inflammatory
revertReason on a legitimate one"*. React escapes markup so there is no XSS,
but the injected value lands inside the highest-severity warning the preview
can show, which is exactly where a user is primed to act on text. A hostile or
compromised endpoint can render arbitrary prose — including instructions — in
the app's own voice, on the screen where someone is deciding whether to sign.

Attribution matters here: this is a **latent path this window activated**.
Before `25fade75`, `willRevert` was never true and neither render site could
fire. The commit is otherwise a genuine improvement (it fixes a real
three-valued-rejection bug and removes raw error text from the *degraded*
path, at `simulate.js:440-447`, explicitly citing CLAUDE.md A10). The
inconsistency is that the same reasoning was not applied to the revert branch
it was restoring. A cap and a decoded-reason-only rule would close it.

### `.github/workflows/ci.yml:21-25` — NEEDS-REVIEW — **RESOLVED, see remediation status below.**
**Commit:** `801f424c` fix(edge): /api/* was unreachable from the native apps (#1610)
**Change summary:** `#1610` added a runtime `EDGE_BASE_UNSET` throw for native
builds with no `VITE_EDGE_BASE`, and set `VITE_EDGE_BASE` in
`android-e2e-emulator.yml`. It did not set it anywhere else.

**Assessment:** The repo variable exists and is correct
(`VITE_EDGE_BASE = https://veyrnox-prod.pages.dev`, set 2026-08-07), and
`git grep VITE_EDGE_BASE` over `origin/main` returns exactly one workflow
reference — the emulator one. `ci.yml`'s workflow-level `env:` block carries
`VITE_WALLETCONNECT_PROJECT_ID`, `VITE_TIP_BASE_URL`, `VITE_SUPABASE_URL` and
`VITE_SUPABASE_ANON_KEY`, and **not** `VITE_EDGE_BASE`. The `android-release`
job runs `npm run build` → `npx cap sync android` → `bundleRelease` under that
block.

So the AAB CI produces still builds with `EDGE_BASE = ''`, and every
`/api/*` call — prices, klines, news, gas, referrals, telemetry, Transak buy
session — throws `EDGE_BASE_UNSET` on device. The fix landed on the path that
tests native and not on the path that ships it.

Not rated REGRESSION: before `#1610` those calls failed silently, and now they
fail loudly, which is strictly better (I4) and is what the throw is for. But
the defect `#1610` was opened against is still present in the shipping
artifact. One line in `ci.yml`'s `env:` block closes it.

### `functions/api/rpc/[fn].js:100-108` — NEEDS-REVIEW (minor) — **RESOLVED, see remediation status below.**
**Commit:** `9c424998` fix(api): let the RPC proxy use service_role, unblocking the H-3 REVOKEs (#1606)
**Change summary:** `supabaseKey` becomes
`env.SUPABASE_SERVICE_ROLE_KEY || env.SUPABASE_ANON_KEY`. The error path is
unchanged: on `!res.ok` it returns `parsed.message || parsed.error` from
PostgREST to the client.

**Assessment:** The privilege change itself is sound and unusually well
guarded — `ALLOWED_RPCS` is a closed `Set` checked before any use, `fn` is
`encodeURIComponent`'d into `/rest/v1/rpc/` only, client headers are not
forwarded, and the anon fallback makes the deploy ordering safe. The comment
block documenting all of this is exemplary, including its refusal to describe
H-3 as closed.

What did not change is that PostgREST error text still reaches the client
verbatim, now produced under a key that bypasses RLS. Under `anon`, a denied
call surfaced a permission error; under `service_role`, a failing statement
surfaces the underlying database message — constraint names, column names,
PostgreSQL error codes. That is the response-hygiene rule again, and again the
fix already exists next door in `buy/session.js`'s new `upstreamErr()`
(generic message + `ref` + `console.error`). Same treatment applies cleanly
here.

---

## Notable positives

- **`functions/api/_middleware.js:27-52` — SAFE, and closes a live hole.**
  `d1dc45bf` (#1598) replaced
  `allowed.find(o => origin === o || origin.endsWith('.pages.dev'))` — which
  ignored the allowlist entry it was nominally comparing and reflected **any**
  `*.pages.dev` origin into `Access-Control-Allow-Origin` — with
  `allowed.includes(origin) || PAGES_PREVIEW_RE.test(origin)`. `*.pages.dev` is
  a free self-service namespace, so the old form let anyone deploy
  `evil.pages.dev` and reach every `/api/*` route cross-origin, including
  `/api/rpc/[fn]` and `/api/buy/session`. The replacement anchors on this
  project's own hosts and forbids a dot in the slug, so
  `evil.veyrnox-prod.pages.dev.attacker.com` cannot match. Unmatched origins
  get the canonical origin rather than their own. Covered by the new
  `middleware-cors.test.js` (+108, additive).
- **`functions/api/buy/session.js` — SAFE.** First rate limit on the endpoint
  (10/IP/60s), unknown IP gets no allowance rather than a shared bucket,
  `fiatAmount`/`fiatCurrency` validated instead of coerced (`Number('abc')` →
  `NaN` → `null` on the wire, now a 400), and the upstream-error echo replaced
  with a correlation id. The fail-open on cache error is documented as a
  considered exception with its reasoning, and the comment explicitly refuses
  to overclaim what a per-colo non-atomic counter provides.
- **`supabase/functions/tip-screen/index.ts` — SAFE.** `0a69a6f9` (#1596)
  removes `http://localhost` from the compiled-in CORS allowlist (L-9
  re-regression from 2026-08-06), pointing local dev at the `ALLOWED_ORIGINS`
  env var instead.
- **`sql/api-security-hardening.sql` — SAFE.** `a22b27dd` (#1609) adds
  `AT TIME ZONE 'UTC'` to the L-8 dedup index and the two read paths.
  `date_trunc(text, timestamptz)` is STABLE, not IMMUTABLE, so the original
  statement could never have been created on any database — verified against
  the live project and rolled back. Fixes a dead migration *and* a
  session-timezone correctness trap in one change.
- **`sql/live-project-hardening-2026-08-07.sql` — SAFE.** STAGE 1 applied and
  verified against the live project: `record_attribution` and
  `get_referral_leaderboard` REVOKEd from PUBLIC/anon/authenticated, `waitlist`
  reduced from `GRANT ALL` to `INSERT` for anon, L-8 index created. STAGE 2 is
  commented out with the ordering dependency spelled out. The file leads with
  the project-ref confusion that cost a full session and tells the reader to
  confirm the ref, not the name.
- **`src/wallet-core/evm/simulate.js` (degraded path) / `TransactionPreview.jsx` — SAFE.**
  `simulation_unavailable` raised `info` → `medium`, raw RPC error text dropped
  from it, `degraded` added as a field distinct from `!simulated` (BTC/SOL are
  decode-only by design), and `noKnownRisks` keyed off the *unfiltered* risk set
  so a display filter cannot buy its way into a reassurance. `role="alert"` on
  the revert banner, `aria-live="polite"` on the risk list.
- **`.github/workflows/deploy-preview.yml` — SAFE.** Removes
  `github.ref == 'refs/heads/main' && 'true' || 'true'` — a tautology that read
  like a production gate — and stops passing `VITE_TRANSAK_API_KEY` into the
  build, which nothing under `src/` reads and which a single future
  `import.meta.env` reference would have baked into the client bundle.
- **`.claude/workflows/branch-review.js` — SAFE.** `fb25a8cb` (#1608) makes the
  review target a required argument and throws without it, rather than
  inheriting the shared checkout's HEAD. On 2026-08-07 that default spent 29
  agents reviewing a branch nobody asked about and returned confident,
  well-formed findings about it. Fail-closed is the right call.
- **`src/wallet-core/shamir.js` — SAFE.** Doc-only. Corrects JSDoc that still
  described the pre-audit v1 envelope and the old "caller MUST authenticate"
  boundary; combine() now verifies a SHA-256 commitment itself (H-6), and the
  GF arithmetic is branch-free (M-7). Notably does *not* upgrade the claim to
  end-to-end constant-time.
- **`index.html` CSP — SAFE.** Adds `https://veyrnox-prod.pages.dev` to
  `connect-src`, required by the native `/api/*` base. `VITE_TIP_BASE_URL` is
  `https://tip.veyrnox.com`, which was already present — checked, because
  `#1614` moved the Advisor to a direct browser fetch and a missing origin
  would have CSP-blocked it.
- **`package-lock.json` — SAFE.** `js-yaml` 4.3.0 → 4.3.1, dev-only, Dependabot.

## Observation (pre-existing, not from this window)

`src/api/edgeApi.js:6` claims *"I3 CHOKEPOINT: every function checks
isDeniabilityOrDemoActive() before making a network call."* That is not true
and was not true before this window either: `fetchPrices`, `fetchKlines`,
`fetchCoinGecko`, `fetchGasFees` and `fetchNews` do not call `i3Guard()`. Only
`createBuySession`, `rpc`, `edgeFn` and `fetchOkxCandles` do.

I3 does hold today, via caller-level gates — verified in `CryptoNewsFeed.jsx`
and `GasTracker.jsx`, both of which compute `egressAllowed = i3Active && !DEMO`
and gate `useQuery`'s `enabled` plus the refetch trigger. So this is a
documentation-accuracy item, not a live violation. It is worth correcting
anyway: a header asserting a chokepoint invites the next caller to skip a gate
that is actually enforced three files away. This is the same shape as the
two-chokepoint lesson recorded in CLAUDE.md for `lib/consent.js`.

## Non-security changes (summary)

`CLAUDE.md`, `docs/rpc-service-role-migration.md`, `docs/security-diffs/*`
(prior reports), and six new/extended test files —
`functions/api/__tests__/middleware-cors.test.js`,
`functions/api/buy/__tests__/session.test.js`,
`functions/api/rpc/__tests__/rpc-proxy.test.js`,
`src/api/__tests__/edgeApi.nativeBase.test.js`,
`src/components/__tests__/{SecurityAdvisor.abort-on-deniability,TransactionPreview.revert-degraded}.test.jsx`,
`src/wallet-core/__tests__/{shamir.doc-drift,simulate-revert}.test.js`.
All additive; `simulate.test.js` (+31/-…) was extended, not weakened — no
assertion was removed.

Also in-window and reviewed as SAFE:
`b86ab3e4` (#1617) aborts an in-flight Advisor SSE stream when a session
becomes deniable. `if (hidden) return null` does not unmount, so an open
stream survived duress/panic — the one moment it matters. Two effects: abort
on `hidden`, abort on unmount. Correct fix, and the reasoning about *why*
render-null is not unmount is right.

## Pattern-list maintenance

Per the task's own rule: `src/components/SecurityAdvisor.jsx` produced three of
this run's six findings and matches **no** pattern in Step 2's list.
`supabase/**` already covers `tip-chat`. Recommended addition to the
"Telemetry / consent / deniability" or a new "AI advisor" group:

- `src/components/SecurityAdvisor.jsx`
- `src/lib/advisorConsent.js`
- `src/lib/advisorKnowledge.js`
- `src/api/tipScreen.js`

This is the **fourth consecutive run** whose real findings sat partly outside
the list. As the task file notes, this write-up cannot apply the change itself
— it may only write its own report.

## Post-scan re-check — **SUPERSEDED, see the post-merge correction below.**

*Accurate when written (checkpoint 1, pre-commit). It names a one-commit gap;
by the time the report actually merged the gap was five commits wide. Left
standing as the point-in-time record it was.*

Verified against `origin/main` at `35d85509` (2026-08-08 08:10:24 UTC). The
scan window closed at `b86ab3e4`; one commit landed after it —
`35d85509` fix(advisor): multi-source threat aggregator + honest UNKNOWN
verdict (#1615).

**No open item is resolved by it, and no amendment is needed.** It touches
`src/components/SecurityAdvisor.jsx` substantially (+116), which is where three
of this report's findings sit, so it was diffed rather than judged from its
subject line: it changes the threat-aggregator and verdict path only. It does
not touch `TIP_CHAT_URL`, the request headers, or `device_id` — a grep of its
diff for those terms returns nothing. The no-credential direct-to-Worker call
and the planned `vault:` prefix both stand as written. `tip-chat/index.ts`,
`simulate.js`, `ci.yml` and `functions/api/rpc/[fn].js` are untouched by it.

## Post-merge correction (2026-08-08, Step 6)

Verified against `origin/main` at `b8096128` — the merge commit of this report's
own PR (#1620), merged 2026-08-08 08:48:36 UTC. Checked 2026-08-08 08:49 UTC.

**No finding was resolved. All six NEEDS-REVIEW items stand.** Two things the
pre-commit re-check above could not see are corrected here.

### 1. The real publication gap was five commits, not one

The section above verified against a one-commit gap. Between window close
(`b86ab3e4`) and publication (`b8096128`), five commits landed:

| commit | PR | relevance |
|---|---|---|
| `35d85509` | #1615 | advisor threat aggregator — checked above, resolves nothing |
| `139f07e9` | #1621 | docs only |
| `9d3af466` | #1622 | `okx-candles` error-code preservation — see note below |
| `86cd501f` | #1619 | **tip-chat / tip-screen — see §2** |
| `b8096128` | #1620 | this report |

Each of the six findings was re-verified against `b8096128` by reading the
file on `main`, not by inferring from subject lines:

- **tip-chat TEMP DEBUG relay** — still present, `tip-chat/index.ts:200-208`.
- **tip-chat header claims a wiring that does not exist** — still present,
  `tip-chat/index.ts:42`. #1619 makes this *more* wrong, not less: its commit
  message states outright that "nothing under `src/` has called it since"
  #1614.
- **Advisor sends no credential** — still present,
  `SecurityAdvisor.jsx:539`.
- **`vault:` prefix entitlement** — still present, `SecurityAdvisor.jsx:575`.
- **`VITE_EDGE_BASE` absent from `ci.yml`** — still absent; the workflow-level
  `env:` block is byte-identical (`ci.yml:21-25`), file untouched in the window.
- **RPC proxy relays PostgREST error text under `service_role`** — still
  present, `functions/api/rpc/[fn].js:104`, file untouched in the window.

`src/wallet-core/evm/simulate.js` is also untouched, so the unsanitised
`revertReason` render stands.

`9d3af466` (#1622) was written by this same session in response to the
`staging-gate` investigation and touches only `functions/api/data/okx-candles.js`
— no overlap with any finding. Recorded rather than omitted, because a scan
report silently containing its own author's change is exactly the kind of thing
a reader should not have to discover.

### 2. #1619 removed a real vulnerability this scan did not find

`86cd501f` deleted an `action: 'chat'` branch from
`supabase/functions/tip-screen/index.ts` that forwarded to
`/api/v1/agents/security-advisor/chat`. Per that commit, the route:

- forwarded `messages` upstream verbatim — no role allowlist, no per-message
  length cap, no message-count cap;
- was **HMAC-signed**, which is precisely what gets tip-screen past Cloudflare
  Bot Fight Mode (#1614 established that the unsigned tip-chat proxy gets a 403
  challenge instead);
- carried no `device_id`, so the Worker's 30-turns/24h per-device cap never
  applied.

A signed, uncapped path to the LLM that laundered both controls protecting the
real endpoint — serving a route with no callers.

**This scan looked at `tip-screen/index.ts` and missed it.** The 07-28 L-9
`http://localhost` removal in that file was reviewed and rated SAFE; the review
stopped at the diff hunk and never read the rest of the function for a dead
route. That is a general lesson, not a one-off: **a diff-scoped read cannot find
a dead route, because a dead route produces no diff.** When a commit touches a
file's *allowlist or auth posture*, read the file's routes, not just the hunk.
Same family as the standing rule on orphaned storage keys — what makes both
dangerous is presence, not activity.

#1619 also added `MAX_CHAT_MESSAGES` (40), `MAX_CHAT_CONTENT` (8192) and a
role allowlist to `tip-chat`, rejecting the whole request rather than filtering
entries (I4). Good, and it narrows the surface — but it does **not** touch the
TEMP DEBUG relay or the header claim, so findings 1 and 2 are unaffected.

## Remediation status (2026-08-08, same day)

Every row verified by reading the file on `main`, not by trusting a merge
status. **3 of 7 closed, 1 in flight, 3 open.**

| # | finding | status | evidence |
|---|---|---|---|
| 1 | `tip-chat` TEMP DEBUG upstream relay | **CLOSED** | `6461b8eb` (#1628) |
| 2 | `tip-chat` header claims a wiring that never existed | **IN FLIGHT** | #1629 |
| 3 | Advisor sends no credential to the TIP Worker | **OPEN** | needs TIP Worker change |
| 4 | `vault:` prefix as a client-supplied entitlement | **OPEN** | needs TIP Worker change |
| 5 | `simulate.js` unsanitised `revertReason` in the signing UI | **OPEN** | untouched |
| 6 | `VITE_EDGE_BASE` absent from `ci.yml` | **CLOSED** | `f0c45770` (#1626) |
| 7 | RPC proxy relays PostgREST text under `service_role` | **CLOSED** | `b24fe6f3` (#1630) |

### What the closures actually did

- **#1628** — generic `{ error, ref }` to the caller, full detail to
  `console.error`. The 402 branch still relays its body deliberately (that is
  the Advisor cap driving the upgrade prompt) and now has its own test so a
  later sweep cannot take the cap UX with it.
- **#1626** — one line in `ci.yml`'s workflow-level `env:`. It also needed
  `VITE_EDGE_BASE: ''` in `vitest.config.js`: `edgeApi.js` reads the var at
  module load and three tests assert behaviour that only exists when it is
  UNSET, so they had been passing on the accident of the var being absent from
  CI. Setting it for the build turned `unit-tests` red. Same class as the
  `.env.local` leak in #1328.
- **#1630** — NOT a blanket genericisation. Two things depended on the
  passthrough: `referralApi.js` matches `e.message?.includes('not found')`, and
  the 42501 text was the documented canary for the H-3 ordering trap. Split on
  the discriminator the SQL already provides — errors carrying our own
  `RAISE … USING errcode` SQLSTATEs (`P0001`, `P0003`, `P0006`, `P0007`,
  `P0008`, `22004`) are author-written and forwarded; everything else is
  Postgres describing itself and is withheld + logged. The canary moved to the
  tail log rather than being deleted.

### Why 3 and 4 are not closed here

Both are TIP Worker problems, not wallet problems. The wallet cannot
authenticate itself to an endpoint that has no auth, and it cannot enforce an
entitlement the Worker reads from a client-supplied `device_id`. Rate limiting
has to be keyed on something the caller does not choose, and the paid tier on
the RevenueCat entitlement this codebase already verifies server-side. Any
change confined to this repo would be theatre — the fix belongs in the Worker.

### Why 5 is not closed

No reason beyond attention: it was under-reported from the moment the count in
the header was written as six, and never picked up. It is the one finding on
this list that touches the SIGNING screen — a hostile or compromised RPC can
render arbitrary prose inside the highest-severity warning `TransactionPreview`
shows, unbounded and unsanitised. On severity it arguably outranks two of the
three that were fixed today. Recorded plainly so the next reader does not have
to rediscover it.

### Verification standard

All three closures are INTERNAL: tests green, mutation-checked, merged. None is
device-verified and none has an on-chain txid. #1626 in particular sets a build
variable — only a real native build proves the edge calls reach the Pages
Functions.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, no
device verification, no on-chain confirmation. Not the outstanding independent
third-party audit.*
