# Security Diff — 2026-09-15

Commits scanned: 12 in last 24h
Security-sensitive files changed: 36

## ⚠️ NEEDS-REVIEW

- `src/lib/otaUpdate.js:23` / `src/main.jsx:59` — provisioning the OTA signing key would enable a mandatory pre-unlock request to `updates.veyrnox.com`. That happens before the app can know whether the next unlock is real, decoy, hidden, or demo. The current empty native key keeps it inert, but activation conflicts with I3's strict zero-backend-call wording for deniability sessions unless the owner explicitly accepts and documents that policy change.

## Flagged changes

### `src/lib/otaUpdate.js:23`, `src/main.jsx:59`, `docs/ota-updates.md:51` — NEEDS-REVIEW
**Commit:** `a00fbaae` feat(ota): signed over-the-air web bundle updates (BUILT, inert until a key is pinned) (#2568)

**Change summary:** Adds a pre-unlock native OTA download flow. The JavaScript side reports boot readiness, reads an unsigned version pointer, downloads to a native-created staging directory, and never makes a trust decision; the document correctly states that the pinned public key is currently empty and OTA is disabled.

**Assessment:** The empty `OtaConfig` key on both platforms means `status.enabled` is false and no update-host request is made today, so this is not an active I2/I3 regression. Before any key is pinned, however, the flow will emit a per-cold-start network request before session type is known. The design document itself says owner I3 sign-off and a privacy disclosure are required before provisioning. Keep OTA **BUILT / inert**, obtain that decision, and complete native device verification before enabling it; no "verified" status is supported by this static review.

### `android/app/src/main/java/com/veyrnox/app/OtaBundleVerifier.kt:27`, `android/app/src/main/java/com/veyrnox/app/OtaUpdatePlugin.kt:89`, `ios/App/App/VeyrnoxOta.swift:23` — SAFE
**Commit:** `a00fbaae` feat(ota): signed over-the-air web bundle updates (BUILT, inert until a key is pinned) (#2568)

**Change summary:** Adds native verification and launch state machines on Android and iOS: P-256 signature validation against a store-pinned public key, exact manifest/file hash checks with no extras, channel/native-API checks, monotonic versions, and rollback after an unready pending boot.

**Assessment:** Static review found the trust decision remains native and fail-closed: a missing/invalid key or embedded manifest removes staged bundles and serves the store bundle; a malformed signature, manifest, path, hash, or version is rejected. Android has JVM coverage for the verifier and both native keys are statically pinned equal. This is INTERNAL static analysis only; the newly added native paths are not device-verified.

### `scripts/ota/manifest.mjs:12`, `scripts/ota/release.mjs:27`, `vite.config.js:168`, `capacitor.config.json:18` — SAFE
**Commit:** `a00fbaae` feat(ota): signed over-the-air web bundle updates (BUILT, inert until a key is pinned) (#2568)

**Change summary:** Generates deterministic manifests, requires release-time rehashing and recorded build flags, rejects the RASP-bypass, dev-send-ungate, and demo flags, and disables Capacitor's unverified persisted deployment path.

**Assessment:** The release tooling verifies the exact manifest bytes and refuses a changed or development-flagged `dist`; native code independently verifies the signed manifest and staged files at launch. `.gitignore` also prevents the documented OTA staging tree and private-key filename from being committed. The public key is deliberately absent, so the control remains honest-disabled at runtime.

### `workers/referral-redirect/src/index.js:38`, `workers/referral-redirect/src/invitePage.js:118`, `workers/referral-redirect/wrangler.toml:16` — SAFE
**Commit:** `1b5975e8` feat(referral): serve a dedicated invite page at veyrnox.com/r/<code>

**Change summary:** Replaces a redirect into the web wallet with a standalone invite page. The Worker continues to use the existing shared validator, preserves the one-segment path rule, escapes the displayed code, serves a nonce-bound restrictive CSP, and does not load wallet code or third-party assets.

**Assessment:** Static review found no new secret, binding, open redirect, unvalidated code reflection, or client egress sink. Invalid/nested paths result in a 404 invite page without a referral code; the valid code reaches the Google Play referrer only through URL encoding. Deployment and live routing remain unverified in this run.

### `src/components/WalletEntry.jsx:655`, `src/lib/referralAttribution.js:61` — SAFE
**Commit:** `2803732c` fix(referral): route the Create Wallet invite field through the shared capture path (#2569)

**Change summary:** Manual Create/Import referral entry now validates through `captureReferralCode`, normalizes valid codes, preserves the existing deniability guard, and refuses malformed input rather than storing it.

**Assessment:** This closes a capture-path inconsistency without weakening the deniability condition; in a deniable session a syntactically valid input returns success but neither persists referral state nor emits the referral event. The associated tests pin both Create and Import paths.

### `.github/workflows/ios-xcuitest-smoke.yml:191`, `ios/App/AppUITests/AppUITests.swift:173` — SAFE
**Commit:** `1578c560` ci(ios): remove the xcuitest warm-up step (#2543) (#2573); `2cdc043b` test(ios): confirm the xcuitest import submit press (#2543)

**Change summary:** Removes a simulator warm-up that repeatedly wedged CI and makes the iOS import smoke test confirm that the submit press was accepted before checking its fail-closed outcome.

**Assessment:** The product's security gate is not removed: the test still requires a known fail-closed banner. The workflow remains a non-required smoke signal pending its documented promotion criterion; this change has no production authentication or signing-path effect.

### `docs/audit-2026-09-14-weekly.md:1`, `docs/audit-findings-tracker.md:1`, `docs/branch-protection-config.md:45` — SAFE
**Commit:** `885490ad` docs(audit): weekly internal security audit 2026-09-14 (#2567); `b4d50ac5` docs(audit): update findings tracker 2026-09-14 (#2571)

**Change summary:** Records new internal-static findings and their unresolved status, and updates the iOS smoke-check promotion criterion.

**Assessment:** The documents consistently label the audit as internal/static, retain the independent-audit and device/on-chain verification gaps, and do not promote any status to verified.

## Non-security changes (summary)

None. Every changed file either adds or tests an OTA code-loading boundary, processes referral data at an internet-facing route, affects a fail-closed onboarding smoke check, or records security posture.

## Post-scan re-check

Verified against `origin/main` at `7c9bc43d3decc5702153cdbd7af6259c8e79d9aa` (2026-09-15T09:04:12Z). Nothing landed between window close and this check.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, no device verification, no live deployment verification, and no on-chain confirmation.*
