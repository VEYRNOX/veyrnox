# Security Diff — 2026-07-20

Commits scanned: 27 in last 24h (`4703ca87..c19ae2b9` on `origin/main`)
Security-sensitive files changed: 1 (pattern-matched) + 4 adjacent (noted below)

## Flagged changes

### src/lib/WalletProvider.jsx:2364-2389 — SAFE
**Commit:** 82d5e191 `fix(types): stop useWallet() narrowing to `never` — removes the cast litter (#1238)`
**Change summary:** `useWallet()` now returns `/** @type {any} */ (ctx)` instead of `ctx`, plus a 20-line explanatory comment. No other line in the file changed.
**Assessment:** SAFE. JSDoc cast, erased at build — zero runtime effect. No key material, signing, gating, or auth logic touched. The null-guard (`if (!ctx) throw`) is preserved unchanged, so the fail-closed behaviour on out-of-provider use is intact. The comment is honest about the tradeoff (it names the loss of member-name/signature checking rather than claiming type safety) and records the real fix as deliberately out of scope for a security-sensitive file. Nothing here claims verification.

## Adjacent changes reviewed (not on the flagged-path list, but egress/build/trust relevant)

### src/api/referralApi.js:65-78 — SAFE
**Commit:** 7afadc6e `fix(referral): tier progression based on paid subscribers, not raw count (#1235)`
**Change summary:** `fetchReferrerTier` aliased to `fetchStatus`; new `fetchPaidCount(code)` queries `referral_attributions` with a `head: true` exact count.
**Assessment:** SAFE for I3. The new export carries the same `if (!supabase || isDeniabilityOrDemoActive()) return null;` chokepoint guard as every other function in the file — the LIVE helper, which covers both the session marker and the persisted `veyrnox-demo=1` flag. No wallet address, balance, or seed-derived value is sent; the query key is the referral code only. Fails closed to `null` on any error.

### vite.config.js:80-84,103 — SAFE
**Commit:** 9fbe2ece `fix(build): exclude Vite internals from JS obfuscator to fix code-split CSS loading (#1249)`
**Change summary:** `preload-helper` and `rolldown-runtime` chunks are skipped by the release obfuscator; `reservedStrings: ['\\.css$', '\\.js$']` added for all other chunks.
**Assessment:** SAFE. Obfuscation is defence-in-depth hardening, never a security control on funds — narrowing it does not remove a gate. The excluded chunks are Vite's own asset-path resolver and module linker, which contain no wallet logic. Worth recording honestly: release bundles now ship two unobfuscated framework chunks. That is a reduction in reverse-engineering friction only, with no I1–I5 consequence.

### src/lib/stubs/revenuecat-stub.js:8-22 — SAFE (posture note)
**Commit:** 4703ca87 `fix(referral): full referral/subscription pipeline + haptic polish (#1207)`
**Change summary:** Web dev stub gained `LOG_LEVEL`, `setLogLevel`, and `setAttributes` no-ops.
**Assessment:** SAFE — this file is a dev-server import shim; every method in `purchases.js` guards on `isNative() === true` before reaching it, so the stub is unreachable at runtime. Posture note only: `setAttributes` appears on CLAUDE.md's "explicitly NOT added — invariant-violating (identity leak)" list from the RevenueCat SDK audit. The real call site (`purchases.js:95-103` `setReferralAttribute`) carries an in-source **owner override dated 2026-07-18** narrowing it to referral-code-only attribution, with the reasoning recorded (the code identifies the referrer, not the purchaser; no address, seed, or balance sent) and an I3 gate at the `Subscription.jsx` call site. Documented and scoped, not silent drift — but the CLAUDE.md "NOT added" list is now stale on this line and should be reconciled.

### .githooks/pre-commit — SAFE (new file)
**Commit:** da0a72bd `chore(ci): add pre-commit stray-artifact hook (#1211)`
**Change summary:** New client-side hook running `scripts/check-stray-files.mjs`, wired via `core.hooksPath`.
**Assessment:** SAFE. Honestly self-labelled ("Not a security control on user funds — it is dev tooling"), fails **open** on a missing toolchain by design, and names CI (`npm run check:strays`) as the actual backstop. Bypassable with `--no-verify`, which is stated. Correct posture for a dev-hygiene hook: no false assurance.

### .claude/settings.json — SAFE
**Commit:** 05885844 `fix(hooks): remove broken hook entries that spawn interactive cmd.exe (#1218)`
**Change summary:** Removed ~10 hook entries whose commands shelled out to `cmd /c` wrappers.
**Assessment:** SAFE, and a net reduction in local execution surface. Agent-harness configuration only — not shipped in any build, not reachable by the app.

## Non-security changes (summary)

UI/nav/a11y: `Layout.jsx`, `navigation.js`, `parentRoute.js`, `useRecentPages.js`, `CommandPalette.jsx`, `NotificationBell.jsx`, `HelpMenu.jsx`, `Spinner.jsx`, `dropdown-menu.jsx`, `AccessibilityWrapper.jsx`, `ErrorBoundary.jsx`, `FeatureGate.jsx`, `PullToRefreshContainer.jsx`, `SessionRevocationGuard.jsx`, `motion-tokens.js`, `haptics.js`, `HDWalletManager.jsx`, `LandingPage.jsx`, `Documentation.jsx`, `TermsLegal.jsx`, `App.jsx`, `featureClassification.js`. Referral/subscription app-layer: `referral.js`, `ReferralTracker.jsx`, `Subscription.jsx`, `setup-referral-offerings.mjs`. Build/deps: `package.json`, `package-lock.json` (`@ledgerhq/hw-app-eth` 7.8.8→7.8.9), `android/app/capacitor.build.gradle`, `android/capacitor.settings.gradle` (restore missing `capacitor-haptics` include), `.gitignore`, `scripts/setup-hooks.mjs`. Docs: `CLAUDE.md`, `docs/audit-findings-tracker.md`, `docs/dependency-audits/*`, `docs/security-diffs/diff-2026-07-19.md`. Tests: `capacitor-gradle-sync`, `terms-legal`, `Layout.mobileSearch`, `navigation-icons`, `HDWalletManager.*`, `Subscription.test.jsx`.

## Notes carried forward

- No RASP, keystore, vault, sign-gate, WalletConnect, or native hardware-KEK plugin file changed in this window.
- One reconciliation item for the owner: the CLAUDE.md RevenueCat "explicitly NOT added" list still names `setAttributes`, which the 2026-07-18 owner override has since permitted in narrowed form. Doc drift, not a code defect. — **RESOLVED, see amendment below.**

## Amendment (2026-07-20, post-scan)

The reconciliation item above was already closed by the time this report was
written. `da1e1973` (PR #1251, *"docs(play): resolve 5/9 Data Safety
owner-decisions from code + correct stale setAttributes claim"*) landed on
`origin/main` after the scan window closed at `c19ae2b9`, so it fell outside the
diff range and the scan could not see it.

PR #1251 removes `setAttributes` from the "Explicitly NOT added" list
(`CLAUDE.md:1295`, leaving `setEmail`/`setPushToken`/`setDisplayName` in place)
and adds a dated CORRECTION block at `CLAUDE.md:1305-1317` recording the honest
scope: a referral *code* identifying the referrer rather than the purchaser,
attached to RevenueCat's anonymous app-generated user ID — no wallet address,
seed, balance, or personal identifier — best-effort and guarded, with I3
unaffected because all RC calls stay behind `isDeniabilityOrDemoActive()` at the
API layer.

It also carries one consequence this report did not identify: the tag is a
**persistent attribution association**, so it must be declared in the Play Data
Safety form under "Device or other IDs" (`docs/play-launch/data-safety-form.md`,
⚠ OWNER-DECISION 5). No action outstanding.

**Scan-process note.** A same-day fix can land between the scan's window close
and the report's merge, so a "carried forward" item is accurate only as of the
window boundary, not as of merge. Re-checking `origin/main` immediately before
opening the PR would catch this class; noted for the task definition rather than
fixed here.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, no device verification, no on-chain confirmation. Not the outstanding independent third-party audit.*
