# Security Diff — 2026-08-30

Commits scanned: 8 in last 24h
Security-sensitive files changed: 10

## Flagged changes

### docs/SecurityAdvisor-TIP-integration.md:3 and docs/tip-signing-secret-rotation.md:1 — NEEDS-REVIEW
**Commit:** `6e251282` docs(security): mark TIP_SIGNING_SECRET rotated + fix runbook mechanism (#2152)

**Change summary:** The documents record a completed signing-secret rotation across three Workers and two Supabase environments, revise the procedure to redeploy warm Edge Function isolates, and retain only a fingerprint rather than a secret value.

**Assessment:** No plaintext signing secret was added in this diff, and the runbook now states the deployment and rollback steps more completely. The claims that the deployed verifiers reject the leaked value and that every environment was rotated are external operational facts, however, and cannot be confirmed by this repository-only static scan. Treat those claims as unverified here; no feature or audit status is promoted.

### functions/api/buy/session.js:84 — SAFE
**Commit:** `cd705e07` fix(buy): send x-user-ip on Transak partner refresh-token (#2154)

**Change summary:** Threads the already-derived client IP into the Transak refresh-token request, including the retry after a 401.

**Assessment:** `clientIpOf()` accepts only Cloudflare's `CF-Connecting-IP`; missing IPs are rejected by the rate limiter before the upstream call. The token cache remains scoped by the explicit Transak environment, and credentials remain server-side. This adds the partner-required IP header to a request path that already sends the same IP when creating the widget session; no wallet material or client-supplied forwarding header is introduced.

### src/pages/WalletConnect.jsx:88 and src/pages/__tests__/WalletConnect.qrScanner.test.jsx:64 — NEEDS-REVIEW
**Commit:** `6a94e6c3` Add QR scanning to WalletConnect pairing

**Change summary:** Adds a camera-driven WalletConnect URI entry point that validates QR content before placing it in the editable pairing field; the existing pair action still requires an explicit user click and validates again before handing a URI to the SDK.

**Assessment:** The new custom parser calls `validateWcUri()` directly and therefore bypasses `QRScanner`'s existing 2,048-byte inbound-payload ceiling. `validateWcUri()` accepts an unbounded topic and passes the query string to `URLSearchParams` on each camera frame, so a valid-looking oversized QR can repeatedly drive parser work on the hot scan loop. The added test covers valid and basic invalid input but not an oversized WalletConnect URI. No signing, approval, or auto-pair bypass was found, but restore the shared length bound (or add an equivalent WalletConnect-specific one) and pin it with a regression test.

### .env.{canary,production,staging}, src/components/subscription/CancelOfferDialog.jsx, and src/pages/Subscription.jsx — SAFE
**Commits:** `a96dbbd5` / `88a71df5` / `f3db86ef` AI Security Protection subscription presentation

**Change summary:** Configures a public RevenueCat offering identifier, makes cancel-intent copy tier-aware, and preserves the AI Security Protection paywall shape when store packages have not resolved.

**Assessment:** The offering identifier is a public lookup key, not a secret. The UI still derives prices only from store-returned packages, renders an em dash when unavailable, and disables the purchase button until a real package resolves. The authoritative entitlement refresh after purchase remains unchanged; the displayed tier is not made an authorization source.

## Non-security changes (summary)

The prior day's report, WalletConnect scanner styling, and layout-only subscription copy were reviewed as supporting context; no additional security effect was found.

## Post-scan re-check

Verified against `origin/main` at `f3db86efdd5a89d38e1a8bc1e23f26fe18968745` (2026-08-30T09:04:46Z). Nothing landed between window close and this check.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, device verification, on-chain confirmation, or independent audit.*
