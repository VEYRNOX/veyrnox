# Security Diff — 2026-08-21

## ⚠️ REGRESSION DETECTED

- `src/pages/SendCrypto.jsx:1788` — the Digital Shield branch bypasses the normal sign-time mutation chokepoint, including fresh RASP, risk, capability, seed-verification, spend-limit, and one-shot second-factor enforcement.
- `src/wallet-core/hw/digitalShield.js:381` — Bitcoin response verification checks outputs only, not the full original PSBT inputs/transaction.
- `src/lib/WalletConnectProvider.jsx:620` and `src/pages/WalletPortfolioPage.jsx:579` — new Base44 queries are not sealed by the canonical deniability/demo predicate, permitting backend traffic in a session-transition window.

Commits scanned: 6 in last 24h
Security-sensitive files changed: 43

## Flagged changes

### `.github/workflows/canary-release.yml:6` — **NEEDS-REVIEW**
**Commit:** `0ae71e44` ci: add canary release lane and reviewer config (#1931)
**Change summary:** Adds a workflow-dispatch `ref` that is checked out before `npm ci`, build, and scripts run with the Cloudflare deployment token.
**Assessment:** The `push` trigger is pinned to `github.sha`, but a manually supplied branch/tag/SHA is not constrained to an approved ref. Confirm the repository's workflow-dispatch permission model and restrict the input to trusted immutable refs (or remove it); otherwise a user permitted to dispatch workflows can execute selected repository code in a deployment-secret context.

### `.env.canary:20` — **NEEDS-REVIEW**
**Commit:** `0ae71e44` ci: add canary release lane and reviewer config (#1931)
**Change summary:** The canary blanks Supabase configuration but retains the WalletConnect and TIP endpoint variables.
**Assessment:** The smoke test proves absence of a Supabase project URL, not absence of all backend/third-party egress. Confirm that a canary artifact is intentionally allowed to contact WalletConnect/TIP and that this is disclosed to testers.

### `package.json:dependencies` / `package-lock.json` — **NEEDS-REVIEW**
**Commit:** `2ef3b12d` Add Digital Shield air-gap signing integration (#1930)
**Change summary:** Adds `@keystonehq/keystone-sdk` and its transitive UR/signing dependency set for the new air-gapped signer.
**Assessment:** This is a material signing-path supply-chain addition. The lockfile contains no observed lifecycle-script anomaly in this static scan, but the new dependency boundary needs the same independent signing-path audit scope as the feature.

### `scripts/run-post-audit-qa.mjs:139` — **SAFE**
**Commit:** `7b8256c0` fix(codeql): avoid shell-built playwright command (#1926)
**Change summary:** Replaces a shell-built Playwright command with `execFileSync` and an argument array.
**Assessment:** Removes shell interpretation of the suite path; no control was weakened.

### `android/app/src/main/java/com/veyrnox/app/AndroidBiometricCachePlugin.kt:20` — **NEEDS-REVIEW**
**Commit:** `9a96bebf` feat(android): harden compatibility snapshot and biometric cache (#1927)
**Change summary:** Adds an Android Keystore-encrypted cache plus a biometric-enrollment invalidation sentinel.
**Assessment:** The storage key itself is generated without user authentication and `getSecret` exposes plaintext through a callable Capacitor method; the intended biometric check remains in JavaScript. Confirm the native bridge cannot be invoked by injected WebView JavaScript without the biometric precondition, and perform the promised device invalidation exercise. This report makes no device-verification claim.

### `android/app/src/main/java/com/veyrnox/app/VeyrnoxEnclavePlugin.kt:102` — **SAFE**
**Commit:** `9a96bebf` feat(android): harden compatibility snapshot and biometric cache (#1927)
**Change summary:** Extends the native hardware snapshot with device metadata while retaining the RASP block before key creation.
**Assessment:** No downgrade or silent fallback was found in the changed fragment. Device-specific data must remain diagnostic-only and not be treated as an attestation result.

### `src/lib/biometricUnlock.js:126` / `src/plugins/androidBiometricCache.js:1` — **NEEDS-REVIEW**
**Commit:** `9a96bebf` feat(android): harden compatibility snapshot and biometric cache (#1927)
**Change summary:** Routes Android cache reads to the new plugin when it advertises availability, otherwise falls back to generic secure storage.
**Assessment:** The fallback preserves usability on unsupported/older Android but not the new enrollment-invalidation property. The UI correctly remains `app-gate`; ensure release copy never presents the Android sentinel as full native biometric ACL parity, and add a negative test for direct bridge access.

### `src/components/security/{BiometricUnlockSettings,HardwareKekSettings,PasskeyUnlockSettings}.jsx` — **SAFE**
**Commit:** `aa13fed1`, `9a96bebf`
**Change summary:** Consolidates biometric settings and displays the native capability snapshot/tier state.
**Assessment:** The changed UI retains the existing honest distinction between available hardware state and completed enrollment; no verified/audited status claim was added.

### `src/lib/WalletConnectProvider.jsx:620` — **REGRESSION**
**Commit:** `46c5faf0` Transaction intelligence and suspicious asset review (#1929)
**Change summary:** Adds history, address-book, and whitelist Base44 queries for WalletConnect transaction review, enabled solely by `!isDecoy && !isHidden`.
**Assessment:** This omits `isDeniabilityOrDemoActive()` and `isUnlocked`. A panic/stealth transition can set the canonical module-level deniability predicate before the React flags update, and the provider can query while locked. The new calls can therefore emit backend traffic and request real-wallet metadata during an I3-protected interval. **Regression remains open.**

### `src/pages/WalletPortfolioPage.jsx:579` — **REGRESSION**
**Commit:** `46c5faf0` Transaction intelligence and suspicious asset review (#1929)
**Change summary:** Adds a Base44 `WalletToken.list()` query used to render suspicious-token counts, enabled solely by `!isDecoy && !isHidden`.
**Assessment:** The same missing canonical deniability/demo seal permits the new backend query in the session-transition window. It should use the live deniability/demo predicate before issuing any request. **Regression remains open.**

### `src/lib/{spamTokenIntel,suspiciousAssetPrefs}.js` — **NEEDS-REVIEW**
**Commit:** `46c5faf0` Transaction intelligence and suspicious asset review (#1929)
**Change summary:** Introduces local spam overrides, contract-intelligence consent, cache, and NFT-dismissal preferences.
**Assessment:** `suspiciousAssetPrefs` guards writes with the canonical predicate, but `spamTokenIntel` writes a shared localStorage override without it. Confirm the suspicious-assets UI is unavailable in deniability sessions or add the same seal, otherwise a decoy interaction can alter primary-session residue.

### `src/api/tipScreen.js:62` / `src/risk/{composeVerdict,reviewContributor,walletConnectIntel}.js` / `src/policy/signingPolicy.js` — **SAFE**
**Commit:** `46c5faf0` Transaction intelligence and suspicious asset review (#1929)
**Change summary:** Adds a local-first transaction-intelligence composer and WalletConnect risk adapter; TIP retains its canonical deniability/demo check before network fallback.
**Assessment:** The pure policy layer does not replace `presignGate`; malformed/failed intelligence falls back to CAUTION. The egress concern is the new caller queries listed above, not the TIP single egress point itself.

### `src/components/walletconnect/RequestApprovalModal.jsx` — **SAFE**
**Commit:** `46c5faf0` Transaction intelligence and suspicious asset review (#1929)
**Change summary:** Presents transaction-intelligence information in the WalletConnect approval UI.
**Assessment:** No signing handler is moved into the UI; the existing provider-side RASP, binding, and re-auth checks remain the enforcement boundary.

### `src/pages/SendCrypto.jsx:1788` — **REGRESSION**
**Commit:** `2ef3b12d` Add Digital Shield air-gap signing integration (#1930)
**Change summary:** Adds a Digital Shield QR branch that prepares and later broadcasts signed EVM/BTC/Solana payloads instead of calling `sendTx.mutate()`.
**Assessment:** The ordinary mutation re-derives every sign-time control, including a fresh RASP artifact, risk/TIP settlement, `evaluateSendGate`, the hard `canSend` assertion, seed-backup verification, limits, and one-shot 2FA consumption. The Digital Shield branch at `startSendAttempt()` performs none of those checks, and `finalizeDigitalShieldSend()` at line 1891 also does not re-check deniability/demo after the QR wait. UI disabled state is not an enforcement boundary. **Regression remains open.**

### `src/wallet-core/hw/digitalShield.js:381` — **REGRESSION**
**Commit:** `2ef3b12d` Add Digital Shield air-gap signing integration (#1930)
**Change summary:** Adds UR import, request construction, local response verification, and broadcast material for Digital Shield.
**Assessment:** `finalizeDigitalShieldBtcResponse()` compares only the input/output counts and output values/scripts. A response PSBT can replace an original input while preserving those outputs, and this code will finalize and broadcast it. Bind the returned PSBT's complete unsigned transaction (all inputs and outputs, plus required derivation ownership) to the originally requested PSBT before accepting signatures. The added test asserts matching outputs only, so it does not cover this attack. **Regression remains open.**

### `src/wallet-core/hw/{provider.js,__tests__/digitalShield.test.js}` — **NEEDS-REVIEW**
**Commit:** `2ef3b12d` Add Digital Shield air-gap signing integration (#1930)
**Change summary:** Registers the new hardware provider and adds happy-path import/EVM/BTC/Solana tests.
**Assessment:** Coverage lacks negative cases for Bitcoin input substitution, stale session/context changes during QR exchange, and bypass attempts around the SendCrypto chokepoint. Passing happy-path unit tests would not verify the air-gapped flow.

### `src/wallet-core/keystore/{index,keyStore,native,tierBadge}.js` / `src/wallet-core/panic.js` — **SAFE**
**Commit:** `9a96bebf`, `46c5faf0`
**Change summary:** Adds native snapshot accessors/tier presentation and a panic-path import adjustment.
**Assessment:** The inspected changes are metadata/UI plumbing and do not relax KEK derivation, wrapping, or panic behavior. Native/device and independent-audit status remains unchanged.

### `src/components/QRScanner.jsx` / `src/components/hw/UrQrPlayer.jsx` / `src/context/DigitalShieldContext.jsx` / `src/pages/HardwareWalletPage.jsx` — **NEEDS-REVIEW**
**Commit:** `2ef3b12d` Add Digital Shield air-gap signing integration (#1930)
**Change summary:** Adds a public-account QR import context, UR display, and scanner integration.
**Assessment:** Inbound UR fragments are length- and type-constrained in the core parser, and the profile is in-memory. The flow still requires real-device exercise and malicious fragmented-UR tests before any stronger status is claimed.

### `src/components/SecurityAdvisor.jsx`, `src/components/TransactionIntelligencePanel.jsx`, `src/pages/{CryptoDetailPage,SecurityDashboard,SuspiciousAssets}.jsx` — **SAFE**
**Commit:** `46c5faf0` Transaction intelligence and suspicious asset review (#1929)
**Change summary:** Adds local presentation and explanatory surfaces for transaction/suspicious-asset intelligence.
**Assessment:** The inspected copy distinguishes local signals, unknowns, and opt-in remote intelligence; it does not itself authorise signing or claim external verification.

### `src/App.jsx` / `src/pages/Settings.jsx` / `src/lib/navigation.js` — **SAFE**
**Commit:** `2ef3b12d`, `aa13fed1`, `46c5faf0`
**Change summary:** Wires new routes/providers and settings navigation.
**Assessment:** No direct key handling, signing, or backend mutation was introduced by these route-level changes.

## Non-security changes (summary)

Documentation, localisation, reviewer configuration, UI layout, and associated source-pin/unit tests were also changed. These were reviewed for status-language drift; no unsupported verified/independent claim was found in the inspected changes.

## Post-scan re-check

Verified against `origin/main` at `0ae71e44b0c802726173974cae036d7401a68203` (2026-08-21T15:16:11Z). Nothing landed between window close and this check.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, device verification, or on-chain confirmation.*
