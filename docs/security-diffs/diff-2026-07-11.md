# Security Diff — 2026-07-11

Commits scanned: 4 in last 24h
Security-sensitive files changed: 0 (none matching the flagged path patterns)

No commit in the last 24h touched any file on the flagged security-sensitive
path list (`src/wallet-core/keystore/**`, `vault.js`, `src/rasp/**`,
`src/sign-gate/**`, `WalletConnect*`, `WalletProvider.jsx`, `copySecret.js`,
`biometricUnlock.js`, `pinAttemptGuard.js`, `twoFactorGate.js`,
`HardwareKekPlugin.kt/.swift`, `CryptoSigning.jsx`, `ColdSign.jsx`,
`evm/walletconnect/**`). Two security-*adjacent* changes are assessed below
because they carry explicit invariant intent (I4 honesty) or touch the stealth
pool, even though neither is on the flagged list.

## Flagged changes

### src/lib/balanceDisplay.js (new) + src/pages/SendCrypto.jsx:393 — SAFE
**Commit:** f527c3d6 fix(ui): show "—"/partial for failed balance reads, never a fabricated 0 (I4) (#778)
**Change summary:** New pure helper module centralises the "—" (indeterminate)
vs "0" (genuine empty wallet) convention for balance reads; `SendCrypto.jsx`
suppresses the "≈ $X" companion when a live on-chain read is null/pending
(`balanceIndeterminate`) instead of asserting "· $0.00".
**Assessment:** Strengthens I4 (fail honest, fail closed) — a failed/pending
read is now rendered as indeterminate rather than a fabricated confident 0.
No key material, signing, RASP, or auth touched. Explicitly session-agnostic
(no isDecoy/isHidden branch), preserving decoy/real render uniformity (I3-safe).
Pure display logic; adds a control, removes none. No unevidenced "verified"
claim.

### src/wallet-core/__tests__/stealth.test.js:111 — SAFE
**Commit:** e675571d docs: record Ethereum mainnet native-ETH send as provenance-pending evidence (#779)
**Change summary:** Test-only change: the "reveals only the matching wallet"
spec now picks a second secret whose HKDF slot doesn't collide with the first
under the random per-run device salt (pool=256, ~1/256 collision risk),
de-flaking the assertion.
**Assessment:** Test hardening, not a production security-control change. The
stealth reveal logic (`slotForSecret`, `createHiddenWallet`, `tryRevealHidden`)
is unchanged; the edit only avoids a probabilistic slot collision in the test
fixture. No invariant impact. Companion doc change (`Feature-Status.md`) records
a mainnet ETH send as provenance-pending evidence — consistent with the
verify-don't-assert rule (not flipped to "verified").

## Non-security changes (summary)
`.claude/settings.json` (#781: adds `mcp__ruflo__*` to permission allowlist — config only), `AGENTS.md` (#780: new AI-agent rules + CI workflow), `docs/Feature-Status.md` (#779: provenance-pending evidence note), and #778 UI files (`IncompleteBalanceNote.jsx` + test, `AdvancedAnalytics.jsx`, `Analytics.jsx`, `PortfolioRewind.jsx`, `PortfolioRiskScore.jsx`, `WalletPortfolioPage.jsx`, `balanceDisplay.test.js`) — all UI/docs/tests/config, no flagged-path source.

---
*Automated daily scan. Static analysis only — no dynamic testing. Internal analysis, not an independent audit.*
