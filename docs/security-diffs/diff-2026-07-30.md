# Security Diff — 2026-07-30

Commits scanned: 32 in last 24h (`4cb3f996`→`6a098eb8`)
Security-sensitive files changed: 28
Window: this is predominantly an i18n / globalization sprint (Phases 1–7), plus
a new SQL analytics surface, dependency bumps, and CI perf-rig scaffolding.

## Flagged changes

### sql/cohort-retention.sql — SAFE
**Commit:** `d7fc22ac` feat(sql,analytics): cohort retention + funnel drop-off views with hourly pg_cron alerts (#1475)
**Change summary:** New file. Adds 4 read-only views over the existing `events` table, a `funnel_dropoff_alert_log` table, a `check_funnel_dropoff_alerts()` SECURITY DEFINER RPC, and an hourly pg_cron schedule.
**Assessment:** Well-locked-down. Views use `security_invoker = true` so the caller's own grants decide access — `anon` and `authenticated` are explicitly REVOKEd; only `service_role` gets SELECT. The alert table has RLS enabled + all access revoked from public/anon/authenticated. The SECURITY DEFINER function has `SET search_path = public, pg_temp` (matching the hardening pattern from PR #1334). The RPC is restricted to `service_role` only — `anon` cannot call it. No new INSERT surface reachable by the anon key. The cron job runs as the database owner (pg_cron's own role), which has the necessary grants. No parameterised-query violations (the function takes typed arguments, not interpolated strings). The `ON CONFLICT DO NOTHING` dedup on `(event_name, prev_event)` prevents alert spam.

### perf/supabase-rpcs.k6.js — SAFE
**Commit:** `81100953` perf(ci): scaffold automated load/perf rigs (#1463), `139441f1` perf(k6): seed a real referral-code pool (#1497), `b7dd111e` perf(ci): graduate throwaway smoke findings (#1491)
**Change summary:** New k6 load test file targeting the 4 anon-callable SECURITY DEFINER RPCs on a staging Supabase instance. Setup seeds a pool of real referral codes via `generate_referral_code`. `record_attribution` intentionally omitted (service_role-only since API hardening).
**Assessment:** Fail-closed production guard: rejects any URL matching PROD_HOSTS, and requires the URL to either match a staging pattern or be explicitly listed in `STAGING_HOSTS_ALLOW`. The anon key is used (correct — tests the real client surface). No secrets hardcoded; all credentials come from env vars. Event names sourced from the canonical analytics file. The `record_attribution` omission is correctly documented as a service_role scope gap. No risk to production data.

### .github/workflows/perf.yml — SAFE
**Commit:** `81100953`, `b7dd111e`
**Change summary:** New workflow. Runs k6 against staging Supabase on manual dispatch or weekly schedule. Uses staging secrets (`STAGING_SUPABASE_URL`, `STAGING_SUPABASE_ANON_KEY`) + `STAGING_HOSTS_ALLOW` repo variable.
**Assessment:** `permissions: contents: read` — minimal. Secrets are staging-only, separated from production. The k6 script's own production guard provides defense-in-depth. No `push` or `pull_request` trigger — cannot be triggered by PR-authored code. Upload artifact is results only. No command injection vectors (no user-controlled inputs interpolated into shell).

### .github/workflows/perf-web.yml — SAFE
**Commit:** `81100953`, `c708b9e4`
**Change summary:** New workflow. Runs Lighthouse-CI against a local `serve -s dist` on PR paths + weekly schedule. Blanks `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` at build time so no analytics egress is possible (I3-adjacent).
**Assessment:** `permissions: contents: read`. LHCI hits `?demo=1` URLs — no backend writes. The env-blanking mirrors `vitest.config.js`'s production-Supabase fix (PR #1328). `--legacy-peer-deps` in the perf-only install step (#1484) is scoped to the LHCI+serve packages, not the main app — acceptable for dev tooling.

### src/lib/locale.js — SAFE
**Commit:** `ff78ac99` feat(i18n): Phase 1 globalization foundation (#1470), `0fc4144e` fix(security,i18n): locale-aware spend-limit inputs (#1490), `075d08e9` Batch B (#1481), `eea363dc` Batch C (#1487), `4cb3f996` Batch A (#1478)
**Change summary:** Major expansion from a small decimal-normaliser to the single source of truth for locale, timezone, fiat, RTL detection, and crypto/USD display formatting. Adds `parseLocaleNumber`, `formatCryptoAmount`, `formatUsd`, `isRtlLocale`, and preference persistence (`setLocale`, `setTimeZone`, `setFiatCurrency`) with an I3 write-chokepoint — all persistence functions no-op in deniability/demo sessions via `isDeniabilityOrDemoActive()`.
**Assessment:** The I3 guard is correct: imports `isDeniabilityOrDemoActive` from `deniabilitySession` (a true leaf with zero imports, so no cycle risk). All three `set*` functions and `clearLocalePreferences` call it before any `localStorage.setItem`. Reads remain ungated (correct — reading leaves no trace). The `LOCALE_CHANGED_EVENT` dispatch is a same-document CustomEvent (no network, no I2 violation). `normalizeDecimalInput` retains its existing safety pin: ambiguous inputs returned unchanged, so downstream validators still flag them. The new `parseLocaleNumber` correctly mirrors `isFormAmountWellFormed`'s regex and returns NaN for anything unrecognised (fail-closed on shape). `formatUsd` and `formatCryptoAmount` throw on non-finite input rather than rendering "NaN" — correct fail-closed. No security control added or removed.

### src/pages/SecurityCenter.jsx — SAFE
**Commit:** `0fc4144e` fix(security,i18n): locale-aware spend-limit inputs (#1490)
**Change summary:** Spend-limit inputs changed from `type="number"` to `type="text" inputMode="decimal"`. Parsing changed from `parseFloat` to `parseLocaleNumber` with explicit `isFinite`/positive validation.
**Assessment:** Security improvement. `type="number"` was silently blanking locale-formatted values (the browser's value-sanitisation step), so a de-DE user typing "1,5" would get an empty string → the limit was either skipped or saved as NaN. The new path canonicalises via `parseLocaleNumber` (which returns NaN for anything ambiguous) and explicitly validates `Number.isFinite(daily) || daily <= 0`. Error feedback added via `onError` toast. No relaxation of validation.

### src/pages/BudgetLimits.jsx — SAFE
**Commit:** `d80682e0` fix(ux,i18n): locale-aware numeric inputs (#1493)
**Change summary:** Same `type="number"` → `type="text" inputMode="decimal"` + `parseLocaleNumber` pattern as SecurityCenter.
**Assessment:** Same improvement. The existing `Number.isFinite(limit) || limit <= 0` guard catches NaN naturally. No relaxation.

### src/components/FeeSelector.jsx — SAFE
**Commit:** `61c6b2f5` fix(fee,i18n): locale-aware FeeSelector custom-gwei inputs (#1494)
**Change summary:** Custom gwei inputs (`maxBaseFeeGwei`, `priorityGwei`) changed from `type="number"` to `type="text" inputMode="decimal"`. Both the emit path and the preview path now canonicalise via `normalizeDecimalInput` before passing to `buildEvmCustomFee` → `parseUnits('gwei')`.
**Assessment:** Security improvement. `parseUnits` is ASCII-strict (correct for signing-side), so a non-canonical "1,5" would have thrown silently and made the custom fee invalid with no feedback. Now it canonicalises first. Critically, both the preview and the emit path use the SAME canonicalisation, so what the user sees matches what gets signed. `gasLimit` stays as-is (integer, handled by `Math.floor(Number(...))` downstream). No control removed.

### src/pages/SendCrypto.jsx — SAFE
**Commit:** `b8c5f323` (Phase 2 slice 3), `30d5e1fa` (Phase 2 slice 2 batch G)
**Change summary:** String extraction to `useTranslation('wallet')` / `useTranslation('security')`. All hardcoded English text replaced with `t(...)` / `tw(...)` calls. `text-left` → `text-start` for RTL support. Some `ml-*` → `ms-*`.
**Assessment:** Pure i18n extraction. No logic changes. The `PoisonWarning` component, `isFormAmountWellFormed` gate, ENS resolution flow, and all signing-path code are unchanged in logic. The poison-warning copy now comes from translation files (checked: the keys exist in `en/security.json`). The `role="alert"` / `role="status"` ARIA semantics on error messages are preserved. No security control added or removed.

### src/pages/DuressPin.jsx — SAFE
**Commit:** `d158a12d` (Phase 2 slice 2 batch F)
**Change summary:** Full i18n extraction. All user-facing strings → `t('security', ...)`. `ml-` → `ms-`, `pl-` → `ps-` for RTL. The `short()` helper gains a `dash` parameter (previously hardcoded `"—"`).
**Assessment:** Pure string extraction. The 2FA gate (`useActionGuard`), PIN length check (`pin.length < 8`), PIN confirmation match, biometric cache-clearing on removal, and demo-mode fencing are all untouched in logic. The coercion-resistant timing disclosure and fund-warning text are now translatable but structurally identical. No security control weakened.

### src/pages/StealthWallets.jsx — SAFE
**Commit:** `30d5e1fa` (Phase 2 slice 2 batch G)
**Change summary:** i18n extraction. User-facing strings → `t('security', ...)`. `ml-` → `ms-`, `pl-` → `ps-` for RTL. Same `short()` dash-param change as DuressPin.
**Assessment:** Pure string extraction. The `useActionGuard` gate on hiding/revealing wallets, the recovery-phrase confirmation, the transition-tell warning, and the reveal-secret flow are all structurally unchanged. No security logic modified.

### src/pages/PanicWipe.jsx — SAFE
**Commit:** `40ed2bcc` (Arabic RTL sweep)
**Change summary:** RTL property sweep only: `ml-auto` → `ms-auto`, `pl-4` → `ps-4`, `mr-2` → `me-2`, `mr-1` → `me-1`. No string extraction in this file.
**Assessment:** Pure CSS logical property changes for RTL layout. No security logic touched. The panic-wipe flow, `inspectKeyMaterial` reporting, and wipe-PIN setup/demo are structurally identical.

### src/pages/ColdSign.jsx — SAFE
**Commit:** `8a93652e` (RTL polish)
**Change summary:** `ArrowLeft` icon gains `rtl:-scale-x-100` so it mirrors in RTL locales.
**Assessment:** Visual-only. ColdSign is documented as unreachable dead code (no route/import). The icon flip is a standard RTL pattern. No security logic.

### src/components/WalletEntry.jsx — SAFE
**Commit:** `40ed2bcc` (Arabic RTL sweep), `8a93652e` (RTL polish)
**Change summary:** RTL layout changes: `-left-16` → `-start-16`, `-right-10` → `-end-10`, `text-left` → `text-start`, `mr-1.5` → `me-1.5`. All `ArrowLeft` icons gain `rtl:-scale-x-100`. No string extraction in this commit.
**Assessment:** Pure visual RTL support. The vault-exists check, unlock flow, PIN creation, seed reveal/confirmation, KEK enrollment gate, consent gate, and FirstRunTour wiring are all untouched. No security logic modified.

### src/components/TelemetryConsent.jsx — SAFE
**Commit:** `ff78ac99` (Phase 1 globalization foundation)
**Change summary:** String extraction to `useTranslation('security')`. `text-left` → `text-start`.
**Assessment:** Pure i18n. The `setConsent(true/false)` calls, the `FunnelEvent` and `trackEvent` calls, the focus-management a11y pattern, and the `onChoice` callback are all structurally identical. The consent mechanism itself is not changed — only the user-facing copy.

### src/components/walletconnect/* — SAFE
**Commit:** `30d5e1fa` (Phase 2 slice 2 batch G)
**Change summary:** i18n extraction across `ActiveSessions.jsx`, `RequestApprovalModal.jsx`, `SessionProposalModal.jsx`. All user-facing strings → `t('security', ...)`.
**Assessment:** Pure string extraction. The `presignGateOrReject` chokepoint, the `isSendReauthRequired` check, the dApp domain-check and scam-warning flow, the `eth_sign` block, the `wallet_addEthereumChain` block, the permit-acknowledgement checkbox, and the M11 expiry enforcement are all structurally unchanged. The `rejectRequest` and `signPersonal`/`signTypedData`/`sendTransaction` call sites are untouched. No signing or gate logic modified.

### src/components/security/* — SAFE
**Commit:** `4ee63be5` (Phase 2 slice 4), `40ed2bcc` (RTL sweep)
**Change summary:** i18n extraction across `BiometricUnlockSettings`, `HardwareKekSettings`, `PasskeyUnlockSettings`, `SessionSettings`, `TwoFactorSettings`. Error messages changed from hardcoded strings to `t(...)` calls. `pr-4` → `pe-4` for RTL. One small improvement: BiometricUnlockSettings stores a `detailKey` instead of a resolved string for the probe-failure message, so language-switching mid-mount re-renders correctly.
**Assessment:** Pure i18n. The biometric enable/disable flow, the KEK PIN-strength validation, the passkey enrollment, the session-settings controls, and the 2FA toggle are all structurally unchanged. The `detailKey` pattern is a minor improvement (UI re-renders in the new locale correctly). No security control weakened.

### src/App.jsx — SAFE
**Commit:** `40ed2bcc` (Arabic RTL sweep)
**Change summary:** Adds a `useEffect` that sets `document.documentElement.lang` and `dir` attributes based on `resolveLocale()` and `isRtlLocale()`. Listens for `LOCALE_CHANGED_EVENT` to update without reload.
**Assessment:** Standard i18n pattern. The DOM attribute changes are read-only from a security perspective (screen readers and Tailwind's `rtl:` variants use them). No new network calls, no new state writes, no security implications. The try/catch around the attribute-setting correctly falls through without modifying anything on error (fail-closed).

### src/pages/Subscription.jsx — SAFE
**Commit:** `ff78ac99` (Phase 1), `8a93652e` (RTL polish)
**Change summary:** (1) Free-tier price changed from hardcoded `"$0"` to locale-aware `Intl.NumberFormat(locale, { style: 'currency', currency: fiatCurrency }).format(0)` with `'Free'` fallback. (2) Monthly/annual price fallbacks changed from hardcoded `"$5.99/mo"` / `"$49.99/yr"` to `null` — unresolvable prices now render `"—"` instead of a wrong number. (3) RTL: `ml-*` → `ms-*`, `right-1` → `end-1`, `mr-2` → `me-2`. ArrowRight gains `rtl:-scale-x-100`.
**Assessment:** I4 improvement (fail-honest on pricing). Removing the hardcoded USD fallbacks means the paywall never advertises a price the store hasn't confirmed — if the package hasn't resolved, the user sees "—" and the upgrade button says "Upgrade — loading pricing" instead of a potentially wrong currency/amount. The `try/catch` around `Intl.NumberFormat` correctly falls back to `'Free'` on unrecognised locale. No purchase flow logic changed.

### package.json — SAFE (supply chain review)
**Change summary:** 5 dependency bumps + new i18n/font packages.
**Assessment:**
- `framer-motion 11.16.4 → 12.43.0`: Major version bump. Animation library — no security surface. Well-maintained, widely used.
- `@vitejs/plugin-react 5.2.0 → 6.0.4`: Major version bump. Build tooling — no runtime security surface.
- `zod 3.25.76 → 4.4.3`: Major version bump. Schema validation library. Zod v4 is a rewrite — any validation logic using Zod should be regression-tested (covered by existing test suite).
- `@testing-library/jest-dom 6.9.1 → 7.0.0`: Major version bump. Test-only dependency — no production impact.
- `@supabase/supabase-js 2.110.8 → 2.111.0`: Minor bump. Client library — standard maintenance.
- New: `i18next ^24.2.3`, `react-i18next ^15.7.4`: i18n framework. Well-audited, widely used. No security surface beyond the translation files themselves (which are static JSON).
- New: 10 `@fontsource-variable/noto-sans-*` packages: Font bundles. No code surface.
- Other minor/patch bumps in npm-minor-patch group: `@types/node`, `globals`, `appium-uiautomator2-driver`, `baseline-browser-mapping`. Dev-only.

No concerns. All are well-maintained packages with expected version progressions.

## Non-security changes (summary)
i18n locale JSON files (44 locales × 3 namespaces), `src/i18n/index.js` catalog registration, `perf/lighthouserc.json` config, `perf/README.md`, `docs/retros/2026-07-29-perf-rig.md`, `src/lib/useLocale.js` (React hook for locale preferences), `src/components/LanguageSwitcher.jsx`, `src/components/FiatCurrencySelector.jsx`, `src/lib/chartPeriods.js`, `src/lib/pdfExport.js`, `src/lib/snapshotStore.js`, `index.html` (RTL `dir` attribute), `tailwind.config.js` (font registration), `src/index.css` (font imports), various page-level i18n string extractions and RTL `text-left` → `text-start` / `ml-` → `ms-` / `pl-` → `ps-` / `mr-` → `me-` / `right-` → `end-` / `left-` → `start-` logical property conversions across ~60 component and page files, plus RTL icon mirroring (`rtl:-scale-x-100` on directional `ArrowLeft`/`ArrowRight` icons). All structurally identical in logic; only presentation and text sourcing changed.

## Post-scan re-check

Verified against `origin/main` at `6a098eb8` (2026-07-30 ~07:01 UTC). No additional commits in window.

---
*Automated daily scan. Static analysis only — no dynamic testing, no device verification, no on-chain confirmation. Not the outstanding independent third-party audit.*
