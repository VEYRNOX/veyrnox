# Security Diff — 2026-07-31

Commits scanned: 1 in last 24h (`e6b65ee3`->`75ed74e6`)
Security-sensitive files changed: 0
Scan window closes at `origin/main` = `75ed74e6` (fetched 2026-07-31 08:03 UTC)

## Summary

A genuinely quiet window. The only commit on `main` in the last 24 hours is
`75ed74e6`, the merge of yesterday's own run-2 security report — one file,
`docs/security-diffs/diff-2026-07-30.md`, +154 lines, 0 deletions, verified via
`git show --stat`. No source, SQL, workflow, native, or test file changed.

Two things were still worth doing, and both are below: confirming that the
report commit did what its subject line says, and re-verifying the open finding
this log is carrying forward.

## Flagged changes

### docs/security-diffs/diff-2026-07-30.md — SAFE
**Commit:** `75ed74e6` docs(security-diff): daily scan 2026-07-30 (run 2) (#1512)
**Change summary:** Appends a run-2 section to the existing 2026-07-30 report.

**Assessment:** Docs-only, and correctly APPENDED rather than overwritten —
worth confirming rather than assuming, because run 2's branch was cut from
`origin/main` before run 1 (`e6b65ee3`, 06:47 UTC) had merged, which is exactly
the shape that silently destroys the earlier text. It did not: the file went
100 → 224 lines with 154 insertions and **0 deletions**, so run 1's content
survives intact underneath. The run-2 section also opens with an explicit scope
note reconciling the two overlapping windows. No source file touched, no
control added or removed.

## Carried-forward open item

### src/i18n/index.js:81-114 `MACHINE_TRANSLATED` — NEEDS-REVIEW (still open)
**Origin:** reported in `diff-2026-07-30.md` (run 2), commit `d4eaf593`
feat(i18n): drop MT-pending banner for es, es-419, pt-BR, fr, it (#1507)

**Re-verified today against `origin/main` at `75ed74e6`, not assumed.** Read
out of the ref (`git show origin/main:src/i18n/index.js`):

```
  es: false,       // security.json reviewer-approved 2026-07-29
  'pt-BR': false,  // security.json reviewer-approved 2026-07-29
  fr: false,       // security.json reviewer-approved 2026-07-29
  it: false,       // security.json reviewer-approved 2026-07-29
  'es-419': false, // security.json reviewer-approved 2026-07-29
```

All five flags are still `false`, so the finding stands unchanged: the flag is
keyed by LOCALE and gates the whole "machine translated, not reviewed by a
native speaker" banner, while the review that justified clearing it covered
only the `security` namespace (~249 of ~860 strings per locale). `common.json`
and `wallet.json` — the latter carrying the biometric backup-exposure risk
acknowledgement and the reset/wipe confirmation copy — remain unreviewed MT and
now carry no disclaimer.

**One detail worth adding to the original finding.** The inline comments
introduced by `d4eaf593` say `security.json reviewer-approved` on every one of
the five lines. So the source file itself now documents the namespace scope of
the approval, sitting directly on the locale-wide flag that erases a
locale-wide claim. That strengthens the run-2 reading rather than undercutting
it — the scope mismatch is visible in the code, not inferred from the commit
message — and it reinforces that this is a flag-granularity oversight rather
than an attempt to overstate the review.

Still an **I4 honesty** item, not an exploitable defect: no gate is bypassed
and nothing fails open. Remediation options are unchanged from the run-2
report (per-namespace flag / extend the review / revert the five flags).

No change made — this task is read-only.

## Process note — the pattern list is still lagging, third run running

Step 2's pattern list has now missed the source of a real finding three runs in
a row (07-27 the SQL/edge surface, 07-28 `src/components/FirstRunTour.jsx`,
07-30 run 2 the i18n surface). Neither `src/i18n/**` nor
`src/components/LanguageSwitcher.jsx` matches any pattern, yet between them
they own the machine-translation disclosure and the rendering of every security
warning string in 44 locales.

Pattern to add, under a new **User-facing security disclosure (I4)** group:

```
- src/i18n/**
- src/components/LanguageSwitcher.jsx
```

Rationale worth keeping with it: a translation catalog is not cosmetic copy
here. A locale file is where the text of a risk acknowledgement lives, and a
flag in `src/i18n/index.js` decides whether the app admits that text is
unreviewed. Both are I4 surface.

As with the two previous runs, **this has NOT been applied** — the task file
permits writing only its own report under `docs/security-diffs/`. It needs an
owner edit to the task's SKILL.md.

## Non-security changes (summary)

None. The single commit in the window is the docs file assessed above.

## Post-scan re-check

Verified against `origin/main` at `75ed74e6` (2026-07-31 08:03 UTC, via
`git fetch origin main`). `git log 75ed74e6..origin/main` returned empty —
nothing landed between window close and this check. The `MACHINE_TRANSLATED`
item above is OPEN as of that tip.

---
*Automated daily scan. Static analysis only — no dynamic testing, no device
verification, no on-chain confirmation. INTERNAL. Not the outstanding
independent third-party audit.*
