# Security Diff — 2026-08-25

Commits scanned: 40 in the preceding 24 hours (`b338b47f` … `44b6d70e`)

Files changed: 144 (+12,511 / −818)

Security-sensitive implementation and direct security-regression-test files changed: 79

Scan range: `b338b47ffb0ffd039617a8af4619661b423c64ee^` →
`44b6d70eb76f49b9ec542579fb6fe311f2171b80` on `origin/main`.

**Window shape.** This is a same-day internal-audit remediation wave, centred on
fast-path biometric unlock, deniability, WalletConnect, RASP and PIN handling.
The audit record itself says these are BUILT / unit-tested only; this static scan
does not promote any control to device-verified, on-chain verified, or independently
audited.

No new I1–I5 regression was found in the scanned diff. Two I4 honesty/reporting
items remain open below.

## Flagged changes

### `src/wallet-core/keystore/native.js`, `src/lib/WalletProvider.jsx`, `src/lib/duressBiometricGuard.js` — SAFE

**Commits:** `dcf67599` (#2071), `b4539599` (#2057), `12f5d404` (#2051)

**Change summary:** The Android fast-path DEK cache is now both prevented from
warming when a duress PIN is configured and refused at read time if that marker
is present. Duress setup clears the cache as a best-effort cleanup, while the two
keystore gates remain load-bearing if that clear cannot land. PIN rotation,
KEK enrolment/unenrolment, vault clear, and other DEK-rotation paths clear the
cache too.

**Assessment:** This closes the 2026-08-25 audit H-1 route where a default-on,
biometric-only button could release the primary vault beside the Emergency-PIN
path. The read-side failure is deliberately an ordinary cache miss, so it does
not reveal that duress is configured. The design still intentionally uses an
H-only wrapped-DEK cache after the mandatory disclosure; that owner-accepted
trade-off is not a claim that the primary I6 wrap has changed.

### `src/wallet-core/keystore/fastpathDekCache.js:13-30`, `android/app/src/main/java/com/veyrnox/app/AndroidBiometricCacheConfig.kt:89`, `src/components/security/FastpathToggle.jsx:4-7` — NEEDS-REVIEW

**Commits:** `f4436353` (#2047), `12f5d404` (#2051), `592f5e08` (#2055)

**Change summary:** The fast-path primitive, Android alias configuration, and
Settings component still say the feature has no production callers and is
TARGET / opt-in / off by default. In the same scanned range it was wired through
`native.js` and changed to default-on behind a mandatory disclosure card.

**Assessment:** The executable guards use the newer disclosure-gated default-on
model, so this is not evidence of an I1 bypass. It is nevertheless an I4
documentation-honesty defect in security-adjacent source: a reviewer reading
the primitive or ACL configuration is told the feature is inert and opt-in when
it is a live Android unlock path. Update these comments to state BUILT,
default-on-after-disclosure, Android-only, and internal/unit-tested status; do
not call it device-verified without real-device evidence.

### `src/wallet-core/deniabilityKdfProfile.js`, `src/wallet-core/{stealth,duress,panic,deniabilityUnlock}.js` — SAFE

**Commit:** `3bba571e` (#2069)

**Change summary:** New deniability writes obtain the device's existing KDF era
from the deniability footprint rather than stamping the current global default.
The pool, duress and panic writers use that profile, preserving a common visible
`kdf` object across real and chaff slots after the 96 MiB / t=6 profile change.

**Assessment:** This closes the future-write form of audit H-2 without adding a
second localStorage source of truth. The code and audit record honestly retain
the installed-base limit: a device that already wrote a mixed v1/v2 footprint is
not healed by this change. That residual remains BUILT / internal static analysis
only, not verified.

### `src/risk/{walletConnectIntel,composeVerdict}.js`, `src/lib/WalletConnectProvider.jsx`, `src/wallet-core/evm/{typed-data.js,walletconnect/router.js}` — SAFE

**Commits:** `b4971439` (#2067), `0c2ad974` (#2068), `7fae74cd` (#2063)

**Change summary:** WalletConnect now includes S9/TIP in the scored signal set,
uses maximum composition across settled local, TIP and runtime verdict planes,
parses ERC-20 transfer value for spend-limit checks, binds the transaction chain
before the approval modal, rejects `eth_signTransaction`, and rejects EIP-712
payloads whose declared `primaryType` differs from the type-graph root.

**Assessment:** The pre-sign path receives the stricter computed level before
`withPrivateKey`, restoring the previously discarded TIP sanctions/block verdict
and preventing a dApp-controlled label from suppressing Permit warnings. A TIP
transport/error now produces the already fail-closed CAUTION path on WalletConnect;
that availability change is explicitly recorded in the audit and is not a
runtime verification result.

### `src/lib/getFreshLocalRaspArtifact.js`, `src/components/{WalletEntry,SeedGrid}.jsx`, `src/components/security/useRevealWithReauth.jsx`, `src/components/backup/RestoreFromFile.jsx`, `src/pages/PersonalBackup.jsx`, `src/rasp/*` — SAFE

**Commits:** `c54e6507` (#2065), `5f1be8df` (#2072), `9ddfc0bc` (#2070), `44b6d70e` (#2075)

**Change summary:** Seed reveal, export and import now obtain a fresh local
RASP artifact at confirmation rather than relying only on the mount-time sample.
The shared helper uses a bounded native-probe race and degrades timeouts, throws
and malformed artifacts fail-closed. The latest refactor removes the duplicate
inline implementation rather than widening the gate.

**Assessment:** The helper intentionally excludes remote attestation for local
seed-material operations, preserving the stated owner decision that a sideloaded
or offline self-custody recovery flow must not be permanently blocked by a remote
attestation endpoint. It still rejects unavailable local integrity evidence for
seed-sensitive actions. The iOS screen-capture condition now blocks seed reveal,
and the emulator tier carries the complete sensitive-action set, restoring the
expected danger monotonicity.

### `src/lib/{pinAttemptGuard,copySecret}.js`, `src/components/WalletEntry.jsx` — NEEDS-REVIEW

**Commits:** `60ad6f92` (#2061), `9ddfc0bc` (#2070)

**Change summary:** PIN backoff is now enforced before an attempt is spent and
the in-memory high-water floor makes an unwritable localStorage counter honest
within the current session. Clipboard wiping adds blur/focus retry behaviour and
dispatches `veyrnox:clipboard-wipe-exhausted` after eight failed overwrite
attempts.

**Assessment:** The backoff wiring is a real improvement, and the floor is
correctly documented as session-scoped rather than durable. However, the only
producer of `veyrnox:clipboard-wipe-exhausted` is `copySecret.js`; there is no
listener on `origin/main`. After all retries fail, the secret can remain on the
clipboard and the promised fail-honest signal is local but not user-visible.
This is the still-open half of audit M-8, not a claim that the wipe succeeded.

### `src/lib/biometricUnlock.js`, `android/app/src/main/java/com/veyrnox/app/AndroidBiometricCachePlugin.kt` — SAFE (static scope only)

**Commits:** `1be28d05` / `8dce8e76` (#2037/#2039), `f4436353` (#2047)

**Change summary:** Android separates the KEK-only cached C-factor alias from
the ordinary biometric-gated password alias, and the fast-path alias is
biometric-strong plus enrollment-invalidated. A cache read precedes the hardware
factor prompt; an expired 30-second device-wide authentication window becomes a
silent cache miss and falls back to typed PIN.

**Assessment:** The code does not disclose a DEK from this cache without the
subsequent hardware factor H, and it handles misses fail-closed. Its real-world
hit rate and prompt behaviour are explicitly unmeasured/device-gated in the
design material. This scan therefore records no functional or device verification
claim for Fast Unlock.

## Non-security changes (summary)

Documentation/catalogue consolidation, onboarding progress presentation, token-list
24-hour price display, Android version-code bump, and related tests changed alongside
the audited surfaces. `.env.production` adds the public native edge base URL; it is
an endpoint, not a secret, and the change does not add a new client credential.

## Post-scan re-check

Verified against `origin/main` at `a9f86d94711e3842e7d0462aaf3e78988b6162e5`
(2026-08-25T09:05:57Z). `a9f86d94` (#2086) landed after the scan window and
touches iOS KEK rejection mapping, WalletConnect low findings, and the RASP
dashboard/plugin. Its diff does not resolve either open item above, so no dated
amendment is required at checkpoint 1.

---
*Automated daily scan. Internal static analysis only — no dynamic testing, device
verification, on-chain confirmation, or independent audit.*
