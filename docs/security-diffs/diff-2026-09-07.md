# Security Diff — 2026-09-07

Commits scanned: 50 in last 24h
Security-sensitive files changed: 72

## ⚠️ REGRESSION DETECTED

- `sql/bug-report-upload.sql` remains in the repository with an unrestricted
  `FOR ALL` policy on `storage.objects`. The disabled recording client and its
  Pages endpoint were removed in this window, but that does not revoke a policy
  already applied to a Supabase project.

## Flagged changes

### sql/bug-report-upload.sql:178 — REGRESSION (carried)

**Origin:** `c1cf8203` feat(bug-report): slice 1e-2 — Supabase schema for upload path (#2334), reported 2026-09-06

**Change summary:** This file was not changed in the present window. It still
creates `bug_reports_service_role_all` as `FOR ALL` with only a bucket predicate
and no `TO service_role` clause.

**Assessment:** PostgreSQL policies without `TO <role>` apply to `PUBLIC`.
Thus, if this SQL was applied, anon and authenticated roles can access matching
objects in the supposedly private `bug-reports` bucket, subject to Storage table
grants. Removing the client route and service-role upload endpoint does not
remove that direct Storage policy. Remove the policy (service role bypasses RLS
anyway) or restrict it explicitly, and check the deployed project's effective
Storage policies and grants. Static analysis only; deployment was not checked.

### functions/api/bug-report/upload.js:1 — SAFE (partial resolution of prior report)

**Commit:** `826bd1c8` chore: remove disabled bug-report screen recording feature

**Change summary:** The unreachable recording surface was removed from Android,
iOS, Settings, the client encryption/upload modules, the RPC allowlist, and the
Pages upload endpoint. Android retains its window-wide `FLAG_SECURE` and RASP
screen-capture detection; the manifest also keeps the no-media-projection pin.

**Assessment:** This removes the formerly reachable service-role Storage writer
and its missing-reservation check. It does not resolve the carried SQL-policy
finding above, because the raw migration remains capable of having altered a
deployed database. No native/device test or deployment check was run here.

### src/pages/Settings.jsx:144 — SAFE

**Commits:** `86bb11fb` fix(i3): gate the Wallet Passkeys block in Settings on deniability (#2380); `e976ae17` fix(legal): back the §9 consent promise with a real control, pin the claims (#2381)

**Change summary:** Deniable sessions now disable and blank the shared Wallet
entity query and reject passkey mutation before it can write. Settings also adds
an Advisor privacy control: OFF records denial; ON clears the old answer so the
full in-Advisor disclosure must be shown before a new grant.

**Assessment:** The two-chokepoint passkey guard prevents real wallet names,
counts, and credential IDs being read or written through a decoy/hidden
session. Advisor consent remains separately I3-gated and remote chat still
checks consent at the egress boundary. New tests are source-level regression
pins, not runtime or deployment evidence.

### src/components/BackupPaywallNudge.jsx:36 and src/components/ReferralPrompt.jsx:28 — SAFE

**Commit:** `05bc6979` fix(i3): gate dismiss writes in BackupPaywallNudge and ReferralPrompt (#2375)

**Change summary:** Both already-mounted interaction handlers now re-check the
deniability/demo state before persisting their shared dismissal markers.

**Assessment:** This closes the state-transition case where a prompt rendered
in a real session survives into a decoy session and a tap otherwise leaves
shared-storage residue. Existing network calls retain their downstream egress
gate.

### patches/@capacitor+android+8.5.1.patch and patches/@capacitor+ios+8.5.1.patch — SAFE

**Commits:** `60bcc77b` fix(android): carry bridge log redaction to Capacitor 8.5.1; `0626c3aa` fix(ios): carry bridge log redaction to Capacitor 8.5.1

**Change summary:** The Capacitor 8.5.1 patches preserve logger-only redaction
for `HardwareKek` and `SecureStorage` bridge calls and results.

**Assessment:** The patches retain the LOG-1 mitigation while leaving plugin
payloads intact for callers. This is static source review only; it does not
prove release-build log behavior on a device.

### functions/api/buy/session.js:174 and src/pages/BuyCrypto.jsx:96 — SAFE

**Commit:** `8727f06c` fix(buy): open Transak in system browser on native (T-INF-103) (#2408)

**Change summary:** Native builds hand the minted Transak session URL to the
system browser rather than the Capacitor WebView iframe. The existing
`createBuySession` client boundary still rejects URLs outside the HTTPS Transak
allowlist before this handoff.

**Assessment:** The change does not open an arbitrary-browser URL from the
edge response, and it keeps `createBuySession`'s I3 gate. The new test is a
source-level branch pin; no Transak session or native deep-link return was
exercised in this scan.

### src/lib/reviewPrompt.js:43 and src/lib/tracking-integration.jsx:157 — SAFE

**Commit:** `d6df1b8f` fix(reviews): manual Rate opens store page, first-inbound is a milestone (#2404)

**Change summary:** A first inbound can now increment the existing review
milestone counter and attempt the OS review prompt; manual rating opens the
store page directly.

**Assessment:** Both the counter and external-store actions are guarded by
`isDeniabilityOrDemoActive()`. The new user-triggered feature-request link is
also behind the same gate, so this review found no new decoy-session egress or
shared-state residue.

### .github/workflows/deploy-preview.yml, package-lock.json, and android/app/build.gradle — SAFE

**Commits:** dependency/action updates, Firebase Test Lab timeout change, and
mobile build-number increments in `2eb77af8` through `418aae8b`

**Change summary:** The preview deploy job remains split so PR-authored code is
not checked out in the token-bearing deploy job. Dependency updates retain
immutable action SHAs; the Capacitor redaction patches were moved to their new
8.5.1 package paths.

**Assessment:** No secret scope, release-signing gate, or production database
configuration was widened by these diffs. Dependency behavior was not
dynamically exercised by this scan.

## Test coverage changes

No changed `e2e/**`, `src/**/__tests__/**`, function, or Android test changed
its `skip`/`fixme`/`todo` count: retained tests were `0 → 0`, deleted
bug-report tests were `0 → absent`, and new tests were `absent → 0`.
The new passkey, Advisor-consent, native-buy, and legal tests are useful
regression pins, but they are not device, deployed-edge, or deployed-Supabase
authorization tests.

## Non-security changes (summary)

Store build-number republishing, Firebase Test Lab timeout tuning, navigation
prefetch, dependency-audit records, and app/store review UX changed alongside
the reviewed controls. Security-status documentation was read; this report
makes no new verification claim.

## Post-scan re-check

Verified against `origin/main` at `8727f06cba68a320608cd94c5f5e3416287e0723`
(`2026-09-07T09:06:24Z`). Nothing landed between the scan-window close and
this checkpoint.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, device verification, deployment verification, or on-chain confirmation.*
