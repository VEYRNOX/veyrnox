# Security Diff — 2026-08-27

Commits scanned: 8 in last 24h
Security-sensitive files changed: 17

## ⚠️ REGRESSION DETECTED

- `src/lib/sentry.js` — Sentry’s I2/I3 guards are evaluated only when the SDK is initialised. A primary session with stored telemetry consent can initialise Sentry before React renders; if that same app instance later unlocks a duress/hidden wallet, global error handlers and `ErrorBoundary` reporting remain active. `beforeSend` scrubs the event but does not re-check `isDeniabilityOrDemoActive()` or consent, so an error in that deniability session can still egress to the configured DSN. This violates I3’s zero-egress requirement (and continues reporting after later consent withdrawal). Internal static analysis only.

## Flagged changes

### src/lib/sentry.js:41 — REGRESSION
**Commit:** `4e59335d` feat(sentry): consent-gated JS crash reporting (#2118)

**Change summary:** Adds Sentry global error handlers behind DSN, consent, and deniability checks, plus a `beforeSend` scrubber; `src/main.jsx` runs this once before React renders and `ErrorBoundary` invokes `reportError()` once initialised.

**Assessment:** The registration-time checks are not a live egress chokepoint. `scrub()` removes request, user, breadcrumbs, and stack-frame context but never returns `null` for a now-active deniability/demo session or revoked consent. Because `initialised` remains true, both global handlers and `reportError()` can transmit after the session changes. The regression test suite covers only initialisation in an already-deniable session, not the real-primary → decoy transition or consent revocation.

### src/wallet-core/deniabilityKdfProfile.js:157 — SAFE
**Commit:** `1d69be2c` fix(deniability): hold the KDF era at the pool's, not the current default (#2116)

**Change summary:** Restores per-device deniability KDF-era writes, probes the stealth pool before mutable `secondary`/`tertiary` slots, and repairs a successful revealed/duress slot toward the surrounding pool’s era rather than the current default.

**Assessment:** Static review finds the three deniability writers (`stealth`, `duress`, and `panic`) routed through the same era resolver. The added parity tests exercise v1-pool writes, pool-majority selection, and repair of the prior mixed-era state. This removes the static KDF-object distinguisher identified in the prior scan; no device or independent-audit claim is implied.

### vite.config.js:186 — SAFE
**Commit:** `f1cb7303` fix(csp): stop Vite inlining fonts, and pin the policy to the build config (#2115) (#2117)

**Change summary:** Configures Vite not to inline `.woff`/`.woff2` assets and adds static tests tying that behavior to the existing `font-src 'self'` CSP in both delivered policies.

**Assessment:** This keeps the build output within the existing restrictive CSP rather than widening `font-src` to permit `data:`. The paired tests cover the config key, `.woff2` handling, and header/meta policy agreement. No relaxation of the CSP or egress allowlist appears in this diff.

### .github/workflows/ios-xcuitest-smoke.yml:105 — SAFE
**Commit:** `bc155342` ci(ios): split the xcuitest build from the test — the compile was never the cost (#2110)

**Change summary:** Splits iOS build-for-testing from test-without-building, keeps independent step timeouts, and preserves upload of both build and test logs.

**Assessment:** Static review finds no new credential exposure, unpinned action, untrusted-code execution, or removed required security check. The change improves failure evidence without changing the app’s signing, key, or runtime security paths.

## Non-security changes (summary)

Documentation/status corrections, iOS UI-test selector updates, English consent copy, and Firebase onboarding-test maintenance; package manifests add the Sentry SDK reviewed above.

## Post-scan re-check

Verified against `origin/main` at `4e59335dc1682b1c30cb96360052dcb281e84b63` (2026-08-27T09:04:46Z). Nothing landed between the scan window close and this check.

---
*Automated daily scan. Internal static analysis only — no dynamic testing, device verification, on-chain confirmation, or independent audit.*
