# Security Diff — 2026-09-05

Commits scanned: 9 in last 24h
Security-sensitive files changed: 7

## Flagged changes

### `.github/workflows/firebase-test-lab.yml:205-240,400-459` — SAFE
**Commits:** `8b705034` fix(ci): fall back to Firebase default bucket when GCS_RESULTS_BUCKET unset (#2314); `280a8ee6` fix(ci): Firebase Test Lab APK filename matches Google flavor upload (#1960) (#2315); `be3fc980` fix(ci): lock iOS SPM to checked-in Package.resolved (#1960) (#2316); `781d3d92` test(testlab): expand Robo device matrix + push past consent into wallet UI (#2318)

**Change summary:** The Android test run now uses the exact uploaded Google-flavour APK, keeps a custom results bucket optional, and exercises three devices. The iOS smoke build preserves the checked-in SwiftPM lock and disables automatic resolution in both hydration and build commands.

**Assessment:** SAFE. The timeout remains allowlisted, the CI-artifact lookup remains SHA and event scoped, and `--no-resign` remains in place. The optional-bucket branch does not print test artifacts or their contents; its diagnostics remain limited to matrix metadata, while bucket inventory runs only when a configured custom bucket exists. The iOS change narrows dependency resolution rather than widening signing or secret access. No workflow trigger, permissions, egress destination, signing identity, or release-publication condition was broadened.

### `.github/testlab/android-pin-onboarding-robo-script.json:137-160` and `src/__tests__/firebase-test-lab-onboarding.test.js:104-128` — SAFE (control strengthened)
**Commit:** `781d3d92` test(testlab): expand Robo device matrix + push past consent into wallet UI (#2318)

**Change summary:** The deterministic Robo script now declines telemetry and asserts that the wallet UI reaches the Send action. Its source-level regression pin checks both additions and updates the expected Android artifact filename.

**Assessment:** SAFE. This adds coverage for the I3 decline path and for routing beyond consent; it does not add an analytics call, permit consent by default, or weaken an assertion. The changed test has no `skip`, `fixme`, `todo`, or `only` marker before or after this range. These are static regression pins and an external device-run configuration, not dynamic proof from this scan.

### `docs/firebase-test-lab-setup.md:1-129` and `CLAUDE.md:379-389` — SAFE (documentation/status record)
**Commit:** `b630782f` docs: sync #1960 state after Firebase Test Lab rebuild (#2317)

**Change summary:** The new owner-only setup guide records the Firebase project, least-privilege bucket role, artifact-redaction rule, and the still-blocked iOS signing path. `CLAUDE.md` records the accepted residual for the absent Play Pre-launch report and references one Firebase matrix as internal evidence.

**Assessment:** SAFE as a documentation-only change. It does not claim an asset or feature is catalogue-verified, and it explicitly preserves the iOS blocker and the Play Pre-launch residual. The reported Firebase matrix evidence is not re-executed or independently verified by this static scan.

### `docs/Feature-Status.md:663-675` — SAFE (honest deferral)
**Commit:** `18ee82d1` docs(feature-status): park PostHog forwarding suggestion (#2319)

**Change summary:** The document parks a proposed server-side PostHog forwarder and expressly states that no code has been built. It requires store clearance, privacy disclosures, and in-app/website privacy updates before any future work resumes.

**Assessment:** SAFE. No new telemetry sink, RPC allowlist entry, client SDK, CSP change, or consent-path modification landed. The proposal correctly identifies that consent alone would not authorize an undeclared third-party analytics destination.

### `docs/security-diffs/diff-2026-09-04.md:364-434` — SAFE (prior report correction/follow-up)
**Commits:** `ab634eb6` docs(security-diff): incremental scan 2026-09-04 (#2311); `8e430546` docs(security-diff): correct checkpoint label 2026-09-04 (#2312)

**Change summary:** The prior report records its incremental range and corrects the re-check label without changing production code.

**Assessment:** SAFE. It retains, rather than suppresses, the post-audit deferral-counting gap and the Advisor snapshot egress regression.

## Carried forward (not in this window)

- **NEEDS-REVIEW:** `scripts/run-post-audit-qa.mjs` / `scripts/lib/postAuditDeferrals.mjs` still treat a missing or malformed Playwright JSON report as zero runtime skips. A runtime-only conditional skip can therefore leave the post-audit gate looking complete. No file in this scan window changes that path.
- **REGRESSION:** `src/lib/useAdvisorSnapshot.js` and its publishers still allow page snapshots to be sent beside a persistent `device_id` through Security Advisor context, exceeding the consent copy's scope. No file in this scan window changes that path.

## Non-security changes (summary)

No unrelated files changed; all seven paths were reviewed because they are CI controls, security-test coverage, or documentation recording control status.

## Post-scan re-check

Verified against `origin/main` at `18ee82d1dbf41fcad23ffe09a63cadddfe1a4c4e` (2026-09-05T09:02:27Z). Nothing landed between the scan window close and this check. The two carried-forward items stand.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, no device verification, no on-chain confirmation. Not the outstanding independent third-party audit.*

---

# Security Diff — 2026-09-05 (incremental, second run)

The scan above closed its window at `18ee82d1` (verified 2026-09-05T09:02:27Z) and
merged as PR #2324 at 09:21 UTC. Twelve further commits landed the same day. This
entry covers **`18ee82d1..c2021239`** only — it does not re-rate anything above.

Commits scanned: 12 (`18ee82d1..c2021239`)
Files changed: 33
Security-sensitive files changed: 14

## ⚠️ REGRESSION DETECTED

`src/lib/reviewPrompt.js` introduces three localStorage keys that survive a panic
wipe, and `inspectKeyMaterial()` still reports `clean: true` with them present.
Detail below.

## Flagged changes

### `src/lib/reviewPrompt.js:17-19` — REGRESSION (panic-wipe residue)
**Commit:** `92580cf6` feat(reviews): native store-review prompt + Settings feedback path

**Change summary:** New store-review trigger module writing three keys —
`veyrnox-review-send-count`, `veyrnox-review-last-asked-ts`,
`veyrnox-review-declined`. `SendCrypto.jsx` calls `recordSuccessfulSend()` from
`SendDoneView`'s mount effect, so the counter increments on every completed send
in a real session today. The module is not behind a ship flag.

**Assessment:** REGRESSION. None of the three keys appears in `panic.js` —
verified against `origin/main:src/wallet-core/panic.js` with `grep -cF`, which
returns 0 for each. They are covered by neither the exact list nor
`RESIDUE_KEY_PREFIXES` (only `veyrnox-snapshots-` and `veyrnox-seed-verify-`).
`ALL_RESIDUE_KEYS` drives **both** the erase and `inspectKeyMaterial().clean`, so
the keys survive a wipe *and* the wipe still reports clean.

`veyrnox-review-send-count` is a stronger tell than most entries already on that
list: its presence asserts a real Veyrnox install existed on this device, and its
**value** states how many crypto sends completed from it. `veyrnox-review-declined`
and the ask-timestamp add install-age evidence.

This is the documented recurring class, not a novel one — the same finding was
filed for the first-run-tour markers (PR #1415, 2026-07-28) and for the paywall
and session-day markers (Codex P2, 2026-08-15), both of which are now in
`METADATA_RESIDUE_KEYS` with comments explaining exactly this reasoning.
`CLAUDE.md` states the rule: *what makes a key a tell is its PRESENCE.*

The I3 discipline in the writer is correct and is not the gap: `recordSuccessfulSend`,
`markDeclined`, `shouldPromptForReview`, `openStoreForRating` and `openFeedback`
all return early on `isDeniabilityOrDemoActive()`, so a decoy session cannot
advance or read the real user's state. The writer being I3-gated is precisely the
condition under which the residue list is still the authoritative sweep — the same
note already sits beside the paywall-nudge entries.

**Proposed fix (not applied — this task may only write this report):** add the
three keys to `METADATA_RESIDUE_KEYS` in `src/wallet-core/panic.js`, with a
regression test in the shape of `panic-residue-first-run-tour.test.js` that is red
before the fix because `localStorageResidue` is empty while the key is present.

### `src/lib/bugReport/recordableRoutes.js:26-52` — NEEDS-REVIEW (route vocabulary does not exist)
**Commit:** `c2021239` feat(bug-report): slice 1a — design doc, route allowlist, enable gate (#2326)

**Change summary:** Fail-closed route gate for opt-in bug-report screen recording:
7 allowlist prefixes, 16 denylist prefixes, segment-boundary matching, denylist
wins, unknown path denied.

**Assessment:** NEEDS-REVIEW. The matching *logic* is sound and genuinely
fail-closed. The *path vocabulary* does not correspond to this application's
router. Cross-checked every literal in both lists against the 92 `path=` values in
`origin/main:src/App.jsx`:

- Allowlist: only `/receive`, `/settings` and `/plans` are real routes.
  `/dashboard` (the route is `/`), `/send/form`, `/help` and `/documentation`
  match nothing.
- **Denylist: 16 of 16 entries match zero routes.** The real sensitive routes are
  `/duress-pin`, `/panic-wipe`, `/stealth-wallets`, `/wallet-seed-qr`,
  `/crypto-signing`, `/personal-backup`, `/onboarding/restore-shares` and
  `/walletconnect`.
- `/send/confirm` and `/send/sign` are not routes at all — confirm and sign are
  component state inside the single `/send` route.
- The deliberate segment-boundary rule makes this worse rather than better: a
  naive `startsWith` would have caught `/duress-pin` under `/duress` and
  `/panic-wipe` under `/panic`. Requiring `prefix + '/'` means they do not match.

Not exploitable today: `canRecordOnRoute` has **no runtime caller**
(`git grep -l` finds only the module, its test, and unrelated bugReport files), and
the allowlist misses are themselves fail-closed. The risk is latent and directional.
Slice 1b consumes this contract, and the obvious repair when someone notices
`/dashboard` and `/send/form` never fire is to add `/` and `/send` to the
allowlist — at which point the signing step becomes recordable and the denylist
stops nothing, because every entry in it is a phantom.

`/settings` is both allowlisted and real. Settings renders duress-PIN
configuration, KEK posture and telemetry consent, so under the design's own stated
principle it is a coercion-state surface; its only denylist guard,
`/settings/wipe`, is also a phantom.

**The test suite cannot catch this, and reads as though it does.**
`recordableRoutes.test.js` is thorough and mutation-checked on the algorithm, and
every path it asserts is invented — `/seed/reveal`, `/panic/erase`,
`/duress/setup`, `/wc/session-request`, `/send/confirm`. It is self-consistent
with the module and has no contact with `App.jsx`. This is the
`CLAUDE.md` "coverage that READS as present and is not" pattern at the vocabulary
level rather than the assertion level: reintroducing any defect the tests name
does turn one red, so the usual mutation check passes while the lists remain
inapplicable.

`docs/bug-report-recording-plan.md:77-92` restates the same phantom vocabulary as
settled design, with no note that the names are aspirational. Three artifacts —
doc, module, tests — agree with each other and none agrees with the router.

**Proposed fix (not applied):** re-derive both lists from `src/App.jsx`, and add a
pin that asserts every denylist literal matches at least one declared route, so
the lists cannot drift from the router again.

### `docs/Feature-Status.md:663-682` and `src/lib/bugReport/bugReportEnabled.js:10-11` — NEEDS-REVIEW (claim contradicted by its own commit)
**Commit:** `c2021239` (same commit as the code below it)

**Change summary:** The status entry records Slice 1a as
`TARGET / HONEST-DISABLED` and justifies merging it under the 1.0.1 submission
hold with: *"**Zero runtime effect** — no application code imports either module
yet."* `bugReportEnabled.js`'s header says the same: *"Nothing imports it yet at
runtime. Slice 1b will consume it from the Settings entry point."*

**Assessment:** NEEDS-REVIEW (honesty, not behaviour). The same commit ships
`src/components/bugReport/BugReportButton.jsx` — whose own header calls itself
"Slice 1b" — and wires it into `src/pages/Settings.jsx:534`. So application code
does import `bugReportEnabled.js`, and `isBugReportEnabled()` is evaluated on every
Settings render.

The behavioural conclusion still holds: `VITE_BUG_REPORT_ENABLED` is set nowhere
in the repo (`git grep -F` finds only docs, the gate itself and its tests), the
comparison is strict-equal `'1'`, and the component returns `null`, so nothing is
visible on any current build. But the *evidence offered* for "safe to merge under
the hold" is false, and it is the load-bearing half. A future reader who flips the
ship flag on the strength of "nothing is wired yet" gets a live Settings entry
whose handler is a placeholder `window.alert`.

`canRecordOnRoute` genuinely has no caller; that half of the claim is accurate.

### `src/components/WalletGate.jsx:20-28` — SAFE (with an untested half)
**Commit:** `bfb3b5e6` feat(paywall): dev-only wallet-gate bypass + Free full feature list (#2329)

**Change summary:** A design-review bypass returning `<Outlet />` ahead of the
wallet gate, requiring `import.meta.env.DEV` **and**
`VITE_DEV_BYPASS_WALLET_GATE === '1'`.

**Assessment:** SAFE. Both conditions are required, the comparison is strict-equal
`'1'`, and the flag is set nowhere in the repo or in any workflow. `DEV` is false
for every `vite build` regardless of mode, so the whole branch
dead-code-eliminates from production and staging bundles. `DEV_BYPASS_WALLET_GATE`
is a module-level constant, so the early return does not create a conditional-hook
hazard.

Two notes, neither raising the rating:

- `WalletGate.devBypass.test.jsx` correctly uses `vi.resetModules()` + dynamic
  import, so the module constant re-evaluates and the three flag cases are real
  pins. But all three run with `DEV` true — the file's own header claims *"Either
  alone must NOT open the gate"* and only the flag half is exercised. The DEV half
  rests on Vite's build-time substitution, which is a stronger guarantee than a
  unit test, but the comment describes coverage the file does not have.
  `vi.stubEnv('DEV', false)` would close it.
- `ios/fastlane/Fastfile`'s new `verify_ipa` lane greps a shipped IPA for
  `VITE_(BYPASS_RASP|DEV_UNGATE_SEND|DEMO_MODE):"1"`. `VITE_DEV_BYPASS_WALLET_GATE`
  landed the same day and is not in that list. See the fastlane entry below.

### `ios/fastlane/Fastfile:52-59` (`verify_ipa`) — NEEDS-REVIEW (deny-list did not grow with the flag set)
**Commit:** `39ebd7b2` build(ios): add fastlane for TestFlight uploads

**Change summary:** New lane automating `CLAUDE.md`'s mandatory pre-submission
IPA check, failing the build if a dev flag is found inlined in the bundle. The
`beta` lane runs `build` → `verify_ipa` → `upload_to_testflight`, in that order.

**Assessment:** NEEDS-REVIEW, low severity. Automating this check is a real
control improvement over a hand-run shell snippet, and the implementation is
correct on every point `CLAUDE.md` calls out: it matches on the **value**
(`:"1"`), not just the flag name; it uses `log: false` so the grep output is not
echoed; the interpolated path is a hardcoded literal, so there is no injection
surface; and `prep_web` runs `npm run build && npx cap sync ios` before archiving,
which is the gitignored-`public/` trap the same file warns about.

The gap is scope. The flag list is a copy of the one in `CLAUDE.md`, and two new
build-time flags landed in the same 24 hours:

- `VITE_DEV_BYPASS_WALLET_GATE` **should** be added. Its safety currently rests on
  `import.meta.env.DEV`; this lane is the mechanism that would catch it if that
  reasoning were ever wrong, and it is exactly the flag class the check exists for.
- `VITE_BUG_REPORT_ENABLED` should **not** be added as a failure condition — it is
  an intended ship flag that Slice 3 will deliberately flip. While the feature is
  undisclosed on the store listings, its state is worth asserting positively
  (expected absent), not treated as a dev-flag leak.

This is the same shape as the residue-key finding above: a checklist that a new
flag or key must be added to, and a change that added one without touching the
checklist.

### `android/Gemfile`, `ios/Gemfile`, `android/fastlane/Pluginfile`, `.gitignore:20,25` — NEEDS-REVIEW (unpinned gems next to release credentials)
**Commits:** `39ebd7b2`, `ad08bf03`, `736e2666`

**Change summary:** Both Gemfiles declare a floating `gem "fastlane"`; the Android
Pluginfile adds an unpinned third-party
`gem "fastlane-plugin-huawei_appgallery_connect"`. `.gitignore` lines 20 and 25
exclude `ios/Gemfile.lock` and `android/Gemfile.lock` — verified by replaying
`origin/main:.gitignore` in a scratch repo, since this checkout is on a stale
branch.

**Assessment:** NEEDS-REVIEW, low severity. With the lockfiles ignored, every
`bundle install` resolves the newest fastlane and the newest AGC plugin, then runs
that freshly-resolved Ruby with the App Store Connect `.p8`, the Play
service-account JSON, and `HUAWEI_CLIENT_SECRET` in the environment. `CLAUDE.md`'s
supply-chain rule — pin exact versions, justify new dependencies, review a native
plugin's surface before adding — applies directly to code holding release signing
credentials.

Not a live exposure: no workflow under `.github/` references fastlane
(`git grep -lni fastlane -- .github/` is empty), so these lanes are owner-run
locally today. It becomes materially worse the moment a lane is wired into CI.

Committing both `Gemfile.lock` files is the ordinary fix and costs nothing.

### `android/.env.fastlane.example`, `ios/.env.fastlane.example`, `.gitignore:49,63` — SAFE (secret hygiene verified)
**Commits:** `39ebd7b2`, `ad08bf03`, `736e2666`

**Change summary:** Templates listing credential variable NAMES, plus a
`!.env.*.example` negation so they are committable while real `.env.fastlane`
files stay ignored.

**Assessment:** SAFE, and verified rather than assumed. Replaying
`origin/main:.gitignore` in a scratch repo: `android/.env.fastlane` and
`ios/.env.fastlane` are ignored by line 49 (`.env.*`); both `.example` templates
are un-ignored by line 63 (`!.env.*.example`), which is correctly placed after the
rule it negates. No secret VALUES are committed — `ASC_KEY_ID` and `ASC_ISSUER_ID`
are identifiers already recorded in `CLAUDE.md`, and the `.p8` stays at
`~/.appstoreconnect/private_keys/`. `Appfile` reads the Play key path from env
rather than embedding it.

**Method note, because it nearly produced a false finding.** The first pass ran
`git check-ignore` in the shared checkout, which is on
`codex/internal-testing-version-code-41` and therefore carried a `.gitignore`
predating the fastlane commits. It reported the `.example` templates as ignored —
i.e. that the new negation did not work. That is the Step 1 "read from the ref,
not the tree" rule biting on a command that takes no ref. `git check-ignore` has
no `<ref>:<path>` form, so the only honest way to test ignore semantics against
`origin/main` is to materialise that `.gitignore` somewhere clean and run the
check there. Worth adding to the runbook's read-from-the-ref note: the rule covers
*any* tool that reads the working tree, not only `cat` and `grep`.

### `src/pages/Settings.jsx:488-545` — SAFE
**Commits:** `c2021239`, `92580cf6`

**Change summary:** Adds "Rate Veyrnox", "Send feedback" and the bug-report entry
to Settings.

**Assessment:** SAFE. The whole block is wrapped in `!isDeniabilityOrDemoActive()`
and each handler re-checks the same predicate internally, so a stale render under
coercion still no-ops — the two-chokepoint shape `lib/consent.js` established in
PR #1410. `openFeedback` builds a fixed `mailto:` with an encoded literal subject
and no user or wallet data. `openStoreForRating` falls back to hardcoded store
URLs chosen by UA; no data is placed in either URL. No new `fetch`, XHR,
WebSocket, `sendBeacon` or `publishAdvisorContext` call appears anywhere in this
window's `src/**` diff.

### `src/pages/SendCrypto.jsx:251-260` — SAFE (as an egress question)
**Commit:** `92580cf6`

**Change summary:** `SendDoneView` mount effect calls `recordSuccessfulSend()` and
`triggerReviewPromptIfEligible()`.

**Assessment:** SAFE with respect to I1/I2 — the OS review prompt sends nothing to
a Veyrnox backend, the plugin import is dynamic and failure-tolerant, and the
promise rejection is swallowed rather than surfaced. Placement is after a
successful send, outside the signing path; nothing about the transaction is passed
in. The local counter this writes is the subject of the REGRESSION above; that is
a wipe-residue problem, not an egress one.

### `package.json`, `package-lock.json` — SAFE
**Commit:** `92580cf6`

**Change summary:** Adds `@capacitor-community/in-app-review@8.0.0`.

**Assessment:** SAFE on the checks available to static analysis. Pinned to an exact
version (no caret), integrity hash present in the lockfile, MIT, zero transitive
dependencies, and a single `@capacitor/core >=8.0.0` peer. It is a native Capacitor
plugin, so `CLAUDE.md` requires a review of its native surface before adding —
that review is **not** evidenced in this window and this scan cannot perform it.
The plugin wraps `SKStoreReviewController` and Play's `ReviewManager`, neither of
which requires a new manifest permission; that expectation is unverified here.

### `src/pages/Subscription.jsx:724-1105` — SAFE
**Commits:** `ec2a4c31`, `8273bfc3`, `bfb3b5e6`

**Change summary:** Paywall presentation — Recommended badge, `1fr 2fr` billing
toggle, outcome-named CTAs, full Free feature list, and the AI tier card now
rendering on web with a disabled CTA.

**Assessment:** SAFE. No entitlement check, tier gate or purchase path changed.
The web CTA is disabled via `disabled={!isNative || busy || !aiPurchaseAvailable}`,
so `handleAiUpgrade` stays unreachable there, and "Restore purchases" / "Redeem
code" were **narrowed** to native rather than widened. Prices continue to come from
`offerPriceInfo` and render `—` when unresolved, so the 2026-07-27 hardcoded-savings
finding is not reopened. The badge and the asymmetric toggle steer toward Annual but
assert nothing numeric, so there is no claim to be false.

### `src/pages/__tests__/Subscription.test.jsx:116-125` — SAFE (assertion strengthened)
**Commit:** `8273bfc3`

**Change summary:** One `getByRole(/mobile only/)` assertion replaced by two
tier-scoped ones.

**Assessment:** SAFE, and a strengthening. The original would have thrown on
multiple matches once the second card rendered on web; the replacement asserts
**both** paid CTAs are disabled. No assertion was dropped.

### Coverage delta across the window — no skips added
**Commits:** `c2021239`, `92580cf6`, `bfb3b5e6`

Counted `(test|it|describe)\.(skip|fixme|todo|only)\(` in all five new test files:
`reviewPrompt.test.js`, `bugReportEnabled.test.js`, `recordableRoutes.test.js`,
`BugReportButton.test.jsx`, `WalletGate.devBypass.test.jsx` — **0 in each**. No
existing test file lost an assertion in this window. `reviewPrompt.test.js` carries
four dedicated I3 cases covering `recordSuccessfulSend`, `shouldPromptForReview`,
`markDeclined` and `triggerReviewPromptIfEligible` under coercion.

The count is a floor, not the verdict: `recordableRoutes.test.js` adds 12 green
assertions that pin an inapplicable vocabulary (see that finding), and no new test
covers panic-wipe residue for the three new keys.

### `docs/veyrnox-com-privacy-corrections-2026-09-05.md` — SAFE (disclosure-strengthening)
**Commit:** `94cbae95`

**Change summary:** Paste-ready privacy copy naming four previously undisclosed
processors — RevenueCat (`rc_user_id`), Transak (KYC), the WalletConnect relay, and
the TIP chat inference chain — plus advance copy for screen recording.

**Assessment:** SAFE, and it closes real disclosure gaps against shipped product.
It does not claim anything verified without evidence: it records that the live page
was checked by rendering (not `curl`, per the standing note) and explicitly flags
the last-updated line as unconfirmed. It correctly makes the store-disclosure
update a precondition of the Slice 3 flag flip rather than a follow-up.

## Non-security changes (summary)

`android/app/capacitor.build.gradle`, `android/capacitor.settings.gradle`,
`ios/App/CapApp-SPM/Package.swift` — `cap sync` output registering the new plugin.
`docs/security-diffs/diff-2026-09-05.md` — the earlier run's own report.
`docs/bug-report-recording-plan.md` is a design doc but is analysed above, not here,
because it records what a security change does.

## Carried forward (not in this window)

Both items from the earlier run stand; nothing in `18ee82d1..c2021239` touches
either path.

- **NEEDS-REVIEW:** `scripts/run-post-audit-qa.mjs` / `scripts/lib/postAuditDeferrals.mjs`
  still treat a missing or malformed Playwright JSON report as zero runtime skips.
- **REGRESSION:** `src/lib/useAdvisorSnapshot.js` and its publishers still allow page
  snapshots to be sent beside a persistent `device_id`.

## Scan-list maintenance

Four of this run's findings came from files matching **no** pattern in Step 2. This
is the **sixth consecutive** run with unmatched-file findings, which the runbook
names as the point to stop extending the index — recording both anyway, since the
task may only write this report.

Structural note first, because it is the actual answer. The runbook already says a
path list organised by module role cannot track a codebase organised by behaviour,
and prescribes the behavioural question. Applying that question is what found every
item here: `reviewPrompt.js` **persists state that outlives a wipe**;
`recordableRoutes.js` **gates a capture action**; `Fastfile` **decides whether a
build ships**; `Gemfile` **decides what code runs beside release credentials**. None
is an egress sink or a credential floor — the two behavioural categories the runbook
currently names — so a third and fourth belong beside them:

- **Wipe-residue writers.** Any file writing a `veyrnox-*` localStorage key. The
  panic-wipe contract is a list that must be edited whenever a key is added, and
  every miss (first-run-tour 2026-07-28, paywall nudges 2026-08-15, review counters
  today) has been found by a scan rather than by the change that caused it. This is
  cheaply mechanised — diff the window for new `'veyrnox-…'` literals and check each
  against `ALL_RESIDUE_KEYS` — and that one command produced today's REGRESSION.
- **Release-gate owners.** Any file deciding what reaches a store: pre-submission
  verification lists, signing config, upload lanes.

Concrete patterns to add:

| Add | Because |
|---|---|
| `src/lib/reviewPrompt.js` | Writes three unswept residue keys (today's REGRESSION). |
| `src/lib/bugReport/**` | Fail-closed capture gate + route allowlist for screen recording. |
| `src/components/bugReport/**` | Renders the capture entry point; composes the gate. |
| `src/wallet-core/panic.js` **as a paired file** | Not "did panic.js change" but "did anything add a key panic.js does not sweep". |
| `ios/fastlane/**`, `android/fastlane/**` | Release-gate + credential-adjacent; `verify_ipa` is now a security control. |
| `android/Gemfile`, `ios/Gemfile`, `android/fastlane/Pluginfile` | Dependency surface running with signing credentials. |
| `src/components/WalletGate.jsx` | Owns whether the wallet gate applies at all. |

Also worth folding into Step 1's read-from-the-ref note: it currently covers reading
file *contents*, and the tool that misled this run was `git check-ignore`, which has
no ref form at all. The general rule is that **any** tool reading the working tree
reads an unknown branch — for those, materialise the file from the ref into a scratch
location and run the tool there. Cost of the shortcut here would have been a
published false finding about a secret-ignoring rule.

## Post-scan re-check

Verified against `origin/main` at `c2021239f73bfd2d8cda36ff09487e4172fb8771`
(2026-09-05T13:01:47Z), which is the same tip the window closed at. Nothing landed
between window close and this check. Checkpoint 1 of 3 — checkpoints 2 (pre-auto-merge)
and 3 (post-merge) follow, per the runbook.

---
*Automated daily scan, second run of 2026-09-05. INTERNAL static analysis only — no
dynamic testing, no device verification, no on-chain confirmation. Not the
outstanding independent third-party audit.*
