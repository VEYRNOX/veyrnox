# Security Diff — 2026-07-21

Commits scanned: 41 in last 24h (range `c19ae2b9`..`origin/main`, tip `159ede7e`)
Security-sensitive files changed: 8 (2 explicit-pattern, 6 security-adjacent)

## Flagged changes

### src/lib/WalletConnectProvider.jsx:771 — SAFE (restores a control)
**Commit:** e907d648 fix(walletconnect): session-approval RASP gate read a property that never existed (H-1) (#1276)
**Change summary:** `handleApproveSession` used to read `gate.blocked` / `gate.sentence`,
properties `presignGateOrReject()` has never returned (it returns `{ proceedAllowed,
rejectCode }`), so the RASP check on WC *session approval* was a permanent no-op. Now reads
`!gate.proceedAllowed` and throws — the same fail-closed shape the three signing handlers use.
**Assessment:** SAFE — this closes the exact open finding carried in CLAUDE.md (H-1,
2026-07-20 weekly). Verified from `origin/main`: `presignGateOrReject` returns
`{ proceedAllowed, rejectCode }` on both branches (never null), and
`handleApproveRequest` / `handleSignTypedData` / `eth_sendTransaction` all gate on
`!gate.proceedAllowed`. The approval path now fails closed on a BLOCK/WARN tier (I4).
No new egress, no key-material touch. **This resolves a previously open item.**

### src/lib/WalletProvider.jsx — SAFE
**Commit:** f3358c2c fix(duress): clear pre-existing real-PIN biometric cache on duress setup (H-3) (#1261); 5bf7c999 fix(types): type useWallet() properly (#1273)
**Change summary:** `setDuressPin` now force-clears any real-PIN one-tap biometric cache
(`setBiometricUnlockEnabled(false)` + `clearUnlockSecret()` + drop decoy marker) BEFORE
provisioning the decoy vault, and does NOT swallow secure-store errors (fail closed). The
decoy-marker key is now imported from the single source of truth in `duressBiometricGuard.js`.
Separately, `useWallet()`'s `/** @type {any} */` cast was removed in favour of a proper
`.d.ts` interface (type-only, erased at build — no runtime effect).
**Assessment:** SAFE — strengthens the duress⇄biometric invariant (a coercer saying "just
use Face ID" can no longer open the REAL wallet). Runs only inside an unlocked real session,
fail-closed on secure-store failure (no half-configured decoy), deniability-preserving
(end state is byte-identical to a device that never enabled Face ID). Type change is inert.

### src/lib/authModel.js:85 — SAFE
**Commit:** f3358c2c (H-3 / P1-B)
**Change summary:** `shouldAutoCacheTypedPin` gains a third fail-closed guard —
`if (duressConfigured !== false) return false;` — so that while a duress PIN is configured,
a subsequent real-PIN unlock never re-caches the real PIN behind biometrics. Keyed on
`veyrnox-duress-configured` (a deliberate-save signal), NOT `hasDuressVault()` (chaff makes
that permanently true — the PR #714 regression).
**Assessment:** SAFE — restores a load-bearing guard on a non-chaff signal, fails closed on
an absent/unknown signal (`!== false`). Losing a convenience cache is safe; re-arming
Face-ID-opens-the-real-wallet is not. Pinned by tests in both suites.

### src/lib/duressBiometricGuard.js — SAFE (new module)
**Commit:** f3358c2c (H-3 / P1-A)
**Change summary:** New `enforceDuressBiometricInvariant()` run on cold lock-screen mount,
re-checking the duress⇄biometric invariant for the installed base that configured a duress
PIN before the setDuressPin fix shipped. `isDuressConfigured()` fails closed (unreadable
signal ⇒ assume duress configured ⇒ protective branch).
**Assessment:** SAFE — no new persisted state (both keys already exist and are already in
panic.js's residue-wipe list), zero writes/awaits on a device with no duress PIN, disarmed
end state is indistinguishable from never-enabled-Face-ID (I3). Honest residual gaps
(pre-PR#762 markerless users) are documented in the module header rather than papered over.

### src/wallet-core/panic.js — SAFE (adds controls)
**Commit:** d7f00751 fix(deniability,referral): More-drawer recents I3 tell (C-1) + referral state wipe on sync failure (K-2) (#1262)
**Change summary:** panic wipe now also clears `sessionStorage` residue
(`veyrnox-recent-pages` — More-drawer recents that named `/duress-pin`, `/stealth-wallets`,
`/panic-wipe`, a direct deniability tell that survived the post-wipe reload). Inspection
(`inspectKeyMaterial`) gains `sessionStorageResidue` / `sessionStorageVerified`, and
`clean` now requires a *verifiable* empty session store (P2-2: an unreadable store reports
`verified:false` and is NEVER called clean).
**Assessment:** SAFE — strengthens I3 (removes a deniability tell) and I4 (refuses to claim
a destruction it cannot substantiate). Single-list discipline drives both erase and
inspection. No key-material or signing change.

### src/lib/deepLinkPairing.js + DeepLinkHandler.jsx + platform manifests — SAFE (new surface)
**Commit:** de44193a feat(walletconnect): deep-links so a dApp can launch Veyrnox for pairing (#1282)
**Change summary:** Adds `veyrnox://wc?uri=` / `https://veyrnox.com/wc?uri=` deep links that
extract a raw `wc:` pairing URI and hand it to the connector. Touches AndroidManifest.xml,
Info.plist, App.entitlements, assetlinks.json, apple-app-site-association.
**Assessment:** SAFE — external deep-link input is treated as untrusted: it NEVER auto-pairs,
only PRE-FILLS the URI into the connector input for the user to review and tap Pair
(coercion-resistant, no side-effect from an external link). Hand-off is in-memory, one-shot,
non-persisted (no I3 forensic tell). `extractWcUri` rejects anything not resolving to a
`wc:` URI. Universal-link association files are static and scoped to veyrnox.com.

### index.html (CSP connect-src) — SAFE
**Commit:** 1761ca0a fix(referral): allow Supabase host in CSP connect-src so referral sync works (#1287)
**Change summary:** Adds a single host `https://jwstkrtslotnjyerzzsi.supabase.co` to
`connect-src` for server-side referral-code sync.
**Assessment:** SAFE — one explicit host, not a wildcard; all other CSP directives unchanged
(`object-src 'none'`, `frame-ancestors 'none'`, `base-uri 'self'` intact). Egress is to a
documented BUILT feature (referral sync), which per K-2 (PR #1262) makes zero backend calls
in deniability/demo sessions (I3 preserved). Controlled, scoped egress addition (I2).

### package.json (overrides: tar) — SAFE
**Commit:** e2828171 fix(deps): pin tar>=7.5.20 to clear CRITICAL advisory GHSA-23hp-3jrh-7fpw (#1291)
**Change summary:** Adds `"tar": "^7.5.20"` to the npm `overrides` block.
**Assessment:** SAFE — dependency-security remediation of a CRITICAL advisory. Build/tooling
dependency, no runtime wallet surface.

## Non-security changes (summary)
Subscription.jsx (plans copy #1285/#1286/#1290), ConnectWallet.jsx / WalletConnect.jsx
(dApp-grid refresh, Raydium/Trust-Wallet churn #1283/#1284/#1288/#1289), SendCrypto.jsx
(S4 amount-input hardening — inputMode/min/step, security-adjacent, SAFE), portfolioBalances.js
(IND-1 non-finite balance → indeterminate not $0), Documentation.jsx (S-1 user-facing
security caveats restored, #1268), Layout.jsx / useRecentPages.js (a11y + C-1 recents),
referral.js / ReferralTracker.jsx (K-2 deniability), plus test-only, CI-config
(ci.yml/playwright/e2e), and docs commits (CLAUDE.md slim, Apple/Play launch docs,
prior daily diff #1253).

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, no device
verification, no on-chain confirmation. Not the outstanding independent third-party audit.*
