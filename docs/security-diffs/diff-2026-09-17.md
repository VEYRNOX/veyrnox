# Security Diff — 2026-09-17

Commits scanned: 28 in last 24h
Security-sensitive files changed: 60

## Flagged changes

### `functions/api/{_middleware,buy/**,edge/[fn].js,rpc/[fn].js}` — SAFE
**Commit:** `ead82be8` fix(security): close eight findings from the 2026-09-16 surface audit (#2580)

**Change summary:** The Pages routes now bound request bodies before proxying, narrow upstream error logging, restrict RSS article links to the known publisher domains, and forward only a screened `X-Rc-User-Id` to the explicitly allowlisted edge function. CORS no longer reflects the Vite development origin in production.

**Assessment:** INTERNAL static review finds the controls fail closed on oversized input and do not introduce a header passthrough or route widening. The edge proxy's entitlement identifier is still an identifier rather than a credential; this commit documents that pre-existing limitation and does not represent it as authentication.

### `sql/api-security-hardening.sql:633`, `sql/get-referral-tier.sql`, `sql/referrals-select-lockdown.sql` — NEEDS-REVIEW
**Commit:** `ead82be8` fix(security): close eight findings from the 2026-09-16 surface audit (#2580)

**Change summary:** The migration revokes direct `anon` and `authenticated` execution of the referral count and tier RPCs, leaving service-role access through the explicit Pages proxy allowlist.

**Assessment:** The SQL is a correctly scoped hardening change in source, but the repo explicitly records it as ship-only. Apply it only after the production Pages service-role configuration and deployment are confirmed, then verify the resulting function privileges in the target database. No database was queried or changed by this scan.

### `supabase/functions/tip-chat/index.ts:140` and `functions/api/edge/[fn].js:53` — NEEDS-REVIEW
**Commit:** `038663df` fix(tip-chat): bound the entitlement lookup and cache both verdicts; `ead82be8` fix(security): close eight findings from the 2026-09-16 surface audit (#2580)

**Change summary:** The advisor entitlement lookup now validates its user-id before URL interpolation, bounds the negative-result cache, and avoids caching upstream 5xx responses. The Pages proxy forwards the same screened header by name, not as an attacker-controlled credential/header set.

**Assessment:** The source hardening is SAFE by INTERNAL static review, but `tip-chat` is a separately deployed Supabase Edge Function and its own change record says it has not been deployed. Confirm both target deployments before treating the live endpoint as changed; possession of a RevenueCat user id remains insufficiently bound for authentication and is not claimed resolved.

### `src/pages/{AddressBook,NotificationCentre,WatchlistPage}.jsx` — SAFE
**Commit:** `b08014e7` fix(review): deniability leaks + L2 pricing + Max precision missed by #2589's merge (#2593)

**Change summary:** Shared-store reads are disabled and already-cached contacts, price alerts, and watchlist rows are masked in demo, decoy, and hidden sessions; their mutations now reject with `DENIABILITY_BLOCKED`.

**Assessment:** This closes three I3 cache-residue/read-write paths in source. The composite deniability predicate and shared-store structural test cover the corrected paths. INTERNAL static analysis only; no device exercise was performed.

### `src/wallet-core/panic.js:517` — SAFE
**Commit:** `c3f14f7f` fix(panic): sweep the two biometric markers the wipe was missing

**Change summary:** Panic wipe and residue inspection now include the legacy biometric-preference migration marker and biometric-consent marker.

**Assessment:** Both keys are primary-session tells rather than secret material; adding them to the authoritative residue list restores fail-honest inspection and wipe coverage. Unit pins cover each marker independently.

### `index.html`, `public/_headers`, `src/wallet-core/__tests__/csp-policy.test.js` — SAFE
**Commit:** `fd5b3a86` fix(csp): reconcile index.html and _headers, and make the guard able to fail

**Change summary:** The two browser-enforced CSPs are aligned, the unused OpenRouter destination is removed, and the guard now compares the meta policy with the Pages header instead of an unreachable meta-to-meta branch.

**Assessment:** The effective policy is narrowed for the removed third-party endpoint and the cross-file regression check is no longer vacuous. This is a static source assessment, not a deployed-header verification.

### `.github/workflows/{ci,perf,perf-web,deploy-preview}.yml`, `.gitignore`, `scripts/check-tracked-env-files.mjs` — SAFE
**Commit:** `52ffb2cb` ci(security): gate new secrets in CI and pin the tracked-.env invariant (#2591); `e987393c` ci: pin the five floating action refs in the perf workflows; `94b730e7` fix(web): build Pages production bundle as release (#2575) (#2594)

**Change summary:** CI adds a checksum-verified, redacted PR-range secret scan and a tracked-env guard; unattended workflow actions are SHA-pinned; production Pages builds use the release mode that compile-time disables demo activation.

**Assessment:** These controls tighten CI and production-build posture without granting a PR new secrets or relaxing the production path. The report does not claim an external secret scan, deployment, or release build has run from this review.

### `src/lib/{coinGecko,newsLinkUrl}.js`, `src/components/CryptoNewsFeed.jsx`, `src/pages/SendCrypto.jsx`, `src/rasp/degrade.js` — SAFE
**Commit:** `ead82be8` fix(security): close eight findings from the 2026-09-16 surface audit (#2580); `1a103891` fix(ux): whole-app audit against 20 UX principles — 40 findings fixed (#2589); `b08014e7` fix(review): deniability leaks + L2 pricing + Max precision missed by #2589's merge (#2593)

**Change summary:** RSS links are constrained to HTTPS publisher domains, the fixed market basket retains an already-returned price without adding a request, ERC-20 Max uses the exact displayed decimal string, and BLOCK-tier RASP guidance no longer recommends destructive recovery steps for an integrity failure.

**Assessment:** The outbound data shape remains fixed and deniability-gated, the Max action remains opt-in and token-only, and the RASP action gates are unchanged. New tests include behavioural checks where practical; several source-content pins are regression coverage, not runtime validation.

### `CLAUDE.md`, `docs/security-diffs/diff-2026-09-16.md` — SAFE
**Commit:** `805eca70` docs(security): record that the published Transak prod pair was rotated (#2592); `fb83f70b` docs(claude): record the simulator code-signing trap and the decoy catch-22 (#2598)

**Change summary:** Documentation records an owner-confirmed rotation of a formerly published partner credential and simulator limitations for signed keychain access and decoy testing.

**Assessment:** These are record-keeping changes only. They do not expose a credential in the diff, change runtime security controls, or promote any claim to independently verified status.

## Non-security changes (summary)

38 changed files were layout, navigation, accessibility, product-copy, public-site, print, chart, or display-only corrections; they were triaged for egress, credential floors, action gates, and security claims and did not add a security finding.

## Post-scan re-check

Verified against `origin/main` at `fb83f70b` (2026-09-17T18:16:14Z). Nothing landed between window close and this check; the two deployment-dependent items above stand.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, device verification, database change, deployment verification, or on-chain confirmation.*
