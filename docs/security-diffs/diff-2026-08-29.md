# Security Diff — 2026-08-29

Commits scanned: 32 in last 24h
Security-sensitive files changed: 45

## Flagged changes

### docs/SecurityAdvisor-TIP-integration.md:3 — NEEDS-REVIEW
**Commits:** `7c633be9` / `3f92c46c` TIP signing-secret rotation documentation

**Change summary:** Records that `TIP_SIGNING_SECRET` was exposed in historical git content, has been scrubbed from the current tree, and has not yet been rotated; adds an owner-only rotation runbook.

**Assessment:** No plaintext secret was added to the current diff, and the documentation corrects the earlier client-side-HMAC description. The recorded historical exposure remains an open operational security item until the documented Worker D1 and both Supabase environments are rotated and a real authenticated screening request succeeds. This is an internal static-analysis finding; the scan cannot verify deployed secret state or rotation completion.

### functions/api/rpc/[fn].js:16 and src/api/referralApi.js:118 — SAFE
**Commit:** `0bd0a8c0` Fix open Strix findings in referral, WalletConnect, and deniability flows

**Change summary:** Removes the public proxy routes for revenue-attribution and earnings RPCs, and makes the corresponding client reads/writes honest no-ops until an owner-bound, server-authored replacement exists.

**Assessment:** The allowlist remains closed before the service-role path is used. The client fails closed rather than exposing sales metadata by public referral code; no raw-table route or fallback RPC remains in this diff.

### android/app/src/main/java/com/veyrnox/app/AndroidBiometricCachePlugin.kt:74 — SAFE
**Commit:** `0bd0a8c0` Fix open Strix findings in referral, WalletConnect, and deniability flows

**Change summary:** Adds the native BLOCK-tier RASP check before every biometric-cache read, including the unauthenticated migration read and the fast-path DEK cache read.

**Assessment:** A BLOCK result rejects before cached secret material is decrypted, while absent/unavailable state continues to fail closed to the normal unlock path. The accompanying source test pins all three read methods. Static analysis only; this does not constitute device verification.

### src/lib/WalletConnectProvider.jsx:602 and src/components/walletconnect/RequestApprovalModal.jsx:66 — SAFE
**Commits:** `0bd0a8c0` / `3f1d772f` / `aef05906` WalletConnect hardening and ERC-20 display fix

**Change summary:** Extends the WalletConnect signing chokepoint’s second-factor rejection to action-password, passkey, and biometric configurations; adds bounded ERC-20 transfer decoding so the approval modal shows the recipient and amount or warns for an unknown token.

**Assessment:** The provider still rejects before key access when any configured second factor cannot be satisfied in the WalletConnect surface. Token decoding is display-only and leaves the existing session-chain, sender-address, gas-cap, RASP, and pre-sign controls intact; unknown tokens are not presented as known assets.

### src/wallet-core/panic.js:1014 and src/wallet-core/__tests__/panic-residue-walletconnect.test.js:89 — SAFE
**Commit:** `e3424573` Fix panic wipe WalletConnect IndexedDB residue

**Change summary:** Adds a clear-then-delete sweep for WalletConnect v2 IndexedDB plus its legacy localStorage keys, and includes that database in the post-wipe honesty inspection.

**Assessment:** Clearing the active object store addresses a blocked database deletion without claiming that an unverified delete completed. The regression test checks both erasure and that a surviving database prevents `clean:true`, preserving I3/I4 honesty.

### src/api/demoClient.js:29, src/lib/sentry.js:45, and src/pages/{NFTPortfolio,SuspiciousAssets,TokenApprovals}.jsx — SAFE
**Commits:** `a17c8a5b` / `d1d120ab` / `0bd0a8c0` deniability and demo hardening

**Change summary:** Clears a stale persisted demo flag when real-vault markers exist, rechecks deniability at Sentry send time, and disables portfolio, suspicious-asset, and approval data access in deniability/demo sessions.

**Assessment:** The changed paths suppress rather than replace real-wallet gates: a stale demo state no longer disarms send-limit behavior, Sentry drops events if session state changes after initialisation, and gated data queries/remote enrichment do not run in a deniable context. Added tests cover the new negative paths.

### src/pages/SendCrypto.jsx:311 and src/pages/TokenApprovals.jsx:74 — SAFE
**Commits:** `6c4fe18e` / `d4b5ca42` / `a17c8a5b` / `0bd0a8c0` send-flow wizard and signing guards

**Change summary:** Refactors the send page into review/confirm stages and sheets, resets a stale unlimited-approval acknowledgement on transaction edits, preserves simulation/TIP query predicates across both pre-sign stages, and wraps approval revocation with two-factor and fresh-RASP checks.

**Assessment:** Static inspection found the broadcast and Digital Shield paths unchanged. The existing composite risk/approval/RASP blocks remain applied before both stage transitions, and the new revoke action refreshes local RASP immediately before mutation. The added tests are source-level regression coverage, not transaction verification.

### ios/App/App/AppDelegate.swift:23 and .github/workflows/ios-xcuitest-smoke.yml:65 — SAFE
**Commits:** `25ffc5c4` / `bbc62dda` plus XCUITest workflow fixes

**Change summary:** Makes the XCUITest fresh-install argument clear WebKit and UserDefaults state, and changes the simulator smoke to require an explicit secure-store failure for both creation and import.

**Assessment:** The test scope is now stated honestly: simulator success is not used to imply real-device provisioning. The workflow is again a required failing check rather than `continue-on-error`; no Secure Enclave or hardware-KEK status is promoted.

### .github/workflows/deploy-preview.yml:300, .zap/rules.tsv:1, and package-lock.json:12160 — SAFE
**Commits:** `e915287b` / `b1dcce23` CI DAST and dependency maintenance

**Change summary:** Adds a least-privilege passive ZAP baseline gate to deployed previews and updates the nested dev-only `brace-expansion` copy from 5.0.8 to 5.0.9.

**Assessment:** ZAP executes in the existing read-only untrusted workflow context, cannot write issues, and its result is included in the required staging gate. The lockfile change is a narrow integrity-pinned patch release; this scan found no widening of CI token or secret exposure.

## Non-security changes (summary)

Daily-report history, UI layout/copy and i18n updates, generic sheet components, and error-toast presentation maintenance. Security-focused test updates were reviewed with their associated controls above.

## Post-scan re-check

Verified against `origin/main` at `3f92c46cca3df86b121c874863d2a273401f5c33` (2026-08-29T09:04:35Z). Nothing landed between window close and this check.

---
*Automated daily scan. Internal static analysis only — no dynamic testing, device verification, on-chain confirmation, or independent audit.*
