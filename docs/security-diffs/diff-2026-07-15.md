# Security Diff — 2026-07-15

Commits scanned: 5 in last 24h
Security-sensitive files changed: 0 (strict patterns); 4 security-facing UI pages changed (copy/a11y only)

Strict flagged-pattern files (`src/wallet-core/keystore/**`, `src/wallet-core/vault.js`,
`src/rasp/**`, `src/sign-gate/**`, `src/lib/WalletConnect*`, `src/lib/WalletProvider.jsx`,
`src/lib/copySecret.js`, `src/lib/biometricUnlock.js`, `src/lib/pinAttemptGuard.js`,
`src/lib/twoFactorGate.js`, `HardwareKekPlugin.{kt,swift}`, `src/pages/CryptoSigning.jsx`,
`src/pages/ColdSign.jsx`, `src/wallet-core/evm/walletconnect/**`): **none changed.**

One non-docs commit (`dbbc6e64`) touched four security-*facing* UI pages. The commit
message and diff confirm copy/comment/test-only changes — no logic, security-control, or
design-token changes. Assessed below because the copy is safety-critical.

## Flagged changes

### src/pages/RaspSecurity.jsx:117 — SAFE
**Commit:** dbbc6e64 fix(copy-review): resolve branch-review findings on humanize-copy pass
**Change summary:** Footer wording "Browser probes are live" → "Browser probes are
active"; test updated to guard the honest-omissions footer copy ("no fake event counts",
"scan buttons", "only real detections").
**Assessment:** Honesty-strengthening. Reserves "live" for txid-verified status per the
verify-don't-assert rule, matching the browser-active stat tile. No probe logic, no RASP
gate, no tier decision touched. Strengthens I4 (fail honest). RASP suite 11/11 green.

### src/pages/DAppSecurityAlerts.jsx:52 — SAFE
**Commit:** dbbc6e64 fix(copy-review): resolve branch-review findings on humanize-copy pass
**Change summary:** "flagged = known bad. Not on the list ≠ safe" → "flagged means known
bad. Not on the list does not mean safe" (a11y: spell out math glyphs).
**Assessment:** a11y fix — screen readers skip/mis-announce `≠`, which would invert the
safety meaning of the warning. No detection logic touched. Improves honest disclosure of
the local-list limitation.

### src/pages/SecurityScanner.jsx:96 — SAFE
**Commit:** dbbc6e64 fix(copy-review): resolve branch-review findings on humanize-copy pass
**Change summary:** "Nothing flagged = nothing detected" → "Nothing flagged means nothing
detected" (a11y glyph spell-out).
**Assessment:** Same class as above. Preserves the "Never says 'safe'" / "no network"
(I2/I3-consistent) honest framing. No scanner logic touched.

### src/pages/WalletAccessReset.jsx:298,472,555 — SAFE
**Commit:** dbbc6e64 fix(copy-review): resolve branch-review findings on humanize-copy pass
**Change summary:** "= funds gone forever" → "and your funds are gone forever" (a11y);
"never leaves this phone" → "never leaves this device" (accuracy on web).
**Assessment:** a11y + accuracy. "device" is correct on the web surface (not always a
phone); consistent with I1 (keys never leave the device). Reinforces no-key-escrow /
no-server-recovery disclosure. No reset or key-handling logic touched.

### src/pages/PersonalBackup.jsx:195 — SAFE
**Commit:** dbbc6e64 fix(copy-review): resolve branch-review findings on humanize-copy pass
**Change summary:** "Forget both = funds gone forever" → "Forget both and your funds are
gone forever" (a11y glyph spell-out).
**Assessment:** Copy-only. Backup password/PIN "not stored in the file" framing preserved.
No backup crypto or export logic touched.

### src/pages/ReceiveCrypto.jsx:31 — SAFE
**Commit:** dbbc6e64 fix(copy-review): resolve branch-review findings on humanize-copy pass
**Change summary:** Restored a rationale comment for the load-bearing `?asset=` re-sync
`useEffect` (#829) that the earlier copy pass had removed.
**Assessment:** Comment-only restore; no behavior change. Not security-sensitive (address
display sync). Noted for completeness.

## Non-security changes (summary)
Docs-only commits: `417d2d7a`, `3d3e43c9` (weekly internal audit 2026-07-14),
`824cf778` (findings tracker), `7f5f22f4` (prior daily security-diff). Plus the test
file `src/pages/__tests__/RaspSecurity.test.jsx` updated alongside `dbbc6e64` (guards the
new honest-omissions footer copy).

---
*Automated daily scan. Internal analysis — static analysis only, no dynamic testing.
Not an independent audit. Nothing marked verified without a real on-chain txid.*
