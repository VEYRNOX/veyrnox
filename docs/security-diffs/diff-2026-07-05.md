# Security Diff — 2026-07-05

Commits scanned: 72 in last 24h
Security-sensitive files changed: 13 (7 on the flagged-pattern list + 6 security-adjacent)

**Overall verdict: SAFE.** No REGRESSION and no NEEDS-REVIEW findings. Every
security-sensitive change in the window is a hardening fix from the 2026-07-04
internal static-analysis audit (1C/5H/10M/8L), or a deniability/biometric UX fix,
or non-functional cleanup (debug-log removal, logging channel swap). All changes
add or tighten controls, fail closed (I4), and are honestly labelled INTERNAL /
BUILT — no "verified" claim without a txid. Static analysis only.

## Flagged changes

### src/sign-gate/presign.js:49 — SAFE
**Commit:** e50f44b2 security: fix RASP-3 HIGH — WARN tier now requires acknowledged before signing
**Change summary:** `proceedAllowed` rule changed from `decision !== CONFIRM || acknowledged`
to `decision === ALLOW || acknowledged`. Only a clean ALLOW now passes without a user tap;
WARN (rooted/jailbroken device) and CONFIRM both require an explicit acknowledgement.
**Assessment:** ADDS friction — closes a real gap where a rooted device reached the signer
on a passive toast. Fail-closed: unknown decisions are not ALLOW so they also require ack;
BLOCK stays unreachable via `signerReachable=false`. WC call-site passes `acknowledged=true`
(post-modal, unaffected); CryptoSigning passes `false` (correctly tightened). No key material
touched. Net security improvement.

### src/wallet-core/keystore/native.js:503 — SAFE
**Commit:** 6f4ded0e security: fix F-04 CRITICAL (H zeroing gap in changePassword);
651a3d9c security: fix F-03/F-08
**Change summary:** Both `getHF()` calls moved inside the `try/finally` so the hardware
factor `H` (and re-enrollment `H2`) are always zeroed even if the second biometric prompt
is cancelled/throws. F-03 removed a duplicate inline `newSaltBytes.fill(0)` that duplicated
the finally-block zeroing.
**Assessment:** Tightens key-material hygiene on an error path — previously `H` could sit
live in the JS heap if `getHF #2` rejected. Wraps the whole key lifetime in try/finally (I4).
Regression test added mocking the second `getHF` to throw. No control removed. Safe.

### src/wallet-core/keystore/web.js:422 — SAFE
**Commit:** 0366fff2 security: fix F-01 HIGH; 651a3d9c security: fix F-08 MEDIUM
**Change summary:** F-01: `H` declared before the try, assigned inside it, and the `finally`
extended to `H.fill(0)` + `C.fill(0)` on every path. F-08: `unlock` now throws
`MALFORMED_VAULT` when `kdf='kek-dek'` but `kekWrap` is absent (was silently falling through
to bare `decryptVault` → misleading "wrong password").
**Assessment:** Both fail-closed hardening. F-08 replaces a misleading error with an explicit
corruption/partial-migration signal (I4). Note: `C` (password-derived factor) is still
assigned outside the try — a residual narrow window if `randomDek()` throws after `C` is set —
but this is a net improvement over the prior state and consistent with the module's documented
JS-string zeroing limitations. Safe.

### src/wallet-core/keystore/kek.js:207 — SAFE
**Commit:** 651a3d9c security: fix F-06 (documentation)
**Change summary:** Comment-only. Notes that `zero(ikm)` wipes the JS-visible IKM copy but
the imported `CryptoKey` retains an opaque internal buffer until GC (WebCrypto limitation).
**Assessment:** No behavioural change; honest disclosure of a platform constraint. Safe.

### src/lib/WalletConnectProvider.jsx:275 — SAFE
**Commit:** 0366fff2 security: fix I3-WC-DENIABILITY HIGH; 334d50a3 security: fix
F-02-GASCAP/F-07-WC; 40bd413b (I2-WC-RELAY TODO doc)
**Change summary:** (1) I3: init `useEffect` now guards on `!isDecoy && !isHidden` so the WC
relay WebSocket never opens in a decoy/hidden session; destroy effect also fires on transition
into deniability. (2) F-02-GASCAP: new pure `resolveMaxFeePerGas()` clamps dApp-supplied
`maxFeePerGas`/`gasPrice` to `MAX_BASE_FEE_GWEI[networkKey]`, fail-closed to null on
absent/unparseable. (3) F-07-WC: `resolveSessionCaip2()` resolves the request chain
exclusively from the live session store, rejecting foreign/stale chains (feeds existing H7
gate).
**Assessment:** Three independent hardening fixes — closes an I3 egress leak (relay opened in
every duress unlock), an I5 gas-griefing vector (unbounded dApp fee), and an I4 chain-binding
bypass (prop-supplied CAIP-2). All fail closed. New red-then-green tests. Strong net
improvement.

### src/lib/WalletProvider.jsx:504 — SAFE
**Commit:** 69e6ad2e fix: remove render-body debug log that crashed WalletEntry (TDZ);
plus deniability-flow commits (9c8f3f86, 63ecb186, 32c5e1dd, 78f4cef9)
**Change summary:** Removed two `console.log` debug statements from `lock()` (one caused a
TDZ crash referencing `authModel` before declaration). Deniability commits add decoy-mode
toast/notification wiring and expose `isDecoy`/`isHidden` context values.
**Assessment:** Debug-log removal is a pure cleanup (release WebView never bridges
`console.log` anyway) and fixes a crash. Seed-overwrite-on-lock logic in `lock()` is
unchanged. No control added or removed. Safe.

### ios/App/App/HardwareKekPlugin.m:32 — SAFE
**Commit:** 41aa806f fix: iOS-F9 — replace NSLog with os_log for SE unlock trace capture
**Change summary:** All 11 `NSLog` calls replaced with `os_log_info` under subsystem
`com.veyrnox` / category `HardwareKek`, so the Secure-Enclave-unlock success marker is
collectable off-device (`log collect` / `log stream`). Dynamic args use `%{public}s`.
**Assessment:** Logging-channel change only. No secret material logged — only fixed markers,
byte lengths, and OSStatus codes; `H`'s bytes are never logged (verified in the SUCCESS
marker hunk). Commit honestly labels the feature BUILT / NOT verified (needs real iPhone SE +
Face ID). No change to the ECIES/SE crypto path. Safe.

## Security-adjacent changes (not on the flagged-pattern list, noted)

### src/wallet-core/duress.js — SAFE
**Commit:** 088613d4 / 9a8b2ee0 — add missing `hasDuressPin()` export (checks decoy vault
presence via `loadDecoy()`, fail-safe `false` on throw). Unblocks CI; no control change.

### src/wallet-core/panic.js — SAFE (with observation)
**Commit:** 6c905c2c / 72eebf91 (PW-1 LOW) — adds `sdw_session_token` to
`DENIABILITY_RESIDUE_KEYS` so a Security-Center backend session token is wiped on panic.
**Observation:** Improves I3 (previously the token survived a wipe and was correlatable to
backend `UserSession` records). The change itself is strictly additive/positive. Worth
tracking separately (not this scan) that a backend-correlatable session token exists at all —
an I5 surface — but no regression here.

### src/lib/priceFeed.js — SAFE
**Commit:** 72eebf91 (I2-LIVEPRICE-DEFAULT-ON MEDIUM) — live prices now opt-in: absent key →
`false`, storage-unavailable → `false` (was `true`). 40bd413b (I3-1 MEDIUM) —
`fetchLivePricesUsd()` throws immediately when `isDeniabilitySessionActive()`, closing a
non-React bypass of the WalletProvider I3 gate. Both tighten I2/I3, fail closed.

### src/wallet-core/evm/fees.js — SAFE
**Commit:** 40bd413b — `MAX_BASE_FEE_GWEI` now exported so SendCrypto and WalletConnectProvider
share one per-chain ceiling. Enables the F-02/F-08 gas-cap fixes. No value change.

### src/pages/SendCrypto.jsx — SAFE
**Commit:** 40bd413b (F-08-TREZOR LOW) — Trezor EVM branch clamps `maxFeePerGas` to the
per-chain cap (5000 gwei fallback for unknown networks). Regular HD send path untouched.

### src/components/security/PasskeyUnlockSettings.jsx — SAFE
**Commit:** 40bd413b (F-PASSKEY-2FA-GLOBAL MEDIUM) — adds device-wide disclosure that the
passkey applies to all wallet sessions. Honesty/disclosure only.

### src/components/security/BiometricUnlockSettings.jsx — SAFE
**Commit:** 530b9924 / 9c8f3f86 — clarifies biometric-unlock labels, restores I4 safeguards,
decoy-unlock notification. Confirm-gate test added. No key/auth logic weakened.

## Non-security changes (summary)
CI/build: `.github/workflows/android-real-device-ci.yml`, `android-e2e-emulator.yml`,
`android-e2e-tests.yml` (BrowserStack/emulator E2E), repeated `package-lock.json` /
`package.json` dependency-resolution syncs, artifact-upload v4 bump. Tests/docs: extensive
Appium Android + Playwright web E2E specs (`tests/android/**`, `tests/web/**`), `TESTING_SETUP.md`,
`docs/TEST_COVERAGE.md`, `docs/audit-2026-07-04-internal.md`, `docs/DEVICE_VERIFICATION.md`,
`docs/hardware-kek-ios-device-verification.md`, `docs/walletconnect-security-audit-2026-07-04.md`,
`CLAUDE.md`. UI: `src/pages/DuressPin.jsx` (remove-duress-PIN flow iterations),
`src/components/WalletEntry.jsx` (decoy toast + biometric self-heal), `src/lib/biometric.js`.
Chore: removal of accidental shell-artifact files (2fd939f7, 8de2378a).

---
*Automated daily scan. Static analysis only — no dynamic testing. INTERNAL analysis —
not an independent audit. Nothing herein is "verified"; on-chain verification requires a
real explorer-confirmed txid.*
