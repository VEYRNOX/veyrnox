# Security Diff — 2026-09-16

Commits scanned: 3 in last 24h
Security-sensitive files changed: 3

## Flagged changes

### `docs/security-diffs/diff-2026-09-15.md:71` — SAFE
**Commit:** `d2c6feff` docs(security-diff): daily scan 2026-09-15 — second-run addendum (#2578)

**Change summary:** Records the prior report's post-merge scan of the referral-invite fix and explicitly carries forward the inert OTA pre-unlock-request I3 concern.

**Assessment:** The addendum preserves the point-in-time finding instead of claiming it was resolved or promoting OTA to verified. It correctly limits its claims to INTERNAL static analysis and says no OTA, `src/main.jsx`, or native-code change landed in that delta.

### `workers/referral-redirect/src/invitePage.js:67` — SAFE
**Commit:** `4d6e4776` fix(referral): invite page keeps the code when copy fails (review of #2574), merged in `3ecdf6bc` (#2577)

**Change summary:** A failed clipboard copy now leaves the invitee on the page, changes the App Store control to a normal follow-up link, and writes fixed status text through `textContent`.

**Assessment:** The destination remains the fixed `APP_STORE_URL` literal supplied in the rendered anchor, so the change introduces no caller-controlled redirect. It adds no request, storage, cookie, external asset, or analytics path; the existing nonce-bound `default-src 'none'` CSP remains intact. This is INTERNAL static analysis only; the Worker is not deployment- or browser-verified here.

### `workers/referral-redirect/__tests__/redirect.test.js:144` — SAFE
**Commit:** `4d6e4776` (same)

**Change summary:** Adds five stub-DOM tests for failed and successful clipboard paths, repeated live-region announcements, and the page's no-external-font styling.

**Assessment:** No assertion was removed, and `skip`/`fixme`/`todo` counts stay at zero. These tests provide source-script regression coverage; they are not real-browser or deployed-Worker validation.

### Open item carried forward
The OTA pre-unlock-request **NEEDS-REVIEW** item in [`diff-2026-09-15.md`](docs/security-diffs/diff-2026-09-15.md) remains unresolved. No commit in this window changes `src/lib/otaUpdate.js`, `src/main.jsx`, or either native OTA implementation. Before a native OTA public key is provisioned, the owner still needs to decide whether its mandatory pre-unlock update-host request is acceptable under I3 and ensure that policy is disclosed.

## Non-security changes (summary)

None. The report addendum records security posture, and the Worker source and tests affect the public invite flow's data and navigation boundaries.

## Post-scan re-check

Verified against `origin/main` at `d2c6feff400b3d42ae1fa96958b32de4a97e5b31` (2026-09-16T09:27:55Z). Nothing landed between window close and this check.

---
*Automated daily scan. INTERNAL static analysis only — no dynamic testing, no device verification, no live deployment verification, and no on-chain confirmation.*
