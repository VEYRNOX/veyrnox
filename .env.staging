# Staging environment configuration.
# Used by `npm run build:staging` and the Deploy Preview CI workflow.
# No secrets here — those are in GitHub Actions secrets.

VITE_ENV_LABEL=Staging
VITE_ENV=staging

# Staging connects to veyrnox-staging Supabase (needed for moonpay-sign Edge Function).
# The anon key is non-secret (public JWT); the signing secret lives in Edge Function env only.
VITE_SUPABASE_URL=https://nszlbcmcysftwyudthjz.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5zemxiY21jeXNmdHd5dWR0aGp6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODUzMDM1NjMsImV4cCI6MjEwMDg3OTU2M30.K_lP3DQebiDI0QECvXX9ZoojW8OMsMFIKft7kwqS4Pc

# MoonPay on-ramp — enabled in staging, disabled everywhere else.
# The API key is injected from GitHub Actions repository variable at build time;
# it must NOT be committed here. See .github/workflows/deploy-preview.yml.
VITE_BUY_ENABLED=true
