name: Update audit snapshot

on:
  push:
    branches: [main]
  pull_request:

jobs:
  audit-snapshot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      # node 22 to match every other workflow — the lockfile is generated by
      # npm from node 22; node 20's npm resolves optional peers differently
      # and fails `npm ci` with a phantom "Missing: utf-8-validate@5" desync.
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - run: npm ci
      - run: node scripts/gen-audit-snapshot.mjs
      - name: Commit updated snapshot (main only)
        if: github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git diff --quiet public/audit-snapshot.json || (git add public/audit-snapshot.json && git commit -m "chore(ci): update npm audit snapshot")
          git push
