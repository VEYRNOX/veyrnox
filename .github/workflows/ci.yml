name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      build_release:
        description: 'Build signed production Android release artifacts'
        type: boolean
        required: false
        default: false
      build_firebase_test:
        description: 'Build the isolated signed APK used only by Firebase Test Lab'
        type: boolean
        required: false
        default: false
      build_staging_release:
        description: 'Build signed Android 1.0.1 staging artifacts for Firebase-gated Internal Testing'
        type: boolean
        required: false
        default: false

# Cancel superseded runs on the same ref to save minutes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# WalletConnect (Reown) project ID — a PUBLIC client identifier (it ships in the
# bundle; not a secret), stored as a repo Actions variable. Injected here at the
# workflow level so every `npm run build` (verify + android-release) bakes it in,
# otherwise CI / release APKs ship with the dApp Connector "Project ID required".
env:
  VITE_WALLETCONNECT_PROJECT_ID: ${{ vars.VITE_WALLETCONNECT_PROJECT_ID }}
  VITE_TIP_BASE_URL: ${{ vars.VITE_TIP_BASE_URL }}
  VITE_SUPABASE_URL: ${{ vars.VITE_SUPABASE_URL }}
  VITE_SUPABASE_ANON_KEY: ${{ vars.VITE_SUPABASE_ANON_KEY }}
  # REQUIRED for any NATIVE build, and `android-release` below is one.
  #
  # Capacitor loads the bundle from `webDir` (capacitor.config.json sets no
  # `server.url`), so the document origin on device is capacitor://localhost /
  # https://localhost. A relative `/api/*` resolves against THAT — the local
  # bundle, which serves no /api/* — so without an absolute base every edge call
  # fails in the webview: prices, klines, news, gas, referrals, telemetry, and
  # the Transak buy session. Empty is correct on WEB only, where the Pages
  # project serving the app also serves the Functions same-origin.
  #
  # #1610 introduced the runtime EDGE_BASE_UNSET throw in src/api/edgeApi.js and
  # set this var in android-e2e-emulator.yml — but NOT here, so the job that
  # actually builds the shipped AAB (`android-release`: npm run build ->
  # npx cap sync android -> bundleRelease) kept building with an empty base. The
  # throw made the breakage loud instead of silent; it did not make it go away.
  # Flagged as a NEEDS-REVIEW finding in docs/security-diffs/diff-2026-08-08.md.
  #
  # Same reason this whole block is workflow-level rather than per-step: `verify`
  # and `android-release` each run their own `npm run build`, and a var set on
  # only one of them is the exact shape of the bug this comment describes.
  VITE_EDGE_BASE: ${{ vars.VITE_EDGE_BASE }}

jobs:
  # Diff-based mainnet activation flag change gate (Critical Blocker #3,
  # Option A). Mainnet is already live and audited (2026-06-17) — this job
  # does NOT gate "is mainnet on", it gates "did this PR CHANGE a mainnet
  # activation flag". See docs/MAINNET_GATE_DESIGN.md and
  # docs/MAINNET_ACTIVATION.md.
  #
  # PROCEDURAL, not cryptographic: this job labels + comments so a human sees
  # it before merge. It does NOT fail the build and does NOT block merge by
  # itself — branch protection / required-reviewer policy is what actually
  # gates the merge, same as any other PR review.
  mainnet-flag-gate:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          # Full history so `git diff <base>...HEAD` can find the merge-base;
          # a shallow checkout would make every PR look like it changed
          # everything (or fail the diff outright).
          fetch-depth: 0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22
      - name: Detect mainnet flag changes
        id: detect
        run: |
          node scripts/detect-mainnet-flag-changes.js --base "origin/${{ github.base_ref }}" > mainnet-flag-report.json
          cat mainnet-flag-report.json
          HAS_CHANGES=$(node -e "console.log(JSON.parse(require('fs').readFileSync('mainnet-flag-report.json','utf8')).hasMainnetChanges)")
          echo "has_changes=$HAS_CHANGES" >> "$GITHUB_OUTPUT"
      - name: Label + comment on flagged PR
        if: steps.detect.outputs.has_changes == 'true'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        with:
          script: |
            const fs = require('fs');
            const report = JSON.parse(fs.readFileSync('mainnet-flag-report.json', 'utf8'));
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;

            await github.rest.issues.addLabels({
              owner, repo, issue_number,
              labels: ['mainnet-gate-required'],
            });

            const changeList = report.changes
              .map(c => `- \`${c.flag}\` in \`${c.file}:${c.lineNumber}\` — \`${c.oldValue}\` → \`${c.newValue}\``)
              .join('\n');

            await github.rest.issues.createComment({
              owner, repo, issue_number,
              body: [
                '⚠️ This PR modifies mainnet activation flags. This requires explicit review by the wallet owner before merge.',
                '',
                changeList,
                '',
                'See `docs/MAINNET_ACTIVATION.md` for the activation process and `docs/MAINNET_GATE_DESIGN.md` for why this is a procedural (not automatic-block) gate.',
              ].join('\n'),
            });

  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22
          cache: npm
      # The lockfile is authored with npm 11; Node 22 ships npm 10, which rejects
      # it (optional native dep resolution). Pin npm 11 to match the toolchain.
      - run: npm install -g npm@11
      - run: npm ci
      # LOG-1 gate (independent-audit F4.1): assert the debug-bridge log-redaction
      # patch actually applied to node_modules. A patch-package patch is silently
      # dropped when @capacitor/{android,ios} is bumped past the pinned 8.4.1, which
      # would re-open the hardware-KEK-H / vault-blob logcat leak with no build error.
      # Dedicated required step (same rationale as check:rng / check:strays below).
      - run: npm run check:log-redaction
      # RASP B4 — Android release-build hygiene gate. Asserts that the release
      # buildType in build.gradle retains the hardening properties that can silently
      # regress via merge conflicts or cap-sync rewrites: setWebContentsDebuggingEnabled
      # (gated on !BuildConfig.DEBUG), FLAG_SECURE, minifyEnabled true,
      # shrinkResources true, debuggable false, proguardFiles referenced.
      - run: npm run check:release-hygiene
      # Supply-chain gate: fail CI on any NEW critical advisory. Known/deferred
      # criticals are allowlisted with a reason in scripts/audit-gate.mjs;
      # highs/moderates are triaged for reachability in docs/SAST_FINDINGS.md.
      - run: node scripts/audit-gate.mjs
      - run: npm run lint
      # Critical Blocker #1 — R0/R1 crypto-core ring-boundary gate as a
      # DEDICATED required step (not only transitively via `npm run lint`), so it
      # can't silently drop if the lint script's scope changes — same pattern as
      # check:rng below. Fails CI (blocks merge) if any UI/backend-layer file
      # imports a key-touching crypto-core module (keystore / vault / mnemonic /
      # derivation / coldkey, or the @vault/@signing/@keys aliases). The four
      # known pre-existing crossings are an explicit labelled baseline in
      # eslint.config.js; anything NEW fails here.
      - run: npm run lint:rings
      # Whole-tree type gate. `pretest` only runs typecheck:core (the fast,
      # security-critical wallet-core slice); CI additionally holds the ENTIRE
      # tree at zero checkJs errors so UI-layer types can't silently drift.
      - run: npm run typecheck
      # Guards the guard. `stripCommentsAndStrings` in scripts/audit/lib/source-scan.mjs
      # is the shared scanner behind check:rng AND scripts/audit/eth-wallet-audit.mjs
      # (key-egress, secret-logging, Math.random, eval). Its 10-case self-test was
      # written with the PR #5bedc9d8 fail-open fix but was never wired to any runner
      # — it was named *.test.mjs, which vitest does not collect (include is
      # `src/**/*.test.{js,jsx}`), so for ~5 weeks it provided zero protection.
      #
      # This matters because a scanner regression is INVISIBLE downstream: reverting
      # the regex-literal state fix makes the scanner blank real code, and check:rng
      # still reports "✓ no insecure randomness" — verified green against a
      # deliberately mutated scanner (2026-07-26). The self-test is the only thing
      # that fails. It runs BEFORE check:rng so a broken scanner is reported as
      # itself, not as a false green from the gate that depends on it.
      - run: npm run check:source-scan
      # F5: run the CSPRNG guard as a dedicated required step, not only
      # transitively via the test `pretest` hook (so it can't silently drop if
      # the test script changes).
      - run: npm run check:rng
      # Stray-artifact gate: fail if the index contains zero-byte files or
      # shell-fragment filenames (accidental redirection artifacts — three
      # reached main before this gate existed: `per-page`, `false))`, `!(r.topic`).
      - run: npm run check:strays
      # Brief A — deniability-string scanner: fails on new wallet/set-count
      # string leaks in rendered copy (D1-D3) or raw-seed clipboard writes.
      # Dedicated required step, same rationale as check:rng above.
      - run: npm run check:deniability-strings
      # Finding-ID drift gate: fails if a user-facing surface (the feature
      # catalogue) describes a RESOLVED audit finding as still open, or if the
      # findings registry cites a source doc that does not mention the ID. This
      # is the tripwire for the 2026-07-06 C-1 mislabel; see docs/findings-registry.json.
      - run: npm run check:finding-ids
      # P2-10 (audit 2026-07-15): stop a future contributor from silently wiring
      # the dormant CertPinManager.kt into an active OkHttp construction path
      # while its PINNED_HOSTS map still carries PLACEHOLDER_ SPKI pins (that
      # combination would make OkHttp refuse every pinned host at connect time
      # — availability regression). Static grep, no runtime.
      - run: npm run check:cert-pin-manager
      # #1107: fail CI if any .env.production* file sets VITE_BYPASS_RASP=1 --
      # that disables the entire RASP pre-sign integrity gate.
      - run: npm run check:rasp-bypass
      # iOS SPM manifest gate: `npx cap sync` run on Windows rewrites
      # ios/App/CapApp-SPM/Package.swift with backslash paths. Backslash is
      # Swift's escape character, so the manifest stops parsing — but ONLY on
      # macOS, and no CI job compiles it. This repo is developed on Windows
      # while the iOS build needs a Mac, so without this gate the breakage is
      # invisible until someone opens the project there (observed 2026-07-22).
      - run: npm run check:spm-paths
      - run: npm run build

  # Unit tests run in their OWN job, deliberately NOT inside `verify`.
  #
  # WHY: steps in a job are fail-fast, so when `verify` ran the suite as its last
  # step, ANY earlier gate failing (lint, typecheck, a policy check) aborted the job
  # before `npm test` executed — and the entire 3.7k-test result silently vanished.
  # A red `verify` could not be distinguished from "tests never ran".
  #
  # That is exactly how #1236 landed: it failed `npm run typecheck`, so the two unit
  # tests it broke never reported, it was merged with `verify` red, and main stayed
  # red on unit tests undetected until #1267 fixed them a day later.
  #
  # Keeping this separate means lint/typecheck and the test suite give INDEPENDENT
  # signal, neither can mask the other, and they run in parallel. Require BOTH in
  # branch protection.
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22
          cache: npm
      # Lockfile is authored with npm 11; Node 22 ships npm 10, which rejects it.
      - run: npm install -g npm@11
      - run: npm ci
      - run: npm test

  android-unit-tests:
    runs-on: ubuntu-latest
    needs: verify
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22
      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v4
        with:
          distribution: temurin
          java-version: 21
      - name: Install JS deps and sync Capacitor Android
        run: |
          npm install -g npm@11
          npm ci
          mkdir -p dist && echo '<html></html>' > dist/index.html
          npx cap sync android
      - name: Run Android JVM unit tests (ES256 DER transcoder + RASP)
        run: |
          chmod +x gradlew
          ./gradlew :app:testDebugUnitTest --info
        working-directory: android

  android-release:
    runs-on: ubuntu-latest
    # Build production artifacts on main as before, or explicitly for a manual
    # release build. Firebase uses the isolated job below; it never consumes the
    # production AAB or its Play app-signing certificate expectation.
    if: >-
      (github.ref == 'refs/heads/main' && github.event_name != 'pull_request')
      || (github.event_name == 'workflow_dispatch'
      && (inputs.build_release == true || inputs.build_staging_release == true))
    needs: verify
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22
          cache: npm
      - run: npm install -g npm@11
      - run: npm ci
      - name: Refuse ambiguous production + staging dispatch
        if: inputs.build_release == true && inputs.build_staging_release == true
        run: |
          echo "::error::Choose either build_release or build_staging_release, never both."
          exit 1
      - name: Build web assets for the selected release channel
        run: |
          if [ "${{ inputs.build_staging_release }}" = "true" ]; then
            npm run build:staging
          else
            npm run build
          fi
      - name: Sync Capacitor
        run: npx cap sync android
      - name: Install Android staging Firebase config
        if: github.event_name == 'workflow_dispatch' && inputs.build_staging_release == true
        run: |
          set -euo pipefail
          cp .github/firebase/android/google-services.json android/app/google-services.json
          jq -e \
            '.client | any(.client_info.android_client_info.package_name == "com.veyrnox.app")' \
            android/app/google-services.json >/dev/null
      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          # Capacitor 8 / the Android Gradle build target Java 21; JDK 17 fails the
          # release build with "invalid source release: 21". (Local APK builds use 21.)
          java-version: 21
      - name: Decode keystore
        # KEYSTORE_BASE64: base64-encoded .jks file stored as a GitHub secret.
        # If absent the build falls back to debug signing (unsigned release not installable).
        # Secret reaches the script via env, never interpolated into the command string —
        # an interpolated secret is expanded into the shell source before the runner's log
        # masking sees it. (Same pattern as release-cert-guard below.)
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            echo "$KEYSTORE_BASE64" | base64 -d > android/keystore.jks
            echo "KEYSTORE_PATH=$PWD/android/keystore.jks" >> $GITHUB_ENV
          fi
      - name: Create debug keystore for release-cert guard
        run: |
          DEBUG_KS="$HOME/.android/debug.keystore"
          if [ ! -f "$DEBUG_KS" ]; then
            mkdir -p "$HOME/.android"
            keytool -genkeypair -keystore "$DEBUG_KS" -storepass android -keypass android \
              -alias androiddebugkey -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=Android Debug,O=Android,C=US"
          fi
          JAVA_USER_HOME="$(java -XshowSettings:property -version 2>&1 | grep 'user.home' | awk -F= '{print $2}' | tr -d ' ')"
          if [ -n "$JAVA_USER_HOME" ] && [ "$JAVA_USER_HOME/.android" != "$HOME/.android" ]; then
            mkdir -p "$JAVA_USER_HOME/.android"
            cp "$DEBUG_KS" "$JAVA_USER_HOME/.android/debug.keystore" 2>/dev/null || true
          fi
      - name: Build release APK
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease \
            -PRELEASE_CERT_SHA256="${{ secrets.RELEASE_CERT_SHA256 }}" \
            -PFIREBASE_OBSERVABILITY_ENABLED="${{ inputs.build_staging_release || false }}"
        working-directory: android
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS:         ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD:      ${{ secrets.KEY_PASSWORD }}
      # Play uploads take an AAB, never an APK. Without this step the pipeline
      # produced only a sideloadable APK, so every Play upload had to be built
      # by hand on a developer machine with the keystore — the exact manual
      # signing path that cost this project its upload key once already.
      #
      # A sideloaded APK also cannot verify Play Billing: Play only serves
      # billing to a build installed FROM a track. So the APK proves the app
      # runs; only this AAB can prove a purchase works.
      - name: Build release AAB (Play upload artifact)
        run: |
          ./gradlew bundleRelease \
            -PRELEASE_CERT_SHA256="${{ secrets.RELEASE_CERT_SHA256 }}" \
            -PFIREBASE_OBSERVABILITY_ENABLED="${{ inputs.build_staging_release || false }}"
        working-directory: android
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS:         ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD:      ${{ secrets.KEY_PASSWORD }}
      - name: Upload APK artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ inputs.build_staging_release && 'veyrnox-staging-apk' || 'veyrnox-release-apk' }}
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 30
      # The Play upload artifact. Download this, upload to the internal track,
      # then install FROM Play — that is the only path on which Play Billing
      # (and therefore the referral / retention offers) can be exercised.
      - name: Upload AAB artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ inputs.build_staging_release && 'veyrnox-staging-aab' || 'veyrnox-release-aab' }}
          path: android/app/build/outputs/bundle/release/app-release.aab
          retention-days: 30
      # Surfaces the versionCode actually built, so it can be checked against
      # what Play has already consumed BEFORE attempting an upload. Play rejects
      # a duplicate versionCode outright, and that is a slow thing to discover
      # at the end of an upload.
      - name: Report built versionCode
        env:
          RELEASE_CHANNEL: ${{ inputs.build_staging_release && 'staging' || 'production' }}
        run: |
          VC=$(grep -m1 'versionCode' app/build.gradle | tr -dc '0-9')
          echo "Built versionCode: ${VC}"
          echo "### Android ${RELEASE_CHANNEL} release artifacts" >> "$GITHUB_STEP_SUMMARY"
          echo "- versionCode: \`${VC}\` — must exceed every versionCode already uploaded to Play" >> "$GITHUB_STEP_SUMMARY"
          echo "- channel: \`${RELEASE_CHANNEL}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- AAB → Play internal track after both Firebase device suites pass" >> "$GITHUB_STEP_SUMMARY"
          echo "- APK → sideload smoke test only (Play Billing will NOT work)" >> "$GITHUB_STEP_SUMMARY"
        working-directory: android

  # Firebase Robo normally re-signs the app. Veyrnox's RASP gate correctly
  # rejects that, while the production AAB expects Google's Play app-signing
  # certificate rather than the upload certificate available in CI. Build a
  # separate release-hardened APK whose unique package ID and disabled bundle
  # task make it non-publishable, and whose embedded RASP fingerprint matches
  # the same upload key that signs the APK.
  android-firebase-test:
    runs-on: ubuntu-latest
    if: >-
      (github.ref == 'refs/heads/main' && github.event_name == 'push')
      || (github.event_name == 'workflow_dispatch' && inputs.build_firebase_test == true)
    needs: verify
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22
          cache: npm
      - run: npm install -g npm@11
      - run: npm ci
      - name: Install Test Lab Android Firebase config
        run: |
          set -euo pipefail
          cp .github/firebase/android/google-services.json android/app/google-services.json
          jq -e \
            '.client | any(.client_info.android_client_info.package_name == "com.veyrnox.app.firebase.testlab")' \
            android/app/google-services.json >/dev/null
      - name: Build web assets for Firebase
        run: |
          if [ "${{ inputs.build_staging_release }}" = "true" ]; then
            npm run build:staging
          else
            npm run build
          fi
      - name: Sync Capacitor
        run: npx cap sync android
      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: 21
      - name: Decode upload keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          set -euo pipefail
          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "::error::KEYSTORE_BASE64 is required for the Firebase Test Lab APK."
            exit 1
          fi
          printf '%s' "$KEYSTORE_BASE64" | base64 -d > android/keystore.jks
          echo "KEYSTORE_PATH=$PWD/android/keystore.jks" >> "$GITHUB_ENV"
      - name: Derive the APK signing certificate for the RASP gate
        id: firebase_cert
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        run: |
          set -euo pipefail
          FIREBASE_SHA=$(keytool -list -v \
            -keystore android/keystore.jks \
            -storepass "$KEYSTORE_PASSWORD" \
            -alias "$KEY_ALIAS" \
            | awk '/SHA256:/{sha=$2} END{print sha}')
          if [[ ! "$FIREBASE_SHA" =~ ^([0-9A-Fa-f]{2}:){31}[0-9A-Fa-f]{2}$ ]]; then
            echo "::error::Could not derive a valid SHA-256 fingerprint from the upload keystore."
            exit 1
          fi
          echo "sha256=$FIREBASE_SHA" >> "$GITHUB_OUTPUT"
      - name: Build non-publishable Firebase Test Lab APK
        env:
          FIREBASE_TEST_CERT_SHA256: ${{ steps.firebase_cert.outputs.sha256 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        working-directory: android
        run: |
          set -euo pipefail
          chmod +x gradlew
          ./gradlew assembleFirebaseTest \
            -PFIREBASE_TEST_CERT_SHA256="$FIREBASE_TEST_CERT_SHA256"
      - name: Upload isolated Firebase APK
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: veyrnox-firebase-test-apk
          path: android/app/build/outputs/apk/firebaseTest/app-firebaseTest.apk
          retention-days: 14
      - name: Report Firebase artifact isolation
        run: |
          {
            echo "### Android Firebase Test Lab artifact"
            echo "- package: \`com.veyrnox.app.firebase.testlab\`"
            echo "- build: release-hardened, signed with the CI upload key"
            echo "- output: APK only; \`bundleFirebaseTest\` is disabled"
            echo "- Play publication: not permitted"
          } >> "$GITHUB_STEP_SUMMARY"

  # --------------------------------------------------------------------------
  # Publish the signed release AAB to Play's INTERNAL track.
  #
  # INERT by default. The `if:` gate reads a REPOSITORY VARIABLE (not a
  # secret) so it can be evaluated at job level without leaking a secret
  # into the log. To enable, an owner must set BOTH of the following on
  # the repo:
  #   * Variable: PLAY_PUBLISH_ENABLED = 'true'
  #     (Settings → Secrets and variables → Actions → Variables)
  #   * Secret:   GOOGLE_PLAY_SERVICE_ACCOUNT_JSON
  #     (a JSON key for a service account granted "Release manager" for
  #      com.veyrnox.app on Play Console → Users & permissions → API access)
  # With either missing, this job skips — no red X, no accidental partial
  # publish. Once both are set, every main-branch merge that produces a
  # new AAB auto-publishes to Internal testing.
  #
  # Track is 'internal' by design — Internal Testing is the CLAUDE.md
  # "Pre-submission verification for 1.0.1" step. Any promotion beyond
  # this (closed/open/production) stays a manual owner action in the
  # Play Console, matching the 1.0.1 SUBMISSION HOLD.
  #
  # status: 'completed' means "fully released to the internal track" —
  # internal testers get the update. It is NOT a production rollout.
  publish-to-play-internal:
    runs-on: ubuntu-latest
    needs: android-release
    # Same trigger scope as android-release: main pushes only, never PRs.
    # AND the opt-in variable must be set — otherwise skip cleanly.
    if: >-
      github.ref == 'refs/heads/main'
      && github.event_name != 'pull_request'
      && vars.PLAY_PUBLISH_ENABLED == 'true'
    steps:
      - name: Download signed AAB artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: veyrnox-release-aab
          path: aab-artifact/
      - name: Verify AAB present
        run: |
          ls -l aab-artifact/
          test -s aab-artifact/app-release.aab
      - name: Upload to Play internal track
        # r0adkll/upload-google-play — pinned by SHA per repo convention.
        # v1.1.3 (2024-08 vintage) — the most recent stable tag on the
        # action's repo. Bump the SHA (and this comment) if a newer
        # audited version is preferred.
        uses: r0adkll/upload-google-play@935ef9c68bfaeb5c8b5c0f9bc8b6a2f7b2fce4d3 # v1.1.3
        with:
          serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.veyrnox.app
          releaseFiles: aab-artifact/app-release.aab
          track: internal
          status: completed
          # No release notes here — internal testers see the versionCode
          # in Play; leave prose to closed/open-track promotions.

  # --------------------------------------------------------------------------
  # Does this change need the release-cert guard regression test?
  #
  # The test costs a full Capacitor + Gradle release build, so it is scoped the
  # same way the CodeQL Swift scan is: always on non-PR events, and on PRs only
  # when something that could affect the guard changed.
  #
  # Fails CLOSED: non-PR events always run, and an unreadable file list runs the
  # test anyway. Skipping only ever happens on a positive "nothing relevant
  # changed" answer.
  # --------------------------------------------------------------------------
  release-guard-scope:
    name: Release-guard test needed?
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    outputs:
      needed: ${{ steps.decide.outputs.needed }}
    steps:
      - name: Decide
        id: decide
        env:
          # Values reach the script via env, never interpolated into the command
          # string — same rule swift-scope and deploy-preview follow.
          GH_TOKEN: ${{ github.token }}
          EVENT_NAME: ${{ github.event_name }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ "$EVENT_NAME" != "pull_request" ]; then
            echo "needed=true" >> "$GITHUB_OUTPUT"
            echo "Not a pull_request ($EVENT_NAME) — guard test runs."
            exit 0
          fi
          if ! files="$(gh api --paginate "repos/${REPO}/pulls/${PR_NUMBER}/files" --jq '.[].filename')"; then
            echo "needed=true" >> "$GITHUB_OUTPUT"
            echo "::warning::Could not list PR files — running the guard test anyway (fail-closed)."
            exit 0
          fi
          if printf '%s\n' "$files" | grep -qE '(^android/|^\.github/workflows/ci\.yml$)'; then
            echo "needed=true" >> "$GITHUB_OUTPUT"
            echo "android/ or ci.yml touched — guard test runs."
          else
            echo "needed=false" >> "$GITHUB_OUTPUT"
            echo "Nothing guard-relevant changed — skipping the guard test on this PR."
          fi

  # --------------------------------------------------------------------------
  # Regression gate for the release-cert guard in android/app/build.gradle.
  #
  # PR #1310 added a debug-fingerprint check; #1313 rewrote the guard and silently
  # dropped it; #1325 restored it; #1338 added this test; #1386 fixed the guard
  # after it had been inert since 2026-07-24 (issue #1373).
  #
  # This job exists because of HOW long that last one took to notice. The test
  # lived inside `android-release`, which is gated on
  # `github.ref == 'refs/heads/main' && github.event_name != 'pull_request'` — so
  # no PR could ever catch a regression, every red run on main looked caused by
  # whatever had just merged, and it survived ~15 merges over two days. Moving it
  # to its own PR-runnable job is the part of #1373 that #1386 did not address.
  # --------------------------------------------------------------------------
  release-cert-guard:
    name: Release-cert guard rejects wrong fingerprints
    runs-on: ubuntu-latest
    needs: release-guard-scope
    # This job ALWAYS runs and ALWAYS reports a conclusion, so that it can be added
    # to the branch ruleset's required checks.
    #
    # It used to carry the scope decision on the JOB `if:`, which meant GitHub
    # SKIPPED the whole job whenever nothing guard-relevant changed. That is fine
    # for an advisory check and fatal for a required one: a required check that is
    # skipped — or that never reports at all — leaves every PR sitting at
    # "Expected — waiting for status to be reported". Since the scope job returns
    # needed=false for any PR touching neither android/ nor ci.yml (most of them),
    # requiring the old shape would have blocked nearly every PR in the repo.
    #
    # So the decision moved OFF the job and ONTO the expensive steps. A not-needed
    # run costs a runner boot and a single echo, then reports success. Only the
    # `if: env.NEEDED == 'true'` steps below cost a real build.
    #
    # `!cancelled()` (not `success()`): a status-check function is what lets this
    # job run even when `needs` FAILED. A failed scope job (runner error, timeout,
    # gh outage) publishes an empty output, which `outputs.needed == 'true'` reads
    # as "not needed" — the same fail-open shape this job exists to prevent. NEEDED
    # therefore treats any non-success scope result as "run the test".
    if: ${{ !cancelled() }}
    timeout-minutes: 45
    env:
      NEEDED: ${{ needs.release-guard-scope.result != 'success' || needs.release-guard-scope.outputs.needed == 'true' }}
    steps:
      - name: Scope decision
        run: |
          echo "release-guard-scope result : ${{ needs.release-guard-scope.result }}"
          echo "release-guard-scope needed : ${{ needs.release-guard-scope.outputs.needed }}"
          echo "this job will run the test : $NEEDED"
          if [ "$NEEDED" != 'true' ]; then
            echo "Nothing guard-relevant changed — the release-cert guard test is not required for this change."
            echo "### Release-cert guard: not needed for this change" >> "$GITHUB_STEP_SUMMARY"
          fi
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        if: env.NEEDED == 'true'
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        if: env.NEEDED == 'true'
        with:
          node-version: 22
          cache: npm
      - run: npm install -g npm@11
        if: env.NEEDED == 'true'
      - run: npm ci
        if: env.NEEDED == 'true'
      - run: npm run build
        if: env.NEEDED == 'true'
      - name: Sync Capacitor
        if: env.NEEDED == 'true'
        run: npx cap sync android
      - uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        if: env.NEEDED == 'true'
        with:
          distribution: temurin
          java-version: 21
      - name: Decode keystore
        if: env.NEEDED == 'true'
        # Secret reaches the script via env, never interpolated into the command
        # string — the rule this file states for swift-scope and deploy-preview.
        # An interpolated secret is expanded into the shell source before the
        # runner's log masking sees it.
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            printf '%s' "$KEYSTORE_BASE64" | base64 -d > android/keystore.jks
            echo "KEYSTORE_PATH=$PWD/android/keystore.jks" >> $GITHUB_ENV
          fi
      - name: Guard must accept the real fingerprint and reject four wrong ones
        if: env.NEEDED == 'true'
        run: |
          set -u
          chmod +x gradlew
          # No keystore secret (forks, Dependabot) means there is no release signing
          # config and no upload cert to test against. Skip — but say so loudly, so a
          # silent skip is never mistaken for a pass.
          if [ ! -f keystore.jks ]; then
            echo "::warning::No release keystore available (fork or Dependabot PR) — release-cert guard test SKIPPED, not passed."
            echo "### Release-cert guard: SKIPPED (no keystore secret)" >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          # The guard in build.gradle fails CLOSED when no debug keystore exists —
          # it refuses to skip the debug-cert comparison silently. Every build that
          # passes RELEASE_CERT_SHA256 therefore needs a debug keystore present
          # BEFORE assembleRelease runs, including the happy-path build below.
          #
          # The guard searches user.home/.android, $HOME/.android, and
          # $USERPROFILE/.android. On GitHub runners the Gradle daemon's user.home
          # is /home/runner/.config (not /home/runner), so create the keystore at
          # $HOME/.android and copy it to the Gradle daemon's search path too.
          DEBUG_KS="$HOME/.android/debug.keystore"
          if [ ! -f "$DEBUG_KS" ]; then
            mkdir -p "$HOME/.android"
            keytool -genkeypair -keystore "$DEBUG_KS" -storepass android -keypass android \
              -alias androiddebugkey -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=Android Debug,O=Android,C=US"
          fi
          # The Gradle daemon's user.home on GitHub runners is /home/runner/.config,
          # not /home/runner. The guard checks user.home/.android too, so copy there.
          JAVA_USER_HOME="$(java -XshowSettings:property -version 2>&1 | grep 'user.home' | awk -F= '{print $2}' | tr -d ' ')"
          if [ -n "$JAVA_USER_HOME" ] && [ "$JAVA_USER_HOME/.android" != "$HOME/.android" ]; then
            mkdir -p "$JAVA_USER_HOME/.android"
            cp "$DEBUG_KS" "$JAVA_USER_HOME/.android/debug.keystore" 2>/dev/null || true
          fi

          # Happy path FIRST. Without it, a guard that rejected everything
          # unconditionally would sail through all four rejection cases below and
          # look perfectly healthy.
          if ! ./gradlew assembleRelease -PRELEASE_CERT_SHA256="$REAL_CERT_SHA256" >/tmp/happy.log 2>&1; then
            echo "::error::release guard rejected the CORRECT fingerprint — the guard is broken closed"
            tail -40 /tmp/happy.log
            exit 1
          fi
          echo "OK — accepted the real app-signing fingerprint"
          grep -F 'release-cert guard:' /tmp/happy.log || true

          expect_rejected() {
            label="$1"; shift
            if ./gradlew assembleRelease "$@" >/tmp/guard.log 2>&1; then
              echo "::error::release guard did NOT reject $label — a broken AAB could ship"
              grep -F 'release-cert guard:' /tmp/guard.log || true
              exit 1
            fi
            if ! grep -q 'RELEASE_CERT_SHA256' /tmp/guard.log; then
              echo "::error::build failed for an unrelated reason while testing $label"
              tail -30 /tmp/guard.log
              exit 1
            fi
            echo "OK — rejected $label"
          }
          expect_rejected "a blank fingerprint"     -PRELEASE_CERT_SHA256=
          expect_rejected "a malformed fingerprint" -PRELEASE_CERT_SHA256=deadbeef
          # The upload cert is the likeliest wrong value: it IS the key that just
          # signed the artifact, so it looks correct. Derived here rather than
          # hardcoded, so a key rotation cannot make this test stale.
          UPLOAD_SHA=$(keytool -list -v -keystore keystore.jks \
            -storepass "$KEYSTORE_PASSWORD" -alias "$KEY_ALIAS" \
            | grep 'SHA256:' | head -1 | awk '{print $2}')
          if [ -z "$UPLOAD_SHA" ]; then
            echo "::error::could not derive the upload cert fingerprint from keystore.jks — refusing to skip the upload-cert case"
            exit 1
          fi
          expect_rejected "the upload cert" -PRELEASE_CERT_SHA256="$UPLOAD_SHA"
          # The debug keystore cert — added by #1310, dropped by #1313, and inert
          # again until #1386. The keystore was created at the top of this step;
          # derive its fingerprint and feed it to the guard.
          DEBUG_SHA=$(keytool -list -v -keystore "$DEBUG_KS" \
            -storepass android -alias androiddebugkey \
            | grep 'SHA256:' | head -1 | awk '{print $2}')
          if [ -z "$DEBUG_SHA" ]; then
            echo "::error::could not derive the debug keystore fingerprint — refusing to skip the debug-cert case"
            exit 1
          fi
          expect_rejected "the debug keystore cert" -PRELEASE_CERT_SHA256="$DEBUG_SHA"
          echo "### Release-cert guard: all 5 cases passed" >> "$GITHUB_STEP_SUMMARY"
        working-directory: android
        env:
          KEYSTORE_PASSWORD:  ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS:          ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD:       ${{ secrets.KEY_PASSWORD }}
          REAL_CERT_SHA256:   ${{ secrets.RELEASE_CERT_SHA256 }}
