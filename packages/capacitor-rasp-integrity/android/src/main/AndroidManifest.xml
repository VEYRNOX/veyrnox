<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- No extra permissions required: all probes use APIs available without manifest permissions.
         Consumers should declare INTERNET if using Play Integrity attestation (requires network). -->
</manifest>
